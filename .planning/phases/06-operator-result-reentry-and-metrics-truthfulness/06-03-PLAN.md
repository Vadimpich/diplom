---
phase: 06-operator-result-reentry-and-metrics-truthfulness
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/lib/operator/examination-navigation.test.ts
  - frontend/lib/server/core-readiness.test.ts
  - .planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-VALIDATION.md
  - docs/02_implementation.md
autonomous: true
gap_closure: true
requirements:
  - EXAM-04
  - KSMI-03
  - RSLT-02
  - OBSV-02
must_haves:
  truths:
    - "Regression checks exist for both the history-to-result routing contract and the truthful frontend dependency gauge."
    - "Phase 06 includes a concrete validation artifact that replays the fixed history route and the fixed metrics behavior."
    - "Implementation history records the shipped gap closure without rewriting prior entries."
  artifacts:
    - path: "frontend/lib/operator/examination-navigation.test.ts"
      provides: "Automated routing regression coverage for final decision-state history re-entry"
    - path: "frontend/lib/server/core-readiness.test.ts"
      provides: "Automated probe/metrics regression coverage for truthful frontend dependency reporting"
    - path: ".planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-VALIDATION.md"
      provides: "Phase-specific verification commands and expected observations"
    - path: "docs/02_implementation.md"
      provides: "Implementation log entry for Phase 06 gap closure"
  key_links:
    - from: "frontend/lib/operator/examination-navigation.test.ts"
      to: "frontend/lib/operator/examination-navigation.ts"
      via: "final-state href assertions"
      pattern: "results\\?specialistId="
    - from: "frontend/lib/server/core-readiness.test.ts"
      to: "frontend/lib/server/core-readiness.ts"
      via: "probe normalization and gauge-value assertions"
      pattern: "probeCoreBackendReadiness"
    - from: ".planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-VALIDATION.md"
      to: "frontend/app/(app)/operator/history/page.tsx"
      via: "manual history reopen verification steps"
      pattern: "/operator/history"
---

<objective>
Add the missing regression harness and verification metadata that make the Phase 6 fixes durable and auditable.

Purpose: satisfy the audit requirement that both the history routing repair and the metrics truthfulness repair have explicit automated coverage and a reproducible validation artifact.
Output: frontend test runner wiring, two focused regression tests, a Phase 06 validation document, and one appended implementation log entry.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@docs/00_project.md
@docs/01_contract.md
@docs/02_implementation.md
@frontend/package.json
@frontend/lib/operator/examination-navigation.ts
@frontend/lib/server/core-readiness.ts
@frontend/app/(app)/operator/history/page.tsx
@frontend/app/api/ready/route.ts
@frontend/app/api/metrics/route.ts

<interfaces>
From `frontend/lib/operator/examination-navigation.ts` after Plan 01:
```ts
export function getOperatorExaminationHref(
  examinationId: number,
  specialistId: number,
  status: ExaminationStatus,
): string;
```

From `frontend/lib/server/core-readiness.ts` after Plan 02:
```ts
export async function probeCoreBackendReadiness(): Promise<{
  dependency: "up" | "down";
  serviceStatus: "ready" | "degraded";
  statusCode: number;
  dependencyGauge: 0 | 1;
}>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused frontend regression tests for routing and truthful dependency probes</name>
  <files>frontend/package.json, frontend/vitest.config.ts, frontend/lib/operator/examination-navigation.test.ts, frontend/lib/server/core-readiness.test.ts</files>
  <read_first>docs/00_project.md, docs/01_contract.md, .planning/v1.0-v1.0-MILESTONE-AUDIT.md, frontend/package.json, frontend/lib/operator/examination-navigation.ts, frontend/lib/server/core-readiness.ts, frontend/app/api/ready/route.ts, frontend/app/api/metrics/route.ts</read_first>
  <behavior>
    - Test 1: `getOperatorExaminationHref` sends `aggregated`, `decision_pending`, and `completed` to `/results`, `failed` to `/processing`, and `created` / `collecting_answers` to the generic examination page.
    - Test 2: `probeCoreBackendReadiness` normalizes a `200` upstream readiness response to `dependency="up"` and `dependencyGauge=1`.
    - Test 3: `probeCoreBackendReadiness` normalizes non-200 and thrown fetch errors to `dependency="down"` and `dependencyGauge=0`.
  </behavior>
  <action>Add a minimal frontend test harness with `vitest` only for Phase 06 regression coverage. Update `frontend/package.json` with a deterministic test script such as `vitest run`, add `frontend/vitest.config.ts` for the Node environment, and create two focused test files. `frontend/lib/operator/examination-navigation.test.ts` must assert the exact href strings for statuses `aggregated`, `decision_pending`, `completed`, `failed`, `created`, and `collecting_answers`. `frontend/lib/server/core-readiness.test.ts` must stub `global.fetch` and assert the normalized probe result fields used by `/api/ready` and `/api/metrics`, including `dependencyGauge` values `1` and `0`. Keep the harness minimal: do not add browser rendering tests or React Testing Library for this phase.</action>
  <acceptance_criteria>
    - `frontend/package.json` contains a `test` script with `vitest run`.
    - `frontend/vitest.config.ts` exists.
    - `frontend/lib/operator/examination-navigation.test.ts` contains the literals `decision_pending`, `completed`, and `/results?specialistId=`.
    - `frontend/lib/server/core-readiness.test.ts` contains `dependencyGauge`, `up`, and `down`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run test -- --run lib/operator/examination-navigation.test.ts lib/server/core-readiness.test.ts</automated>
  </verify>
  <done>Frontend regression tests now pin both the history routing contract and the truthful dependency probe normalization used by the readiness/metrics routes.</done>
</task>

<task type="auto">
  <name>Task 2: Publish Phase 06 validation evidence and implementation log sync</name>
  <files>.planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-VALIDATION.md, docs/02_implementation.md</files>
  <read_first>docs/00_project.md, docs/01_contract.md, docs/02_implementation.md, .planning/v1.0-v1.0-MILESTONE-AUDIT.md, frontend/lib/operator/examination-navigation.ts, frontend/lib/server/core-readiness.ts, frontend/app/(app)/operator/history/page.tsx, frontend/app/api/ready/route.ts, frontend/app/api/metrics/route.ts</read_first>
  <action>Create `.planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-VALIDATION.md` with exact automated commands for `npm run lint`, `npx tsc --noEmit`, `npm run build`, and the focused `vitest` suite from Task 1, plus manual verification steps that open `/operator/history`, select one `aggregated` or `completed` examination, confirm it lands on `/operator/examinations/{id}/results`, and verify `/api/metrics` reports `diplom_frontend_dependency_up{dependency=\"core_backend\"} 0` when the shared probe is forced down. Append one concise Phase 06 entry to `docs/02_implementation.md` describing both fixes: history final-state re-entry and shared-probe-backed frontend dependency metrics. Do not rewrite prior history entries.</action>
  <acceptance_criteria>
    - `.planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-VALIDATION.md` exists.
    - `.planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-VALIDATION.md` contains `npm run test`, `/operator/history`, and `diplom_frontend_dependency_up{dependency="core_backend"} 0`.
    - `docs/02_implementation.md` contains `Phase 6` or `Phase 06`.
    - `docs/02_implementation.md` contains both `history` and `metrics`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom && rg -n 'npm run test|/operator/history|diplom_frontend_dependency_up\\{dependency=\"core_backend\"\\} 0' .planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-VALIDATION.md && rg -n 'Phase 0?6|history|metrics' docs/02_implementation.md</automated>
  </verify>
  <done>Phase 06 has explicit validation evidence and an implementation log entry that make the gap closure auditable in later requirement revalidation.</done>
</task>

</tasks>

<verification>
Phase 06 is verifiable when the focused frontend tests pass and the validation doc gives a reproducible manual path for both repaired flows: history reopen and truthful dependency metrics.
</verification>

<success_criteria>
Both audit gaps now have durable automated regression coverage plus a concrete validation artifact, so later milestone revalidation can treat Phase 06 as evidence-backed rather than code-only.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-03-SUMMARY.md`
</output>
