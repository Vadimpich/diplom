---
phase: 06-operator-result-reentry-and-metrics-truthfulness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/api/ready/route.ts
  - frontend/app/api/metrics/route.ts
  - frontend/lib/api/types.ts
  - frontend/lib/server/core-readiness.ts
autonomous: true
gap_closure: true
requirements:
  - OBSV-02
must_haves:
  truths:
    - "Frontend readiness and frontend metrics derive `core_backend` health from the same live probe outcome."
    - "When the core backend readiness probe fails or returns non-200, `/api/metrics` exposes `diplom_frontend_dependency_up{dependency=\"core_backend\"} 0` instead of a hardcoded `1`."
    - "Frontend still reports its own request metrics family while keeping dependency labels low-cardinality."
  artifacts:
    - path: "frontend/lib/server/core-readiness.ts"
      provides: "Single probe helper for frontend runtime dependency truth"
    - path: "frontend/app/api/ready/route.ts"
      provides: "Frontend readiness route backed by the shared probe helper"
    - path: "frontend/app/api/metrics/route.ts"
      provides: "Prometheus exposition whose dependency gauge reflects the shared probe result"
  key_links:
    - from: "frontend/app/api/ready/route.ts"
      to: "frontend/lib/server/core-readiness.ts"
      via: "shared `probeCoreBackendReadiness` helper"
      pattern: "probeCoreBackendReadiness"
    - from: "frontend/app/api/metrics/route.ts"
      to: "frontend/lib/server/core-readiness.ts"
      via: "shared dependency truth for gauge emission"
      pattern: "diplom_frontend_dependency_up"
    - from: "frontend/lib/server/core-readiness.ts"
      to: "frontend/app/api/ready/route.ts"
      via: "same `/ready` upstream target"
      pattern: "/ready"
---

<objective>
Replace static frontend dependency metrics with the same backend-readiness truth already used by the readiness route.

Purpose: close the observability audit gap where `/api/metrics` invents `core_backend=1` even when the actual readiness probe is degraded or unreachable.
Output: one shared core-backend readiness probe helper plus updated frontend `/api/ready` and `/api/metrics` routes that emit consistent dependency status.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@.planning/phases/05-operational-trustworthiness/05-04-SUMMARY.md
@docs/00_project.md
@docs/01_contract.md
@docs/02_implementation.md
@frontend/app/api/ready/route.ts
@frontend/app/api/metrics/route.ts
@frontend/lib/api/types.ts

<interfaces>
From `docs/01_contract.md`:
```text
Frontend `/api/ready` returns { status, service, dependencies: { core_backend } }.
Frontend `/api/metrics` must expose `diplom_frontend_dependency_up{dependency}`.
Frontend readiness depends only on reachability of `core-backend`, not on PostgreSQL or RabbitMQ directly.
```

From `frontend/app/api/ready/route.ts`:
```ts
const response = await fetch(coreUrl("/ready"), { cache: "no-store" });
const dependency = response.ok ? "up" : "down";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract one shared core-backend readiness probe for frontend runtime routes</name>
  <files>frontend/app/api/ready/route.ts, frontend/lib/server/core-readiness.ts, frontend/lib/api/types.ts</files>
  <read_first>docs/00_project.md, docs/01_contract.md, .planning/v1.0-v1.0-MILESTONE-AUDIT.md, frontend/app/api/ready/route.ts, frontend/app/api/metrics/route.ts, frontend/lib/api/types.ts</read_first>
  <behavior>
    - Test 1: successful upstream `GET /ready` produces `{ status: "ready", dependencies.core_backend: "up" }`.
    - Test 2: non-200 upstream readiness produces `{ status: "degraded", dependencies.core_backend: "down" }`.
    - Test 3: fetch/network failure also produces the same degraded/down outcome so `/api/ready` and `/api/metrics` cannot diverge on exceptions.
  </behavior>
  <action>Create `frontend/lib/server/core-readiness.ts` with a server-only helper such as `probeCoreBackendReadiness()` that centralizes the `NEXT_PUBLIC_API_URL` base resolution, performs `fetch(<core>/ready, { cache: "no-store" })`, and returns a normalized shape with exact dependency values `up` or `down`, an HTTP status code to reuse, and a boolean or numeric `up` gauge value. Refactor `frontend/app/api/ready/route.ts` to call this helper instead of inlining its own fetch logic. If useful, add a small type in `frontend/lib/api/types.ts` for frontend readiness payloads so the route and future tests consume one explicit contract. Preserve the existing contract from `docs/01_contract.md`; do not add new JSON fields to the public route without updating the contract.</action>
  <acceptance_criteria>
    - `frontend/lib/server/core-readiness.ts` exists and exports `probeCoreBackendReadiness`.
    - `frontend/app/api/ready/route.ts` imports `probeCoreBackendReadiness`.
    - `frontend/app/api/ready/route.ts` no longer contains an inline `fetch(coreUrl(\"/ready\")`.
    - `frontend/lib/server/core-readiness.ts` contains `core_backend`, `up`, and `down`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint && npx tsc --noEmit && rg -n 'probeCoreBackendReadiness|core_backend|up|down' app/api/ready/route.ts lib/server/core-readiness.ts lib/api/types.ts</automated>
  </verify>
  <done>The frontend has one shared source of truth for probing core-backend readiness, and `/api/ready` uses it directly.</done>
</task>

<task type="auto">
  <name>Task 2: Make frontend Prometheus dependency metrics reflect the shared probe result</name>
  <files>frontend/app/api/metrics/route.ts, frontend/lib/server/core-readiness.ts</files>
  <read_first>docs/00_project.md, docs/01_contract.md, .planning/v1.0-v1.0-MILESTONE-AUDIT.md, frontend/lib/server/core-readiness.ts, frontend/app/api/metrics/route.ts, frontend/app/api/ready/route.ts</read_first>
  <action>Rewrite `frontend/app/api/metrics/route.ts` so it calls `probeCoreBackendReadiness()` from Task 1 and emits `diplom_frontend_dependency_up{dependency=\"core_backend\"} 1` only when the probe reports `core_backend: "up"`, otherwise `0`. Keep the existing metric family names `diplom_frontend_requests_total`, `diplom_frontend_request_duration_seconds_*`, and `diplom_frontend_dependency_up`. Make the output text deterministic and low-cardinality: use only the label `dependency=\"core_backend\"`, no free-form error text or backend response body in metric labels. Keep the response content type `text/plain; version=0.0.4` and avoid inventing a separate backend status model beyond the shared probe helper.</action>
  <acceptance_criteria>
    - `frontend/app/api/metrics/route.ts` imports `probeCoreBackendReadiness`.
    - `frontend/app/api/metrics/route.ts` contains `diplom_frontend_dependency_up{dependency=\"core_backend\"}`.
    - `frontend/app/api/metrics/route.ts` no longer contains the exact hardcoded line `diplom_frontend_dependency_up{dependency=\"core_backend\"} 1`.
    - `frontend/app/api/metrics/route.ts` still contains `text/plain; version=0.0.4`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run build && rg -n 'probeCoreBackendReadiness|diplom_frontend_dependency_up|text/plain; version=0.0.4' app/api/metrics/route.ts lib/server/core-readiness.ts && ! rg -n '^diplom_frontend_dependency_up\\{dependency=\"core_backend\"\\} 1$' app/api/metrics/route.ts</automated>
  </verify>
  <done>The frontend metrics endpoint now reports dependency truth from the same live readiness probe used by `/api/ready`, eliminating the static-success audit gap.</done>
</task>

</tasks>

<verification>
The plan is complete when `/api/ready` and `/api/metrics` both depend on one shared probe helper and the metrics route can no longer emit a permanently hardcoded success gauge.
</verification>

<success_criteria>
Frontend observability surfaces report `core_backend` dependency state from the real readiness probe path instead of a static gauge, satisfying the Phase 6 observability gap closure without changing the public contract.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-02-SUMMARY.md`
</output>
