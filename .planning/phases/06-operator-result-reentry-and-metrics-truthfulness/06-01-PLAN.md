---
phase: 06-operator-result-reentry-and-metrics-truthfulness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/(app)/operator/history/page.tsx
  - frontend/app/(app)/operator/examinations/[id]/results/page.tsx
  - frontend/components/operator/status-badge.tsx
  - frontend/lib/api/types.ts
  - frontend/lib/operator/examination-navigation.ts
autonomous: true
gap_closure: true
requirements:
  - EXAM-04
  - KSMI-03
  - RSLT-02
must_haves:
  truths:
    - "History entries with status `aggregated`, `decision_pending`, and `completed` open `/operator/examinations/{id}/results` instead of the generic examination screen."
    - "History entries with status `failed` still reopen the processing diagnostics path instead of a result page that cannot explain channel failure."
    - "Reopened final-state examinations show the same result-screen decision, baseline, metrics, and channel-contribution surface as the direct result flow."
  artifacts:
    - path: "frontend/lib/operator/examination-navigation.ts"
      provides: "One backend-status-aware routing contract for operator history links"
    - path: "frontend/app/(app)/operator/history/page.tsx"
      provides: "Operator history list that uses the shared routing contract"
    - path: "frontend/app/(app)/operator/examinations/[id]/results/page.tsx"
      provides: "Result screen copy that explicitly supports reopened `aggregated` / `decision_pending` / `completed` examinations"
  key_links:
    - from: "frontend/app/(app)/operator/history/page.tsx"
      to: "frontend/lib/operator/examination-navigation.ts"
      via: "shared `getOperatorExaminationHref` helper"
      pattern: "getOperatorExaminationHref"
    - from: "frontend/lib/operator/examination-navigation.ts"
      to: "frontend/lib/api/types.ts"
      via: "typed `ExaminationStatus` union"
      pattern: "decision_pending|completed|aggregated|failed"
    - from: "frontend/app/(app)/operator/history/page.tsx"
      to: "frontend/app/(app)/operator/examinations/[id]/results/page.tsx"
      via: "href `/operator/examinations/${id}/results`"
      pattern: "/results\\?specialistId="
---

<objective>
Repair the operator history reopen path so final decision-state examinations always land on the canonical result screen instead of falling back to the generic examination view.

Purpose: close the audit gap that leaves `EXAM-04`, `KSMI-03`, and `RSLT-02` unsatisfied even though the result DTO and result page already exist.
Output: one typed frontend routing contract for examination-history links plus result-screen/status copy that is consistent with backend-owned Phase 3/4 statuses.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-decision-delivery-to-operator/04-03-SUMMARY.md
@docs/00_project.md
@docs/01_contract.md
@docs/02_implementation.md
@frontend/lib/api/types.ts
@frontend/components/operator/status-badge.tsx
@frontend/app/(app)/operator/history/page.tsx
@frontend/app/(app)/operator/examinations/[id]/results/page.tsx

<interfaces>
From `docs/01_contract.md`:
```text
Workflow statuses allowed in the frontend contract: created, collecting_answers, ready_for_processing, processing, aggregating, aggregated, decision_pending, completed, failed.
`completed` means terminal projection decision delivery, and the result details must be read via `GET /examinations/{id}/result`.
```

From `frontend/app/(app)/operator/history/page.tsx`:
```tsx
function getExaminationHref(examinationId: number, specialistId: number, status: string) {
  if (status === "ready_for_processing" || status === "processing" || status === "aggregating" || status === "failed") {
    return `/operator/examinations/${examinationId}/processing?specialistId=${specialistId}`;
  }

  if (status === "aggregated") {
    return `/operator/examinations/${examinationId}/results?specialistId=${specialistId}`;
  }

  return `/operator/examinations/${examinationId}?specialistId=${specialistId}`;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract and correct the operator history navigation contract for final states</name>
  <files>frontend/app/(app)/operator/history/page.tsx, frontend/lib/api/types.ts, frontend/lib/operator/examination-navigation.ts</files>
  <read_first>docs/00_project.md, docs/01_contract.md, .planning/v1.0-v1.0-MILESTONE-AUDIT.md, frontend/lib/api/types.ts, frontend/app/(app)/operator/history/page.tsx, frontend/app/(app)/operator/examinations/[id]/results/page.tsx</read_first>
  <behavior>
    - Test 1: `aggregated`, `decision_pending`, and `completed` all resolve to `/operator/examinations/{id}/results?specialistId={specialistId}` per the backend-owned status model.
    - Test 2: `ready_for_processing`, `processing`, `aggregating`, and `failed` continue to resolve to `/operator/examinations/{id}/processing?specialistId={specialistId}` so failure diagnostics do not disappear.
    - Test 3: non-final intake states such as `created` and `collecting_answers` still resolve to `/operator/examinations/{id}?specialistId={specialistId}`.
  </behavior>
  <action>Create `frontend/lib/operator/examination-navigation.ts` with a typed helper `getOperatorExaminationHref(examinationId, specialistId, status)` that imports `ExaminationStatus` from `frontend/lib/api/types.ts`. Implement the exact status mapping required by `docs/01_contract.md`: `aggregated`, `decision_pending`, and `completed` must route to `/operator/examinations/${examinationId}/results?specialistId=${specialistId}`; `ready_for_processing`, `processing`, `aggregating`, and `failed` must route to `/operator/examinations/${examinationId}/processing?specialistId=${specialistId}`; `created` and `collecting_answers` must stay on `/operator/examinations/${examinationId}?specialistId=${specialistId}`. Replace the local `getExaminationHref` function in `frontend/app/(app)/operator/history/page.tsx` with the shared helper and keep the page wired only to backend statuses from `GET /examinations` per EXAM-04, KSMI-03, and RSLT-02. Do not invent any new API or frontend-only status.</action>
  <acceptance_criteria>
    - `frontend/lib/operator/examination-navigation.ts` exists and exports `getOperatorExaminationHref`.
    - `frontend/lib/operator/examination-navigation.ts` contains the status literals `"aggregated"`, `"decision_pending"`, `"completed"`, and `"failed"`.
    - `frontend/app/(app)/operator/history/page.tsx` imports `getOperatorExaminationHref`.
    - `frontend/app/(app)/operator/history/page.tsx` no longer contains `function getExaminationHref(`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint && npx tsc --noEmit && rg -n 'getOperatorExaminationHref|decision_pending|completed|aggregated|failed' app/'(app)'/operator/history/page.tsx lib/operator/examination-navigation.ts lib/api/types.ts</automated>
  </verify>
  <done>The history page uses one shared, typed routing function that sends final decision-state examinations to the result screen and preserves processing/failure navigation for non-final statuses.</done>
</task>

<task type="auto">
  <name>Task 2: Make result-entry copy and final-state badges consistent with reopened decision flows</name>
  <files>frontend/app/(app)/operator/examinations/[id]/results/page.tsx, frontend/components/operator/status-badge.tsx</files>
  <read_first>docs/00_project.md, docs/01_contract.md, .planning/phases/04-decision-delivery-to-operator/04-03-SUMMARY.md, frontend/components/operator/status-badge.tsx, frontend/app/(app)/operator/examinations/[id]/results/page.tsx</read_first>
  <action>Update `frontend/app/(app)/operator/examinations/[id]/results/page.tsx` so the page explicitly supports reopened final-state examinations from history: the loading/fallback card and page description must mention that `aggregated`, `decision_pending`, and `completed` all use this screen, and the warning text must stay specific about `decision_pending` being an in-progress delivery state rather than a missing route. Adjust `frontend/components/operator/status-badge.tsx` copy so `aggregated`, `decision_pending`, and `completed` communicate a coherent operator progression for the same result surface, without changing backend-authoritative status values. Keep `failed` as an error badge and do not reroute it to the result screen.</action>
  <acceptance_criteria>
    - `frontend/app/(app)/operator/examinations/[id]/results/page.tsx` contains the strings `aggregated`, `decision_pending`, and `completed`.
    - `frontend/app/(app)/operator/examinations/[id]/results/page.tsx` contains `К истории`.
    - `frontend/components/operator/status-badge.tsx` contains labels for `aggregated`, `decision_pending`, `completed`, and `failed`.
    - `frontend/components/operator/status-badge.tsx` still contains `variant: "danger"` for `failed`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run build && rg -n 'aggregated|decision_pending|completed|failed|К истории' app/'(app)'/operator/examinations/'[id]'/results/page.tsx components/operator/status-badge.tsx</automated>
  </verify>
  <done>The result screen and operator status labels now describe the same backend-driven final-state path that history re-entry uses.</done>
</task>

</tasks>

<verification>
History routing is fixed only when the shared navigation helper exists, the history page consumes it, and final-state result copy no longer implies that only the direct result flow can open the screen.
</verification>

<success_criteria>
Operators can reopen `aggregated`, `decision_pending`, and `completed` examinations from history and land on the same result screen used by the direct flow, while `failed` examinations keep their processing-diagnostics route.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operator-result-reentry-and-metrics-truthfulness/06-01-SUMMARY.md`
</output>
