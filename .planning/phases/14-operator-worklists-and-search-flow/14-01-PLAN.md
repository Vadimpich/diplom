# Phase 14 Plan 01

## Goal

Replace operator dashboard patterns with search-first worklists on home, specialists, and history.

## Tasks

1. Simplify shared operator registries into denser row-based tables.
2. Rebuild `/operator` as header + inline stats + search + specialist list.
3. Rebuild `/operator/specialists` and `/operator/history` as toolbar-plus-list screens.
