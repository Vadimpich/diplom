---
phase: 04-decision-delivery-to-operator
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - wimi-server/Dockerfile
  - wimi-server/scripts/entrypoint.sh
  - wimi-server/scripts/smoke.sh
  - docker-compose.yml
  - .env.example
  - docs/01_contract.md
  - README.md
  - docs/02_implementation.md
autonomous: true
requirements:
  - KSMI-01
  - KSMI-02
must_haves:
  truths:
    - "WiMi is a mandatory internal compose service for local runtime and is reachable only through core-backend."
    - "The repository tracks the exact env/config contract needed for WiMi startup and backend connectivity."
    - "Runtime verification proves WiMi starts and answers instead of relying on file-only checks."
  artifacts:
    - path: "wimi-server/Dockerfile"
      provides: "Containerized WiMi runtime using the vendor package and required Linux library exports"
    - path: "docker-compose.yml"
      provides: "Mandatory internal-only `wimi` service plus backend dependency wiring"
    - path: "docs/01_contract.md"
      provides: "Authoritative runtime/env contract for `WIMI_HOST_PORT` and `KESMI_*`"
  key_links:
    - from: "wimi-server/Dockerfile"
      to: "wimi-server/scripts/entrypoint.sh"
      via: "runtime startup command and required `LD_LIBRARY_PATH` / `LD_PRELOAD` exports"
      pattern: "WiMi -e"
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "shared `WIMI_HOST_PORT` and `KESMI_*` variables"
      pattern: "KESMI_"
    - from: "docker-compose.yml"
      to: "docs/01_contract.md"
      via: "documented Phase 4 local runtime/env contract"
      pattern: "WIMI_HOST_PORT"
---

<objective>
Make WiMi a mandatory part of the reproducible runtime stack and prove it starts plus answers together with `core-backend`.

Purpose: Deliver the deployment/runtime half of D-14 and D-15 as an explicit execution slice instead of hiding it inside backend orchestration work.
Output: Containerized WiMi runtime, compose/env wiring, documented env contract, and a real smoke command that boots the stack and probes both WiMi and backend.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md
@.planning/phases/04-decision-delivery-to-operator/04-RESEARCH.md
@.planning/phases/04-decision-delivery-to-operator/04-02-SUMMARY.md
@docs/00_project.md
@docs/01_contract.md
@docs/04_wimi_guide.md
@docker-compose.yml
@.env.example
@README.md
@wimi-server/doc/Разворачивание/На-Linux.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mandatory WiMi compose runtime and env contract</name>
  <files>wimi-server/Dockerfile, wimi-server/scripts/entrypoint.sh, wimi-server/scripts/smoke.sh, docker-compose.yml, .env.example, docs/01_contract.md, README.md, docs/02_implementation.md</files>
  <read_first>.planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md, docs/01_contract.md, docs/04_wimi_guide.md, docker-compose.yml, .env.example, README.md, wimi-server/doc/Разворачивание/На-Linux.md, wimi-server/scripts/smoke.sh</read_first>
  <action>Create `wimi-server/Dockerfile` that installs `wimi-server/bin/WiMi-0.1.7.deb`, copies an executable `wimi-server/scripts/entrypoint.sh`, exports `LD_LIBRARY_PATH=/usr/local/bin/WiMi/libs`, exports `LD_PRELOAD=/usr/local/bin/WiMi/libs/libjemalloc.so.2`, and starts `WiMi -e`. Create `wimi-server/scripts/entrypoint.sh` with the same exported library variables and exec path. Update `docker-compose.yml` to add an internal-only `wimi` service built from `./wimi-server` with no published host port, add a healthcheck using `wget -qO- http://127.0.0.1:8081/Models >/dev/null`, and make `core-backend` depend on that healthcheck. Update `.env.example` with exact keys `KESMI_BASE_URL=http://wimi:8081`, `KESMI_MODEL_ID=stub-decision-model`, `KESMI_TIMEOUT_MS=1000`, `KESMI_MAX_RETRIES=2`, and `KESMI_RETRY_BACKOFF_MS=500`. Update `docs/01_contract.md` and `README.md` so the Phase 4 runtime contract explicitly lists `KESMI_BASE_URL`, `KESMI_MODEL_ID`, `KESMI_TIMEOUT_MS`, `KESMI_MAX_RETRIES`, and `KESMI_RETRY_BACKOFF_MS`, and explains that WiMi is a mandatory internal compose service reachable only by backend. Update `wimi-server/scripts/smoke.sh` to be the canonical runtime smoke harness for this internal-only topology: it should start `wimi` with `core-backend`, probe `GET /Models` from inside the compose network via `docker compose exec core-backend sh -lc 'wget -qO- http://wimi:8081/Models >/dev/null'`, and verify backend health without exposing WiMi on the host. Append one line to `docs/02_implementation.md` noting that WiMi is now a mandatory internal compose service for Phase 4 smoke verification per D-14 and D-15.</action>
  <acceptance_criteria>
    - `wimi-server/Dockerfile` contains `WiMi-0.1.7.deb`, `LD_LIBRARY_PATH`, `LD_PRELOAD`, and `WiMi -e`.
    - `wimi-server/scripts/entrypoint.sh` contains `LD_LIBRARY_PATH`, `LD_PRELOAD`, and `exec`.
    - `docker-compose.yml` contains a top-level service named `wimi:` and `KESMI_BASE_URL` under `core-backend.environment`, and does not publish `8081:8081`.
    - `.env.example` contains `KESMI_BASE_URL=http://wimi:8081`, `KESMI_MODEL_ID=stub-decision-model`, and `KESMI_MAX_RETRIES=2`.
    - `docs/01_contract.md` contains `KESMI_BASE_URL`, `KESMI_MODEL_ID`, `KESMI_TIMEOUT_MS`, `KESMI_MAX_RETRIES`, and `KESMI_RETRY_BACKOFF_MS`, and does not require `WIMI_HOST_PORT`.
    - `README.md` contains `wimi`.
    - `wimi-server/scripts/smoke.sh` contains `docker compose exec core-backend`.
    - `docs/02_implementation.md` contains the phrase `WiMi is now a mandatory internal compose service`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom && docker compose up -d --build wimi core-backend && docker compose exec core-backend sh -lc 'wget -qO- http://wimi:8081/Models >/dev/null' && curl -fsS http://localhost:8080/health >/dev/null</automated>
  </verify>
  <done>The repository contains one reproducible WiMi container path, explicit runtime contract documentation, and a real smoke command proving WiMi plus backend come up together.</done>
</task>

</tasks>

<verification>
Plan 04 is complete when WiMi can be booted from compose with `core-backend`, `GET /Models` responds, and the backend health endpoint remains reachable with the documented `KESMI_*` runtime contract.
</verification>

<success_criteria>
Requirement IDs `KSMI-01` and `KSMI-02` are backed by a reproducible runtime path instead of manual host-side setup.
The phase has one real smoke check for D-14 and D-15, not just file-level configuration assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-decision-delivery-to-operator/04-04-SUMMARY.md`
</output>
