---
phase: 04-decision-delivery-to-operator
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - core-backend/internal/results/service.go
  - core-backend/internal/results/repository.go
  - core-backend/internal/http/results_handler.go
  - frontend/lib/api/types.ts
  - frontend/lib/api/client.ts
  - frontend/app/(app)/operator/examinations/[id]/results/page.tsx
  - README.md
  - .planning/phases/04-decision-delivery-to-operator/04-VALIDATION.md
  - docs/02_implementation.md
autonomous: true
requirements:
  - KSMI-03
  - RSLT-02
must_haves:
  truths:
    - "Operator result screen shows the real decision-delivery state and diagnostics from backend-owned `decision_result`."
    - "When no real model output exists, the UI shows the fixed not-implemented message instead of a synthetic recommendation."
    - "When decision delivery succeeds, the result screen shows normalized recommendation together with existing metrics, baseline, and channel contributions."
  artifacts:
    - path: "core-backend/internal/results/repository.go"
      provides: "Phase 4 result projection with normalized decision block"
    - path: "frontend/app/(app)/operator/examinations/[id]/results/page.tsx"
      provides: "Operator-facing recommendation/diagnostics rendering"
    - path: ".planning/phases/04-decision-delivery-to-operator/04-VALIDATION.md"
      provides: "Phase 4 validation commands and smoke expectations"
  key_links:
    - from: "core-backend/internal/results/repository.go"
      to: "frontend/lib/api/types.ts"
      via: "GET /examinations/{id}/result decision block"
      pattern: "decision"
    - from: "frontend/lib/api/client.ts"
      to: "frontend/app/(app)/operator/examinations/[id]/results/page.tsx"
      via: "typed query for result DTO"
      pattern: "getExaminationResult"
---

<objective>
Expose the normalized decision result through the existing result endpoint and operator page, then document validation and implementation outcomes for Phase 4.

Purpose: Finish the user-visible slice so the operator sees honest recommendation or delivery failure diagnostics on top of the already shipped Phase 3 aggregated result surface.
Output: Extended result projection, typed frontend contract usage, updated result page, and Phase 4 validation metadata.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md
@.planning/phases/04-decision-delivery-to-operator/04-02-SUMMARY.md
@docs/01_contract.md
@core-backend/internal/results/service.go
@core-backend/internal/results/repository.go
@core-backend/internal/http/results_handler.go
@frontend/lib/api/types.ts
@frontend/lib/api/client.ts
@frontend/app/(app)/operator/examinations/[id]/results/page.tsx

<interfaces>
From frontend/app/(app)/operator/examinations/[id]/results/page.tsx:
```tsx
const resultQuery = useQuery({
  queryKey: ["examination-result", examinationId],
  queryFn: () => apiClient.getExaminationResult(examinationId),
});
```

From core-backend/internal/http/results_handler.go:
```go
type resultsProvider interface {
	GetExaminationResult(context.Context, int64) (aggregation.AggregatedProfile, error)
	GetSpecialistHistory(context.Context, int64) (results.SpecialistHistoryResponse, error)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project decision_result through the backend result endpoint</name>
  <files>core-backend/internal/results/service.go, core-backend/internal/results/repository.go, core-backend/internal/http/results_handler.go</files>
  <read_first>.planning/phases/04-decision-delivery-to-operator/04-01-PLAN.md, .planning/phases/04-decision-delivery-to-operator/04-02-PLAN.md, docs/01_contract.md, core-backend/internal/results/service.go, core-backend/internal/results/repository.go, core-backend/internal/http/results_handler.go</read_first>
  <behavior>
    - Test 1: `GET /examinations/{id}/result` returns `status="decision_pending"` with `decision.state="pending"` or `decision.state="transport_exhausted"` when delivery is unresolved, while still including metrics, baseline, and channel contributions.
    - Test 2: successful decision delivery returns `status="completed"` and `decision.recommendation` in `allowed|risk|denied`.
    - Test 3: placeholder mode returns `decision.recommendation="unavailable"` and `decision.message="analysis_not_implemented_yet"` per D-11 and D-12.
  </behavior>
  <action>Extend `core-backend/internal/results/service.go` so `GetExaminationResult` returns a Phase 4 DTO instead of bare `aggregation.AggregatedProfile`; add a new struct `ExaminationResultResponse` that preserves all existing Phase 3 fields and adds a `Decision` object with exact JSON fields `state`, `recommendation`, `message`, `correlation_id`, `attempt_count`, `max_attempts`, `last_attempt_at`, and `diagnostics`. Update `core-backend/internal/results/repository.go` to join `decision_snapshots` onto `aggregated_examination_profiles`, allow `examinations.status IN ('decision_pending','completed')`, and populate `Decision` from normalized snapshot columns rather than raw response JSON. Update `core-backend/internal/http/results_handler.go` stub fallback so it returns `status: "decision_pending"` and `decision.message: "analysis_not_implemented_yet"` instead of only `neutral_recommendation_placeholder`.</action>
  <acceptance_criteria>
    - `core-backend/internal/results/service.go` contains `type ExaminationResultResponse struct` and `type DecisionResultView struct`.
    - `core-backend/internal/results/repository.go` contains `decision_snapshots`, `decision_pending`, and `completed`.
    - `core-backend/internal/http/results_handler.go` contains the string `analysis_not_implemented_yet`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/http ./internal/results -run 'TestExaminationResultIncludesDecisionBlock|TestDecisionFailureDiagnostics|TestCompletedResultIncludesRecommendation' -count=1</automated>
  </verify>
  <done>The backend result endpoint now serves one normalized Phase 4 DTO that keeps Phase 3 metrics intact and adds operator-safe decision delivery state.</done>
</task>

<task type="auto">
  <name>Task 2: Render the Phase 4 result experience and refresh validation metadata</name>
  <files>frontend/lib/api/types.ts, frontend/lib/api/client.ts, frontend/app/(app)/operator/examinations/[id]/results/page.tsx, README.md, .planning/phases/04-decision-delivery-to-operator/04-VALIDATION.md, docs/02_implementation.md</files>
  <read_first>docs/01_contract.md, frontend/lib/api/types.ts, frontend/lib/api/client.ts, frontend/app/(app)/operator/examinations/[id]/results/page.tsx, README.md</read_first>
  <action>Update `frontend/lib/api/types.ts` so `ExaminationStatus` includes `decision_pending` and `completed`, and `ExaminationResult` includes a `decision` block with the exact backend fields from Task 1. Update `frontend/lib/api/client.ts` only as needed so `getExaminationResult` returns the new typed DTO without raw field remapping. Rewrite `frontend/app/(app)/operator/examinations/[id]/results/page.tsx` to render a dedicated top-level decision card: show badge text `Не реализовано` when `decision.recommendation === "unavailable"` and `decision.message === "analysis_not_implemented_yet"`, show `Допуск`, `Риск`, or `Недопуск` only when `decision.recommendation` equals `allowed`, `risk`, or `denied`, and always render diagnostics lines for `correlation_id`, `attempt_count/max_attempts`, and last known `error_code` / `error_message` when present. Keep the existing metrics, baseline, explanations, and channel contributions sections below the decision card; do not remove them. Update `README.md` and `.planning/phases/04-decision-delivery-to-operator/04-VALIDATION.md` with Phase 4 verification commands: backend `go test ./... -count=1`, frontend `npm run lint && npm run build && npx tsc --noEmit`, and compose smoke `docker compose up -d --build wimi core-backend frontend && docker compose ps`. Append the Phase 4 implementation record to `docs/02_implementation.md`.</action>
  <acceptance_criteria>
    - `frontend/lib/api/types.ts` contains the status literals `"decision_pending"` and `"completed"` and an `decision:` field inside `ExaminationResult`.
    - `frontend/app/(app)/operator/examinations/[id]/results/page.tsx` contains the strings `Не реализовано`, `Допуск`, `Риск`, `Недопуск`, `correlation_id`, and `attempt_count`.
    - `.planning/phases/04-decision-delivery-to-operator/04-VALIDATION.md` contains `docker compose up -d --build wimi core-backend frontend`.
    - `docs/02_implementation.md` contains `Phase 4` and `decision delivery`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint && npm run build && npx tsc --noEmit</automated>
  </verify>
  <done>The operator result page now shows Phase 4 decision state honestly, and the repository documents how to validate the backend, frontend, and compose runtime for this phase.</done>
</task>

</tasks>

<verification>
Plan 03 is complete when backend tests pass for the result DTO, frontend validation passes for the updated result page, and Phase 4 runbook/validation artifacts are present.
</verification>

<success_criteria>
Requirement IDs `KSMI-03` and `RSLT-02` are satisfied through one backend-owned result contract and one operator-facing page that shows recommendation or integration diagnostics without inventing WiMi output.
</success_criteria>

<output>
After completion, create `.planning/phases/04-decision-delivery-to-operator/04-03-SUMMARY.md`
</output>
