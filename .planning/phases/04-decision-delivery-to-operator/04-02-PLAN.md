---
phase: 04-decision-delivery-to-operator
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - core-backend/internal/decision/service.go
  - core-backend/internal/decision/service_test.go
  - core-backend/internal/decision/repository.go
  - core-backend/internal/decision/relay.go
  - core-backend/internal/decision/relay_test.go
  - core-backend/internal/kesmi/client.go
  - core-backend/internal/kesmi/classifier.go
  - core-backend/internal/aggregation/service.go
  - core-backend/internal/aggregation/service_test.go
  - core-backend/internal/app/app.go
  - core-backend/internal/config/config.go
  - docs/02_implementation.md
autonomous: true
requirements:
  - KSMI-01
  - KSMI-02
must_haves:
  truths:
    - "Every aggregated examination enters a durable decision-delivery path that stores a snapshot before any WiMi call happens."
    - "Retry behavior is idempotent, restart-safe, and limited to retryable transport failures only, with correlation ID and attempt ledger preserved."
    - "The backend delivery subsystem consumes environment-driven WiMi config without coupling runtime containerization into the orchestration slice."
  artifacts:
    - path: "core-backend/internal/decision/service.go"
      provides: "Decision delivery orchestration and state transitions per D-04, D-07, D-08, D-09, D-10"
    - path: "core-backend/internal/decision/relay.go"
      provides: "Background poller/reconciler that resumes `decision_pending` delivery after restart"
    - path: "core-backend/internal/kesmi/client.go"
      provides: "HTTP adapter to WiMi `GET /Models` and `POST /ModelCalc`"
  key_links:
    - from: "core-backend/internal/aggregation/service.go"
      to: "core-backend/internal/decision/service.go"
      via: "post-aggregation handoff creating decision snapshot"
      pattern: "decision_pending"
    - from: "core-backend/internal/decision/service.go"
      to: "core-backend/internal/kesmi/client.go"
      via: "persisted attempt execution against WiMi"
      pattern: "ModelCalc"
    - from: "core-backend/internal/decision/relay.go"
      to: "core-backend/internal/decision/repository.go"
      via: "polling pending decision snapshots from PostgreSQL"
      pattern: "ListPendingDecisionSnapshots"
---

<objective>
Implement the core-owned decision delivery subsystem so Phase 4 has a working integration boundary, retry classification, and durable diagnostics.

Purpose: Turn the contract/schema from Plan 01 into executable workflow that preserves PostgreSQL as the source of truth and keeps WiMi behind a narrow adapter inside `core-backend`.
Output: Core decision service plus persisted relay, WiMi client, aggregation handoff, and runtime config for the future compose-wired WiMi container.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md
@.planning/phases/04-decision-delivery-to-operator/04-RESEARCH.md
@.planning/phases/04-decision-delivery-to-operator/04-01-SUMMARY.md
@docs/00_project.md
@docs/01_contract.md
@docs/04_wimi_guide.md
@core-backend/internal/processing/publisher.go
@core-backend/internal/app/app.go
@core-backend/internal/config/config.go

<interfaces>
From core-backend/internal/config/config.go:
```go
type Config struct {
	RabbitMQURL       string
	BaselineBaseURL   string
	BaselineTimeout   time.Duration
	OutboxPollInterval time.Duration
	OutboxMaxAttempts int32
}
```

From core-backend/internal/processing/publisher.go:
```go
type RelayConfig struct {
	BrokerURL    string
	PollInterval time.Duration
	MaxAttempts  int32
}
```

From docs/04_wimi_guide.md:
```text
WiMi integration env must include:
KESMI_BASE_URL
KESMI_MODEL_ID
KESMI_TIMEOUT_MS
KESMI_MAX_RETRIES
KESMI_RETRY_BACKOFF_MS
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build persisted decision delivery relay and WiMi error classification</name>
  <files>core-backend/internal/decision/service.go, core-backend/internal/decision/service_test.go, core-backend/internal/decision/repository.go, core-backend/internal/decision/relay.go, core-backend/internal/decision/relay_test.go, core-backend/internal/kesmi/client.go, core-backend/internal/kesmi/classifier.go, core-backend/internal/aggregation/service.go, core-backend/internal/aggregation/service_test.go, core-backend/internal/app/app.go, core-backend/internal/config/config.go</files>
  <read_first>.planning/phases/04-decision-delivery-to-operator/04-01-PLAN.md, docs/01_contract.md, docs/04_wimi_guide.md, core-backend/internal/decision/service_test.go, core-backend/internal/aggregation/service.go, core-backend/internal/aggregation/service_test.go, core-backend/internal/processing/publisher.go, core-backend/internal/app/app.go, core-backend/internal/config/config.go</read_first>
  <behavior>
    - Test 1: after aggregation success, `aggregation.Service` creates or refreshes one `decision_snapshot` with examination status `decision_pending`, `max_attempts=2`, and recommendation `unavailable` per D-04 and D-11.
    - Test 2: WiMi timeout, network errors, HTTP `500..599`, and response codes classified as `pool_busy` or `pool_exhausted` are retried until `max_attempts=2`; all other classifier outcomes stay non-retryable per D-07, D-08, and D-09.
    - Test 3: successful delivery before the real model exists normalizes into `DecisionResult{State:"succeeded", Recommendation:"unavailable", Message:"analysis_not_implemented_yet"}` and updates the examination to `completed`; exhausted or business failures also become terminal operator-visible outcomes with `examinations.status='completed'` plus normalized `decision.state` diagnostics per D-10, D-11, D-12, and D-06a.
    - Test 4: `decision.Relay` polls PostgreSQL for `decision_pending` snapshots after restart and resumes undelivered attempts without duplicating a finished business submission.
  </behavior>
  <action>Add RED-to-green coverage in `core-backend/internal/decision/service_test.go`, `core-backend/internal/decision/relay_test.go`, and `core-backend/internal/aggregation/service_test.go` for `TestDecisionSuccessMarksCompleted`, `TestDecisionRelayResumesPendingSnapshots`, terminal `business_error`, and terminal `transport_exhausted` projection after retry exhaustion. Create `core-backend/internal/kesmi/client.go` with an HTTP client that calls `GET /Models` for liveness/model presence and `POST /ModelCalc` for decision execution using env-driven config keys `KESMI_BASE_URL`, `KESMI_MODEL_ID`, `KESMI_TIMEOUT_MS`, `KESMI_MAX_RETRIES`, and `KESMI_RETRY_BACKOFF_MS`. Add `core-backend/internal/kesmi/classifier.go` that maps timeout, network failure, HTTP `5xx`, and WiMi busy-pool errors to `retryable=true`, and maps parameter validation, unknown model, type mismatch, and constraint errors to `retryable=false`. Implement `core-backend/internal/decision/service.go` plus repository methods so the Phase 3 aggregation success path only persists the decision snapshot and the first append-only attempt row, leaving examination status at `decision_pending` and recommendation fixed to `unavailable` with message `analysis_not_implemented_yet` per D-11 and D-12. Create `core-backend/internal/decision/relay.go` as a background poller/reconciler, modeled after the existing persisted relay pattern, that loads rows from `ListPendingDecisionSnapshots`, executes the WiMi call, persists correlation IDs and attempt history, and resumes `decision_pending` delivery after crash/restart. On pre-model delivery success set `decision_snapshots.status='succeeded'`, keep `recommendation='unavailable'`, keep `message='analysis_not_implemented_yet'`, set `completed_at`, set `raw_response_available=true`, and update `examinations.status='completed'`. On terminal business failure set snapshot state `business_error`, keep `recommendation='unavailable'`, store `error_code`, `error_message`, raw response JSON, set `completed_at`, and update `examinations.status='completed'` so the operator sees a final failed decision state rather than indefinite pending. On exhausted retryable failure set snapshot state `transport_exhausted`, keep `recommendation='unavailable'`, increment `attempt_count`, preserve the last correlation ID, set `completed_at`, and update `examinations.status='completed'` after the retry budget is exhausted. Extend `core-backend/internal/config/config.go` with exact config fields `KESMIBaseURL`, `KESMITimeout`, `KESMIModelID`, `KESMIMaxRetries`, and `KESMIRetryBackoff`. Wire the decision relay in `core-backend/internal/app/app.go` and inject snapshot creation into the aggregation success path without introducing a new standalone adapter service, per D-16.</action>
  <acceptance_criteria>
    - `core-backend/internal/config/config.go` contains the identifiers `KESMIBaseURL`, `KESMITimeout`, `KESMIModelID`, `KESMIMaxRetries`, and `KESMIRetryBackoff`.
    - `core-backend/internal/kesmi/classifier.go` contains the strings `pool_busy`, `pool_exhausted`, `business_error`, and `transport_exhausted`.
    - `core-backend/internal/decision/service.go` contains the literal `analysis_not_implemented_yet` and the examination status literal `decision_pending`.
    - `core-backend/internal/decision/relay.go` contains `ListPendingDecisionSnapshots` and `Run(ctx context.Context)`.
    - `core-backend/internal/aggregation/service.go` contains an invocation into the decision subsystem after aggregated profile persistence.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/decision ./internal/kesmi ./internal/aggregation -run 'TestDecisionPendingTransition|TestRetriesOnlyTransportFailures|TestDecisionSuccessMarksCompleted|TestDecisionBusinessErrorMarksCompleted|TestDecisionTransportExhaustedMarksCompleted|TestDecisionRelayResumesPendingSnapshots' -count=1</automated>
  </verify>
  <done>Core backend now owns persisted decision snapshot creation, restart-safe relay/recovery, WiMi classification, bounded retries, and `completed` transition without leaking raw WiMi contracts into the domain/UI boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Record backend delivery implementation outcome</name>
  <files>docs/02_implementation.md</files>
  <read_first>.planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md, docs/02_implementation.md</read_first>
  <action>Append one concise entry to `docs/02_implementation.md` stating that Phase 4 backend planning/implementation now includes persisted `decision_pending` snapshot creation, restart-safe relay recovery, WiMi classifier boundaries, and honest pre-model completion semantics with `analysis_not_implemented_yet`.</action>
  <acceptance_criteria>
    - `docs/02_implementation.md` contains the phrases `decision_pending`, `relay recovery`, and `analysis_not_implemented_yet`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom && rg -n 'decision_pending|relay recovery|analysis_not_implemented_yet' docs/02_implementation.md</automated>
  </verify>
  <done>The implementation log explicitly records the backend Phase 4 delivery shape for future execution/audit.</done>
</task>

</tasks>

<verification>
Plan 02 is complete when targeted Go tests pass for persisted decision relay/recovery and the backend delivery path can hand off aggregated profiles into `decision_pending` without inventing UI-facing WiMi contracts.
</verification>

<success_criteria>
Requirement IDs `KSMI-01` and `KSMI-02` are implemented through a persisted, retry-aware integration boundary.
The backend delivery path consumes the documented `KESMI_*` config and leaves runtime stack boot verification to the dedicated compose plan.
</success_criteria>

<output>
After completion, create `.planning/phases/04-decision-delivery-to-operator/04-02-SUMMARY.md`
</output>
