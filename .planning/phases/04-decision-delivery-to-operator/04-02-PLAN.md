---
phase: 04-decision-delivery-to-operator
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - core-backend/internal/decision/service.go
  - core-backend/internal/decision/repository.go
  - core-backend/internal/kesmi/client.go
  - core-backend/internal/kesmi/classifier.go
  - core-backend/internal/aggregation/service.go
  - core-backend/internal/app/app.go
  - core-backend/internal/config/config.go
  - wimi-server/Dockerfile
  - wimi-server/scripts/entrypoint.sh
  - docker-compose.yml
  - .env.example
  - docs/02_implementation.md
autonomous: true
requirements:
  - KSMI-01
  - KSMI-02
must_haves:
  truths:
    - "Every aggregated examination enters a durable decision-delivery path that stores a snapshot before any WiMi call happens."
    - "Retry behavior is idempotent and limited to retryable transport failures only, with correlation ID and attempt ledger preserved."
    - "WiMi starts with the local stack and is reachable from core-backend through environment-driven config."
  artifacts:
    - path: "core-backend/internal/decision/service.go"
      provides: "Decision delivery orchestration and state transitions per D-04, D-07, D-08, D-09, D-10"
    - path: "core-backend/internal/kesmi/client.go"
      provides: "HTTP adapter to WiMi `GET /Models` and `POST /ModelCalc`"
    - path: "docker-compose.yml"
      provides: "Mandatory WiMi runtime wired into the existing compose stack per D-14 and D-15"
  key_links:
    - from: "core-backend/internal/aggregation/service.go"
      to: "core-backend/internal/decision/service.go"
      via: "post-aggregation handoff creating decision snapshot"
      pattern: "decision_pending"
    - from: "core-backend/internal/decision/service.go"
      to: "core-backend/internal/kesmi/client.go"
      via: "persisted attempt execution against WiMi"
      pattern: "ModelCalc"
    - from: "docker-compose.yml"
      to: "core-backend/internal/config/config.go"
      via: "KESMI_* env vars"
      pattern: "KESMI_"
---

<objective>
Implement the core-owned decision delivery subsystem and mandatory WiMi runtime so Phase 4 has a working integration boundary, retry classification, and durable diagnostics.

Purpose: Turn the contract/schema from Plan 01 into executable workflow that preserves PostgreSQL as the source of truth and keeps WiMi behind a narrow adapter inside `core-backend`.
Output: Core decision service, WiMi client, aggregation handoff, runtime config, and compose-wired WiMi container.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md
@.planning/phases/04-decision-delivery-to-operator/04-RESEARCH.md
@.planning/phases/04-decision-delivery-to-operator/04-01-SUMMARY.md
@docs/00_project.md
@docs/01_contract.md
@docs/04_wimi_guide.md
@core-backend/internal/processing/publisher.go
@core-backend/internal/app/app.go
@core-backend/internal/config/config.go
@docker-compose.yml
@.env.example

<interfaces>
From core-backend/internal/config/config.go:
```go
type Config struct {
	RabbitMQURL       string
	BaselineBaseURL   string
	BaselineTimeout   time.Duration
	OutboxPollInterval time.Duration
	OutboxMaxAttempts int32
}
```

From core-backend/internal/processing/publisher.go:
```go
type RelayConfig struct {
	BrokerURL    string
	PollInterval time.Duration
	MaxAttempts  int32
}
```

From docs/04_wimi_guide.md:
```text
WiMi integration env must include:
KESMI_BASE_URL
KESMI_MODEL_ID
KESMI_TIMEOUT_MS
KESMI_MAX_RETRIES
KESMI_RETRY_BACKOFF_MS
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build persisted decision delivery and WiMi error classification</name>
  <files>core-backend/internal/decision/service.go, core-backend/internal/decision/repository.go, core-backend/internal/kesmi/client.go, core-backend/internal/kesmi/classifier.go, core-backend/internal/aggregation/service.go, core-backend/internal/app/app.go, core-backend/internal/config/config.go</files>
  <read_first>.planning/phases/04-decision-delivery-to-operator/04-01-PLAN.md, docs/01_contract.md, docs/04_wimi_guide.md, core-backend/internal/aggregation/service.go, core-backend/internal/processing/publisher.go, core-backend/internal/app/app.go, core-backend/internal/config/config.go</read_first>
  <behavior>
    - Test 1: after aggregation success, `aggregation.Service` creates or refreshes one `decision_snapshot` with examination status `decision_pending`, `max_attempts=2`, and recommendation `unavailable` per D-04 and D-11.
    - Test 2: WiMi timeout, network errors, HTTP `500..599`, and response codes classified as `pool_busy` or `pool_exhausted` are retried until `max_attempts=2`; all other classifier outcomes stay non-retryable per D-07, D-08, and D-09.
    - Test 3: successful WiMi response normalizes into `DecisionResult{State:"succeeded"}` and updates the examination to `completed`; exhausted or business failures preserve diagnostics without setting `completed` per D-10.
  </behavior>
  <action>Create `core-backend/internal/kesmi/client.go` with an HTTP client that calls `GET /Models` for liveness/model presence and `POST /ModelCalc` for decision execution using env-driven config keys `KESMI_BASE_URL`, `KESMI_MODEL_ID`, `KESMI_TIMEOUT_MS`, `KESMI_MAX_ATTEMPTS`, and `KESMI_RETRY_BACKOFF_MS`. Add `core-backend/internal/kesmi/classifier.go` that maps timeout/network/HTTP `5xx` and WiMi busy-pool errors to `retryable=true`, and maps parameter validation, unknown model, type mismatch, and constraint errors to `retryable=false`. Implement `core-backend/internal/decision/service.go` plus repository methods so the Phase 3 aggregation success path calls decision delivery immediately after persisting the aggregated profile: create or update one snapshot row, insert attempt row `attempt_number=1`, persist `correlation_id`, and keep examination status `decision_pending` until normalized success. On normalized success set `decision_snapshots.status='succeeded'`, `recommendation` to one of `allowed`, `risk`, `denied`, set `completed_at`, and update `examinations.status='completed'`. On business failure set snapshot state `business_error`, keep `recommendation='unavailable'`, store `error_code`, `error_message`, and raw response JSON, and leave examination status `decision_pending`. On exhausted retryable failure set snapshot state `transport_exhausted`, keep `recommendation='unavailable'`, increment `attempt_count`, and preserve the last correlation ID. Extend `core-backend/internal/config/config.go` with exact config fields `KESMIBaseURL`, `KESMITimeout`, `KESMIModelID`, `KESMIMaxAttempts`, and `KESMIRetryBackoff`. Wire the new service in `core-backend/internal/app/app.go` and inject it into the aggregation success path without introducing a new standalone adapter service, per D-16.</action>
  <acceptance_criteria>
    - `core-backend/internal/config/config.go` contains the identifiers `KESMIBaseURL`, `KESMITimeout`, `KESMIModelID`, `KESMIMaxAttempts`, and `KESMIRetryBackoff`.
    - `core-backend/internal/kesmi/classifier.go` contains the strings `pool_busy`, `pool_exhausted`, `business_error`, and `transport_exhausted`.
    - `core-backend/internal/decision/service.go` contains the examination status literals `decision_pending` and `completed`.
    - `core-backend/internal/aggregation/service.go` contains an invocation into the decision subsystem after aggregated profile persistence.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/decision ./internal/kesmi ./internal/aggregation -run 'TestCreateDecisionInput|TestDecisionPendingTransition|TestRetriesOnlyTransportFailures|TestDecisionSuccessMarksCompleted' -count=1</automated>
  </verify>
  <done>Core backend now owns decision snapshot creation, WiMi classification, bounded retries, and `completed` transition without leaking raw WiMi contracts into the domain/UI boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Make WiMi part of the reproducible runtime stack</name>
  <files>wimi-server/Dockerfile, wimi-server/scripts/entrypoint.sh, docker-compose.yml, .env.example, docs/02_implementation.md</files>
  <read_first>.planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md, docs/04_wimi_guide.md, docker-compose.yml, .env.example, wimi-server/doc/Разворачивание/На-Linux.md</read_first>
  <action>Create `wimi-server/Dockerfile` that installs `wimi-server/bin/WiMi-0.1.7.deb`, copies an executable `wimi-server/scripts/entrypoint.sh`, exports `LD_LIBRARY_PATH=/usr/local/bin/WiMi/libs`, exports `LD_PRELOAD=/usr/local/bin/WiMi/libs/libjemalloc.so.2`, and starts `WiMi -e`. Update `docker-compose.yml` to add a `wimi` service built from `./wimi-server`, publish host port `${WIMI_HOST_PORT:-8081}:8081`, add a healthcheck using `wget -qO- http://127.0.0.1:8081/Models >/dev/null`, and make `core-backend` depend on that healthcheck. Update `.env.example` with exact keys `WIMI_HOST_PORT=8081`, `KESMI_BASE_URL=http://wimi:8081`, `KESMI_MODEL_ID=stub-decision-model`, `KESMI_TIMEOUT_MS=1000`, `KESMI_MAX_ATTEMPTS=2`, and `KESMI_RETRY_BACKOFF_MS=500`. Append one line to `docs/02_implementation.md` noting that WiMi is now a mandatory compose service for Phase 4 smoke verification per D-14 and D-15.</action>
  <acceptance_criteria>
    - `wimi-server/Dockerfile` contains `WiMi-0.1.7.deb`, `LD_LIBRARY_PATH`, `LD_PRELOAD`, and `WiMi -e`.
    - `docker-compose.yml` contains a top-level service named `wimi:` and `KESMI_BASE_URL` under `core-backend.environment`.
    - `.env.example` contains `KESMI_BASE_URL=http://wimi:8081`, `KESMI_MODEL_ID=stub-decision-model`, and `KESMI_MAX_ATTEMPTS=2`.
    - `docs/02_implementation.md` contains the phrase `WiMi is now a mandatory compose service`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom && rg -n 'wimi:|KESMI_BASE_URL|depends_on:' docker-compose.yml && rg -n 'WiMi-0.1.7.deb|LD_LIBRARY_PATH|LD_PRELOAD|WiMi -e' wimi-server/Dockerfile wimi-server/scripts/entrypoint.sh && rg -n 'KESMI_BASE_URL=http://wimi:8081|KESMI_MODEL_ID=stub-decision-model|KESMI_MAX_ATTEMPTS=2' .env.example</automated>
  </verify>
  <done>The repository contains one reproducible WiMi container path and the compose/env wiring needed for Phase 4 liveness and smoke verification.</done>
</task>

</tasks>

<verification>
Plan 02 is complete when targeted Go tests pass for decision delivery and the repository contains a compose-bootable WiMi service with explicit `KESMI_*` runtime configuration.
</verification>

<success_criteria>
Requirement IDs `KSMI-01` and `KSMI-02` are implemented through a persisted, retry-aware integration boundary.
The stack can boot `core-backend` and WiMi together without inventing manual host-side setup outside tracked files.
</success_criteria>

<output>
After completion, create `.planning/phases/04-decision-delivery-to-operator/04-02-SUMMARY.md`
</output>
