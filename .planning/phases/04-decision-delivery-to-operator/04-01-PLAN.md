---
phase: 04-decision-delivery-to-operator
plan: 01
type: execute
wave: 1
depends_on:
  - 04-00
files_modified:
  - docs/01_contract.md
  - core-backend/internal/decision/contracts.go
  - core-backend/internal/decision/service_test.go
  - core-backend/internal/kesmi/client_test.go
  - core-backend/internal/http/results_handler_test.go
  - core-backend/migrations/000008_decision_delivery.up.sql
  - core-backend/migrations/000008_decision_delivery.down.sql
  - core-backend/db/queries/decision.sql
  - core-backend/db/sqlc/decision.sql.go
  - core-backend/db/sqlc/models.go
  - docs/02_implementation.md
autonomous: true
requirements:
  - KSMI-01
  - KSMI-02
must_haves:
  truths:
    - "Core backend and frontend share one documented decision contract built on top of the aggregated profile, not raw WiMi payloads."
    - "The workflow vocabulary includes `decision_pending` and `completed`, and the result endpoint contract explains how decision diagnostics surface before final model delivery."
    - "Durable persistence exists for one latest decision snapshot plus append-only delivery attempts with correlation ID and retry classification."
  artifacts:
    - path: "core-backend/internal/decision/contracts.go"
      provides: "Stable `decision_input` and `decision_result` contracts per D-01, D-02, D-03"
    - path: "core-backend/migrations/000008_decision_delivery.up.sql"
      provides: "Decision snapshot and attempt ledger schema"
    - path: "core-backend/db/sqlc/decision.sql.go"
      provides: "Generated sqlc query methods used by the future decision repository"
  key_links:
    - from: "core-backend/internal/aggregation/contracts.go"
      to: "core-backend/internal/decision/contracts.go"
      via: "decision_input fields sourced from aggregated profile and baseline snapshot"
      pattern: "DecisionInput"
    - from: "docs/01_contract.md"
      to: "frontend/lib/api/types.ts"
      via: "documented `decision` block added to GET /examinations/{id}/result"
      pattern: "decision"
---

<objective>
Publish the Phase 4 contracts, RED verification scaffolds, and PostgreSQL schema for decision delivery before any runtime implementation starts.

Purpose: Lock the workflow, DTO, and persistence shape required by D-01..D-10 so later implementation cannot drift into raw WiMi coupling or ad-hoc retry logic.
Output: Updated system contract, decision DTO package, failing backend tests, and migration/query skeletons for decision snapshots plus attempts.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md
@.planning/phases/04-decision-delivery-to-operator/04-RESEARCH.md
@docs/00_project.md
@docs/01_contract.md
@docs/04_wimi_guide.md
@core-backend/internal/aggregation/contracts.go
@core-backend/internal/results/service.go
@core-backend/internal/http/results_handler.go
@frontend/lib/api/types.ts

<interfaces>
From core-backend/internal/aggregation/contracts.go:
```go
type AggregatedProfile struct {
	SchemaVersion        int
	AggregationVersion   string
	ExaminationID        int64
	SpecialistID         int64
	Status               string
	GeneratedAt          time.Time
	Summary              ProfileSummary
	Metrics              []Metric
	ChannelContributions []ChannelContribution
	Explanations         []Explanation
	BaselineSnapshot     BaselineSnapshot
}
```

From core-backend/internal/results/service.go:
```go
type Repository interface {
	GetExaminationResult(context.Context, int64) (aggregation.AggregatedProfile, error)
	GetSpecialistHistory(context.Context, int64) (SpecialistHistoryResponse, error)
}
```

From frontend/lib/api/types.ts:
```ts
export interface ExaminationResult {
  schema_version: number;
  aggregation_version: string;
  examination_id: number;
  specialist_id: number;
  status: Extract<ExaminationStatus, "aggregated" | "aggregating">;
  generated_at: string;
  summary: ResultSummary;
  metrics: ResultMetric[];
  channel_contributions: ResultChannelContribution[];
  explanations: ResultExplanation[];
  baseline_snapshot: { ... };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock Phase 4 decision contracts and RED tests</name>
  <files>docs/01_contract.md, core-backend/internal/decision/contracts.go, core-backend/internal/decision/service_test.go, core-backend/internal/kesmi/client_test.go, core-backend/internal/http/results_handler_test.go</files>
  <read_first>docs/00_project.md, docs/01_contract.md, .planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md, core-backend/internal/aggregation/contracts.go, core-backend/internal/results/service.go, core-backend/internal/http/results_handler.go, frontend/lib/api/types.ts</read_first>
  <behavior>
    - Test 1: `TestCreateDecisionInput` proves `decision_input` is built from `AggregatedProfile` fields, baseline deltas, `schema_version`, `aggregation_version`, and fixed service metadata per D-01 and D-02.
    - Test 2: `TestDecisionPendingTransition` proves Phase 4 transitions `aggregated -> decision_pending` before external delivery and `decision_pending -> completed` only after normalized success per D-04.
    - Test 3: `TestRetriesOnlyTransportFailures` proves only `timeout`, network errors, HTTP `5xx`, and WiMi `pool_busy` / `pool_exhausted` classify as retryable per D-07, while parameter and model errors stay non-retryable per D-08.
    - Test 4: `TestExaminationResultIncludesDecisionBlock` proves `GET /examinations/{id}/result` returns a backend-owned `decision` object with normalized state, correlation_id, diagnostics, and the fixed not-implemented placeholder instead of fake recommendation per D-11, D-12, and D-13.
  </behavior>
  <action>Update `docs/01_contract.md` to move the examination status vocabulary from Phase 3 to Phase 4 by adding `decision_pending` and `completed`, define the `decision_input` contract with fields `schema_version`, `payload_version`, `aggregation_version`, `examination_id`, `specialist_id`, `generated_at`, `summary`, `metrics`, `channel_contributions`, `baseline_snapshot`, and `service_metadata`, and define the `decision_result` block for `GET /examinations/{id}/result` with exact fields `state`, `recommendation`, `message`, `correlation_id`, `attempt_count`, `max_attempts`, `last_attempt_at`, `diagnostics`, and `raw_response_available`. Create `core-backend/internal/decision/contracts.go` with stable Go structs `DecisionInput`, `DecisionMetric`, `DecisionContribution`, `DecisionBaselineSnapshot`, `DecisionResult`, and string constants `DecisionStatePending`, `DecisionStateSucceeded`, `DecisionStateTransportExhausted`, `DecisionStateBusinessError`, `DecisionRecommendationUnavailable`, `DecisionRecommendationAllowed`, `DecisionRecommendationRisk`, `DecisionRecommendationDenied`. Add RED tests in `core-backend/internal/decision/service_test.go`, `core-backend/internal/kesmi/client_test.go`, and `core-backend/internal/http/results_handler_test.go` using those exact state and recommendation constants. Per D-03 and D-06, keep raw WiMi payloads out of the operator contract and document them as diagnostics-only.</action>
  <acceptance_criteria>
    - `docs/01_contract.md` contains the strings `decision_input`, `decision_result`, `decision_pending`, `completed`, `correlation_id`, and `transport_exhausted`.
    - `core-backend/internal/decision/contracts.go` exports `type DecisionInput struct`, `type DecisionResult struct`, `const DecisionStatePending = "pending"`, and `const DecisionRecommendationUnavailable = "unavailable"`.
    - `core-backend/internal/kesmi/client_test.go` contains `TestRetriesOnlyTransportFailures`.
    - `core-backend/internal/http/results_handler_test.go` contains `TestExaminationResultIncludesDecisionBlock`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && ! go test ./internal/decision ./internal/kesmi ./internal/http -run 'TestCreateDecisionInput|TestDecisionPendingTransition|TestRetriesOnlyTransportFailures|TestExaminationResultIncludesDecisionBlock' -count=1</automated>
  </verify>
  <done>Phase 4 contracts are explicitly documented, shared Go DTOs exist, and RED tests fail for missing implementation instead of missing contract artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Add authoritative decision-delivery schema and query skeletons</name>
  <files>core-backend/migrations/000008_decision_delivery.up.sql, core-backend/migrations/000008_decision_delivery.down.sql, core-backend/db/queries/decision.sql, core-backend/db/sqlc/decision.sql.go, core-backend/db/sqlc/models.go, docs/02_implementation.md</files>
  <read_first>docs/01_contract.md, .planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md, core-backend/migrations/000007_aggregated_profiles.up.sql, core-backend/db/queries/aggregation.sql, core-backend/db/queries/processing.sql, core-backend/db/sqlc.yaml, core-backend/db/sqlc/answers.sql.go</read_first>
  <action>Create migration `000008_decision_delivery` that extends examination status checks to include `decision_pending` and `completed`, creates `decision_snapshots` with columns `id`, `examination_id UNIQUE`, `specialist_id`, `status`, `payload_version`, `aggregation_version`, `recommendation`, `message`, `correlation_id`, `max_attempts`, `attempt_count`, `last_attempt_at`, `completed_at`, `diagnostics_json`, `raw_response_json`, `created_at`, `updated_at`, and creates append-only `decision_attempts` with columns `id`, `decision_snapshot_id`, `attempt_number`, `correlation_id`, `request_payload_json`, `response_payload_json`, `error_code`, `error_message`, `error_class`, `retryable`, `http_status`, `started_at`, `finished_at`, `created_at`. In `core-backend/db/queries/decision.sql`, add named query skeletons for `CreateDecisionSnapshot`, `GetDecisionSnapshotByExamination`, `InsertDecisionAttempt`, `ListPendingDecisionSnapshots`, `MarkDecisionAttemptSucceeded`, `MarkDecisionAttemptFailed`, `IncrementDecisionAttemptCount`, and `GetExaminationDecisionProjection`. Run `sqlc generate` from `core-backend/db` so the generated outputs `core-backend/db/sqlc/decision.sql.go` and any required `models.go` adjustments are committed in the same plan; the downstream repository implementation must consume these generated query methods instead of inventing manual SQL. Append one concise entry to `docs/02_implementation.md` stating that Phase 4 planning locked the decision snapshot + attempt ledger model.</action>
  <acceptance_criteria>
    - `core-backend/migrations/000008_decision_delivery.up.sql` contains `CREATE TABLE decision_snapshots`, `CREATE TABLE decision_attempts`, `status TEXT NOT NULL`, `correlation_id TEXT`, and `retryable BOOLEAN NOT NULL`.
    - `core-backend/migrations/000008_decision_delivery.up.sql` contains both examination status literals `decision_pending` and `completed`.
    - `core-backend/db/queries/decision.sql` contains query names `CreateDecisionSnapshot`, `InsertDecisionAttempt`, `ListPendingDecisionSnapshots`, and `GetExaminationDecisionProjection`.
    - `core-backend/db/sqlc/decision.sql.go` contains generated methods `CreateDecisionSnapshot`, `InsertDecisionAttempt`, and `ListPendingDecisionSnapshots`.
    - `docs/02_implementation.md` contains the phrase `decision snapshot + attempt ledger`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend/db && sqlc generate && cd /home/vadim/diplom && rg -n 'CREATE TABLE decision_snapshots|CREATE TABLE decision_attempts|decision_pending|completed' core-backend/migrations/000008_decision_delivery.up.sql && rg -n 'CreateDecisionSnapshot|InsertDecisionAttempt|ListPendingDecisionSnapshots|GetExaminationDecisionProjection' core-backend/db/queries/decision.sql && rg -n 'CreateDecisionSnapshot|InsertDecisionAttempt|ListPendingDecisionSnapshots' core-backend/db/sqlc/decision.sql.go</automated>
  </verify>
  <done>The database and sqlc layer now have an explicit Phase 4 skeleton for decision snapshots, append-only attempts, and result projection queries.</done>
</task>

</tasks>

<verification>
Contracts and persistence skeleton are complete when the RED tests exist with the exact Phase 4 names, the migration/query files exist, and the documented contract no longer treats `aggregated` as the final Phase 4 success surface.
</verification>

<success_criteria>
Requirement IDs `KSMI-01` and `KSMI-02` are mapped into a documented contract and schema.
The executor can implement decision delivery without inventing statuses, DTO fields, retry classes, or table names.
</success_criteria>

<output>
After completion, create `.planning/phases/04-decision-delivery-to-operator/04-01-SUMMARY.md`
</output>
