---
phase: 08-ui-contours-design-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - frontend/app/page.tsx
  - frontend/app/(auth)/login/page.tsx
  - frontend/app/(app)/operator/layout.tsx
  - frontend/app/(app)/admin/layout.tsx
  - frontend/app/(app)/admin/page.tsx
  - frontend/app/(app)/operator/loading.tsx
  - frontend/app/(app)/admin/loading.tsx
  - frontend/components/layout/route-guard.tsx
autonomous: false
requirements:
  - CNTR-01
  - CNTR-02
  - DSGN-01
must_haves:
  truths:
    - "Fresh auth and in-app role checks always send admins to `/admin` and operators to `/operator`."
    - "Operator pages render inside the operator shell and admin pages render inside the admin shell with no cross-contour controls."
    - "Admin now has an explicit overview home instead of inheriting `/admin/users` as the contour default."
    - "Contour-level loading/auth transitions use skeleton-compatible Phase 8 primitives rather than full-page spinner boxes."
  artifacts:
    - path: "frontend/app/(app)/admin/page.tsx"
      provides: "Explicit admin overview entry page"
    - path: "frontend/app/page.tsx"
      provides: "Root role redirect wired to canonical role-home mapping"
    - path: "frontend/app/(auth)/login/page.tsx"
      provides: "Login success redirect wired to canonical role-home mapping"
    - path: "frontend/components/layout/route-guard.tsx"
      provides: "In-app role mismatch and auth-state handling aligned to contour home mapping and shared loading primitives"
    - path: "frontend/app/(app)/operator/loading.tsx"
      provides: "Operator contour skeleton loading surface"
    - path: "frontend/app/(app)/admin/loading.tsx"
      provides: "Admin contour skeleton loading surface"
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/lib/navigation/role-home.ts"
      via: "server-side root redirect"
      pattern: "roleHome"
    - from: "frontend/app/(auth)/login/page.tsx"
      to: "frontend/lib/navigation/role-home.ts"
      via: "client-side login/session redirect"
      pattern: "roleHome"
    - from: "frontend/components/layout/route-guard.tsx"
      to: "frontend/lib/navigation/role-home.ts"
      via: "role mismatch fallback"
      pattern: "roleHome"
    - from: "frontend/app/(app)/admin/layout.tsx"
      to: "frontend/components/layout/admin-shell.tsx"
      via: "admin contour wrapper"
      pattern: "AdminShell"
---

<objective>
Wire the new foundation into the real app so users enter the correct contour, admin gets a true `/admin` home, and contour-level loading/auth states match the Phase 8 design system.

Purpose: close `CNTR-01`, `CNTR-02`, and the last visible Phase 8 gaps around redirects, contour entry points, and admin home ownership.
Output: updated redirects and layouts, a minimal admin overview page, contour loading surfaces, and human verification steps for both roles.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ui-contours-design-foundation/08-CONTEXT.md
@.planning/phases/08-ui-contours-design-foundation/08-UI-SPEC.md
@.planning/phases/08-ui-contours-design-foundation/08-RESEARCH.md
@docs/00_project.md
@docs/01_contract.md
@frontend/app/page.tsx
@frontend/app/(auth)/login/page.tsx
@frontend/app/(app)/operator/layout.tsx
@frontend/app/(app)/admin/layout.tsx
@frontend/app/(app)/admin/page.tsx
@frontend/components/layout/route-guard.tsx
@frontend/components/layout/operator-shell.tsx
@frontend/components/layout/admin-shell.tsx
@frontend/lib/navigation/role-home.ts

<interfaces>
From `frontend/lib/navigation/role-home.ts` after Plan 08-02:
```ts
export function roleHome(role: RoleSlug | null | undefined): "/admin" | "/operator";
```

From `frontend/components/layout/operator-shell.tsx` after Plan 08-02:
```tsx
export function OperatorShell({ children }: { children: ReactNode })
```

From `frontend/components/layout/admin-shell.tsx` after Plan 08-02:
```tsx
export function AdminShell({ children }: { children: ReactNode })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire all role entry points and contour layouts to the new shell and home mapping</name>
  <files>frontend/app/page.tsx, frontend/app/(auth)/login/page.tsx, frontend/app/(app)/operator/layout.tsx, frontend/app/(app)/admin/layout.tsx, frontend/components/layout/route-guard.tsx</files>
  <action>Replace hardcoded `/admin/users` redirect ownership with the shared role-home helper everywhere it currently exists: root landing, login success redirect, existing-session redirect, and route-guard mismatch handling per `D-14`, `D-15`, and `D-16`. Swap the current generic `AppShell` usage in operator/admin layouts to `OperatorShell` and `AdminShell`. Update the auth/guard loading state so contour entry uses skeleton-compatible Phase 8 feedback primitives instead of the old full-page spinner box. Do not add backend changes, do not touch admin CRUD pages, and do not let operator layouts expose admin navigation or vice versa.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm test -- role-home && npm run lint && npm run build</automated>
  </verify>
  <done>All auth and layout entry paths route through one role-home mapping and both contours render through their dedicated shell wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Create the explicit admin overview entry and contour loading surfaces</name>
  <files>frontend/app/(app)/admin/page.tsx, frontend/app/(app)/operator/loading.tsx, frontend/app/(app)/admin/loading.tsx</files>
  <action>Replace the current admin index redirect with a minimal but real admin overview page per `D-03` and `D-16`. The page should emphasize overview/status and management entry points without implementing Phase 9 or 10 CRUD/monitoring details. It can contain concise summary cards, section entry links, and honest placeholder messaging where later phases will deepen content. Add contour-level `loading.tsx` files that use the shared skeleton/page-header primitives and reflect each contour's reading order: operator loading should foreground the workstation/task area, admin loading should foreground the overview/status area. Keep the operator home page task-first and compact; do not redesign result/history pages here.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint && npm run build</automated>
  </verify>
  <done>Admins now land on a real `/admin` overview page, and both contours expose shared skeleton-based loading surfaces consistent with Phase 8 foundation rules.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify contour separation and `/admin` ownership in the running app</name>
  <action>Run one manual verification pass after Tasks 1 and 2 complete to confirm the real app behavior matches the planned contour split, redirect ownership, and loading-state expectations before Phase 8 is considered complete.</action>
  <what-built>Shared design foundation, contour-aware shells, explicit admin overview entry, and role-home redirects aligned to `/admin` and `/operator`.</what-built>
  <how-to-verify>
    1. Start the frontend and sign in as an `admin`.
    2. Confirm fresh login lands on `/admin`, not `/admin/users`.
    3. Confirm the admin shell looks overview-oriented, includes admin-only navigation, and does not expose operator examination actions.
    4. Open `/` while still authenticated as `admin` and confirm it redirects back to `/admin`.
    5. Sign out, sign in as an `operator`, and confirm fresh login lands on `/operator`.
    6. Confirm the operator shell is task-driven, compact, and does not expose admin management links.
    7. Refresh both contours and confirm loading/auth transitions use skeleton-style placeholders or shared state surfaces rather than the old centered spinner box.
  </how-to-verify>
  <resume-signal>Type "approved" if both contours and redirects match the expected behavior, or describe the mismatch precisely.</resume-signal>
  <done>Human verification explicitly approves that `/admin` is the admin home, `/operator` is the operator home, and both shells are clearly separated in the running app.</done>
</task>

</tasks>

<verification>
Phase 8 is only complete when the real app, not just the source tree, demonstrates contour separation with explicit admin home ownership and shared foundation-driven loading/auth states.
</verification>

<success_criteria>
Admins consistently enter `/admin`, operators consistently enter `/operator`, both contours are visually and structurally distinct, and the shared foundation is visible in the running shells and loading states.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-contours-design-foundation/08-03-SUMMARY.md`
</output>
