---
phase: 08-ui-contours-design-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/lib/navigation/role-home.ts
  - frontend/lib/navigation/role-home.test.ts
  - frontend/components/layout/app-shell.tsx
  - frontend/components/layout/operator-shell.tsx
  - frontend/components/layout/admin-shell.tsx
autonomous: true
requirements:
  - CNTR-01
  - CNTR-02
  - DSGN-01
must_haves:
  truths:
    - "Operator and admin contours are structurally distinct even before page content differs."
    - "Role-to-home routing is defined in one reusable place, with admin home mapped to `/admin` per `D-16`."
    - "The shared shell primitive preserves one codebase while preventing the 'same shell with different menu arrays' anti-pattern."
  artifacts:
    - path: "frontend/lib/navigation/role-home.ts"
      provides: "Canonical role-to-home mapping for operator and admin redirects"
    - path: "frontend/components/layout/operator-shell.tsx"
      provides: "Task-driven operator contour wrapper"
    - path: "frontend/components/layout/admin-shell.tsx"
      provides: "Overview-oriented admin contour wrapper"
    - path: "frontend/components/layout/app-shell.tsx"
      provides: "Shared structural shell primitive consumed by both wrappers"
  key_links:
    - from: "frontend/lib/navigation/role-home.ts"
      to: "frontend/lib/navigation/role-home.test.ts"
      via: "TDD coverage for redirect ownership"
      pattern: "/admin|/operator"
    - from: "frontend/components/layout/operator-shell.tsx"
      to: "frontend/components/layout/app-shell.tsx"
      via: "operator wrapper composes shared shell frame"
      pattern: "AppShell"
    - from: "frontend/components/layout/admin-shell.tsx"
      to: "frontend/components/layout/app-shell.tsx"
      via: "admin wrapper composes shared shell frame"
      pattern: "AppShell"
---

<objective>
Create the contour-aware shell contracts and redirect ownership needed to separate operator and admin UI safely without duplicating the whole frontend.

Purpose: satisfy `CNTR-01`, `CNTR-02`, and the contour identity decisions `D-01` through `D-03`, `D-11` through `D-16`.
Output: a tested role-home helper plus a shared shell frame wrapped by distinct `OperatorShell` and `AdminShell` components.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ui-contours-design-foundation/08-CONTEXT.md
@.planning/phases/08-ui-contours-design-foundation/08-UI-SPEC.md
@.planning/phases/08-ui-contours-design-foundation/08-RESEARCH.md
@docs/00_project.md
@docs/01_contract.md
@frontend/components/layout/app-shell.tsx
@frontend/components/layout/route-guard.tsx
@frontend/app/(app)/operator/layout.tsx
@frontend/app/(app)/admin/layout.tsx
@frontend/app/page.tsx
@frontend/app/(auth)/login/page.tsx

<interfaces>
From `frontend/components/layout/app-shell.tsx`:
```tsx
interface NavItem {
  href: string;
  label: string;
}

export function AppShell({
  title,
  subtitle,
  navItems,
  children,
}: {
  title: string;
  subtitle: string;
  navItems: NavItem[];
  children: ReactNode;
})
```

From `frontend/lib/api/types.ts`:
```ts
export type RoleSlug = "admin" | "operator";
```

From `frontend/hooks/use-current-user.ts`:
```ts
export function useCurrentUser() {
  return useQuery({
    queryKey: ["session"],
    queryFn: apiClient.me,
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize role-home routing with a tested helper</name>
  <files>frontend/lib/navigation/role-home.ts, frontend/lib/navigation/role-home.test.ts</files>
  <behavior>
    - Test 1: role `admin` resolves to `/admin` per `D-16`.
    - Test 2: role `operator` resolves to `/operator` per `D-14` and `D-15`.
    - Test 3: unknown or nullish role input falls back to `/operator` only when the existing auth boundary cannot determine a valid role.
  </behavior>
  <action>Create a small role-home helper and cover it with Vitest before wiring it into pages. Keep it framework-light so both server redirects and client router replacements can reuse the same mapping. This helper becomes the only Phase 8-owned source of truth for redirect ownership; do not leave `/admin/users` literals in redirect code paths after downstream wiring.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm test -- role-home</automated>
  </verify>
  <done>The repo has one tested helper that maps `admin` to `/admin` and `operator` to `/operator`.</done>
</task>

<task type="auto">
  <name>Task 2: Split the current shell into one shared frame plus contour-specific wrappers</name>
  <files>frontend/components/layout/app-shell.tsx, frontend/components/layout/operator-shell.tsx, frontend/components/layout/admin-shell.tsx</files>
  <action>Refactor the existing `AppShell` into a shared structural primitive and introduce `OperatorShell` and `AdminShell` wrappers per `D-01`, `D-02`, `D-03`, `D-11`, `D-12`, and `D-13`. Preserve the shared left-sidebar architecture, session panel, and logout affordance, but encode contour identity through wrapper-owned nav grouping, copy, density, surface treatment, and first-read hierarchy so blurred screenshots of both contours remain distinguishable. Do not duplicate logout/session logic across wrappers and do not leave contour identity expressed only by different arrays passed from layouts.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm test -- role-home && npm run lint && npm run build</automated>
  </verify>
  <done>The codebase exposes distinct operator/admin shell wrappers on top of one shared frame, and the redirect helper remains green under test.</done>
</task>

</tasks>

<verification>
Plan 08-02 is complete only when shells and redirect ownership are explicit reusable contracts that later layouts can consume without inventing their own routing or shell variants.
</verification>

<success_criteria>
Phase 8 now has durable shell and routing contracts: a tested role-home helper and clearly separate operator/admin shell wrappers built on one shared foundation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-contours-design-foundation/08-02-SUMMARY.md`
</output>
