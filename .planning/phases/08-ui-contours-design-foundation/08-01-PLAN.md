---
phase: 08-ui-contours-design-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/app/globals.css
  - frontend/tailwind.config.ts
  - frontend/components/providers/app-providers.tsx
  - frontend/components/ui/button.tsx
  - frontend/components/ui/card.tsx
  - frontend/components/ui/page-header.tsx
  - frontend/components/ui/alert.tsx
  - frontend/components/ui/badge.tsx
  - frontend/components/ui/input.tsx
  - frontend/components/ui/textarea.tsx
  - frontend/components/ui/empty-state.tsx
  - frontend/components/ui/skeleton.tsx
  - frontend/components/ui/confirm-dialog.tsx
  - frontend/components/ui/toaster.tsx
autonomous: true
requirements:
  - DSGN-01
must_haves:
  truths:
    - "Both contours can render from one shared token set for typography, spacing, radius, surfaces, and semantic states."
    - "Primary loading, empty, alert, confirmation, and toast feedback use shared primitives instead of page-local ad hoc markup."
    - "Foundation styling follows the approved corporate visual direction and does not require a Tailwind or Next.js migration."
  artifacts:
    - path: "frontend/app/globals.css"
      provides: "Phase 8 token source for color, spacing, radius, motion, and surface variables"
    - path: "frontend/tailwind.config.ts"
      provides: "Tailwind aliases for the approved token system"
    - path: "frontend/components/ui/skeleton.tsx"
      provides: "Shared skeleton primitive for contour/page loading states"
    - path: "frontend/components/ui/confirm-dialog.tsx"
      provides: "Shared destructive confirmation primitive"
    - path: "frontend/components/ui/toaster.tsx"
      provides: "Shared toast feedback primitive mounted once at app level"
  key_links:
    - from: "frontend/app/globals.css"
      to: "frontend/tailwind.config.ts"
      via: "CSS variables mapped into Tailwind utilities"
      pattern: "--accent|--success|--warning|--danger"
    - from: "frontend/components/providers/app-providers.tsx"
      to: "frontend/components/ui/toaster.tsx"
      via: "single app-level toaster mount"
      pattern: "Toaster"
    - from: "frontend/components/ui/page-header.tsx"
      to: "frontend/components/ui/card.tsx"
      via: "shared typography and surface rhythm"
      pattern: "text-|rounded-|p-"
---

<objective>
Lock the shared design foundation for Phase 8 so later contour work reuses one token system and one set of feedback/state primitives instead of drifting page-level styles.

Purpose: satisfy `DSGN-01` and Decisions `D-04` through `D-10` before shell wiring begins.
Output: normalized tokens in `globals.css` and `tailwind.config.ts`, plus reusable UI primitives for headers, forms, alerts, badges, skeletons, confirmation, and toast feedback.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ui-contours-design-foundation/08-CONTEXT.md
@.planning/phases/08-ui-contours-design-foundation/08-UI-SPEC.md
@.planning/phases/08-ui-contours-design-foundation/08-RESEARCH.md
@docs/00_project.md
@docs/01_contract.md
@frontend/app/globals.css
@frontend/tailwind.config.ts
@frontend/components/providers/app-providers.tsx
@frontend/components/ui/button.tsx
@frontend/components/ui/card.tsx
@frontend/components/ui/page-header.tsx
@frontend/components/ui/alert.tsx
@frontend/components/ui/badge.tsx
@frontend/components/ui/input.tsx
@frontend/components/ui/textarea.tsx
@frontend/components/ui/empty-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize tokens and base primitives to the approved Phase 8 foundation</name>
  <files>frontend/app/globals.css, frontend/tailwind.config.ts, frontend/components/ui/button.tsx, frontend/components/ui/card.tsx, frontend/components/ui/page-header.tsx, frontend/components/ui/alert.tsx, frontend/components/ui/badge.tsx, frontend/components/ui/input.tsx, frontend/components/ui/textarea.tsx, frontend/components/ui/empty-state.tsx</files>
  <action>Update the frontend token source and primitive styles to match the approved UI contract per `D-04`, `D-05`, `D-06`, `D-07`, and `D-08`. Keep the existing Next 15 and Tailwind 3 stack unchanged per research scope. In `globals.css`, lock the approved color, typography, spacing, radius, surface, and restrained motion variables; remove arbitrary shell-era values only where they conflict with the approved scale. In `tailwind.config.ts`, expose those variables through stable theme aliases rather than hardcoded per-component colors. Normalize the shared primitives so page headers, cards, badges, alerts, buttons, inputs, textareas, and empty states all use one typography and spacing system. Do not restyle domain pages here and do not introduce contour-specific colors into the primitive layer.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint && npm run build</automated>
  </verify>
  <done>The token layer and existing shared primitives visibly align with the approved Phase 8 design contract and remain buildable on the current stack.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared feedback and loading primitives for later contour and CRUD flows</name>
  <files>frontend/package.json, frontend/components/providers/app-providers.tsx, frontend/components/ui/skeleton.tsx, frontend/components/ui/confirm-dialog.tsx, frontend/components/ui/toaster.tsx</files>
  <action>Add the missing Phase 8 feedback primitives required by `D-09` and `D-10`: skeleton, confirmation dialog, and toast feedback. Prefer a thin shared wrapper around a standard toast implementation if a small dependency is needed; do not hand-roll a global event bus. Mount the toaster once in `AppProviders` so later admin/operator plans can reuse it without page-level setup. The confirmation primitive must be reusable for logout and later destructive admin/operator actions. Keep the API of these primitives simple and repo-owned.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint && npm run build</automated>
  </verify>
  <done>The frontend has reusable skeleton, confirmation, and toast primitives wired at the provider level and ready for contour-level integration.</done>
</task>

</tasks>

<verification>
Phase 8 foundation is complete only when the shared token layer and UI primitives can support both contours without page-local styling forks or spinner-only loading placeholders.
</verification>

<success_criteria>
`DSGN-01` is materially enabled: the repo contains one approved token system plus shared primitives for surfaces, forms, semantic states, loading, confirmation, and toast feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-contours-design-foundation/08-01-SUMMARY.md`
</output>
