---
phase: 12-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/(auth)/login/page.tsx
  - frontend/components/layout/app-shell.tsx
  - frontend/components/layout/operator-shell.tsx
  - frontend/components/layout/admin-shell.tsx
autonomous: true
requirements:
  - DSGN-02
  - OPRX-03
must_haves:
  truths:
    - "Login screen reads like a strict enterprise auth page instead of a promo landing."
    - "Operator and admin sidebars show compact navigation without explanatory blurbs."
    - "Shared contour chrome no longer contains decorative focus/summary blocks."
  artifacts:
    - path: "frontend/app/(auth)/login/page.tsx"
      provides: "Compact login entry with explicit error/loading states"
    - path: "frontend/components/layout/app-shell.tsx"
      provides: "Dense shared sidebar and session area"
    - path: "frontend/components/layout/operator-shell.tsx"
      provides: "Operator nav labels without descriptive copy"
    - path: "frontend/components/layout/admin-shell.tsx"
      provides: "Admin nav labels without descriptive copy"
  key_links:
    - from: "frontend/components/layout/operator-shell.tsx"
      to: "frontend/components/layout/app-shell.tsx"
      via: "navSections and shared shell props"
      pattern: "navSections|contourSummary"
    - from: "frontend/app/(auth)/login/page.tsx"
      to: "frontend/lib/navigation/role-home.ts"
      via: "post-login redirect"
      pattern: "getRoleHome"
---

<objective>
Tighten the shared entry and contour chrome so the first-read experience matches the locked Phase 12 direction.

Purpose: Remove promo-style, decorative, and developer-facing framing before screen-specific densification starts.
Output: A compact login screen plus strict operator/admin sidebars that can support denser work surfaces in later plans.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ui/12-CONTEXT.md
@.planning/phases/12-ui/12-RESEARCH.md
@.planning/phases/11-operator-ux-visual-completion/11-UI-REVIEW.md
@frontend/app/(auth)/login/page.tsx
@frontend/components/layout/app-shell.tsx
@frontend/components/layout/operator-shell.tsx
@frontend/components/layout/admin-shell.tsx

<interfaces>
From `frontend/components/layout/app-shell.tsx`:

```tsx
export interface NavItem {
  href: string;
  label: string;
  description?: string;
}

export interface NavSection {
  title?: string;
  items: NavItem[];
}
```

From `frontend/app/(auth)/login/page.tsx`:

```tsx
const loginMutation = useMutation({
  mutationFn: apiClient.login,
  onSuccess: (data) => {
    router.replace(getRoleHome(data.user.role.slug));
    router.refresh();
  },
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip decorative contour chrome and densify sidebar navigation</name>
  <files>frontend/components/layout/app-shell.tsx, frontend/components/layout/operator-shell.tsx, frontend/components/layout/admin-shell.tsx</files>
  <action>Remove long per-item descriptions, remove `Фокус смены` and `Контур администрирования` style summary blocks, and simplify the session area so the sidebar reads as a compact enterprise tool rail per Phase 12 scope. Keep role separation from Phase 8 intact, preserve logout/session behavior, and do not introduce a new shell abstraction. This task implements the locked navigation/text decisions from 12-CONTEXT.md.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint -- --file components/layout/app-shell.tsx --file components/layout/operator-shell.tsx --file components/layout/admin-shell.tsx</automated>
  </verify>
  <done>Both contours keep the same routes and guards, but the sidebar shows only concise section labels/items with no decorative explanatory content.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the login screen as a compact enterprise entry surface</name>
  <files>frontend/app/(auth)/login/page.tsx</files>
  <action>Replace the oversized promo panel and developer-facing copy with a short role-aware heading, 1-2 lines of operational context, and a stricter form-first layout per the login checklist in 12-CONTEXT.md. Keep the existing `useCurrentUser`, validation, loading skeleton, error alert, disabled submit, and redirect flow. Remove references to backend contracts, JWT, placeholders, or other internal implementation wording.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run build</automated>
  </verify>
  <done>Login is visually secondary to the form, contains only user-facing copy, and retains honest loading/error/submit states.</done>
</task>

</tasks>

<verification>
Confirm that `/login` still redirects authenticated users correctly and that operator/admin shells still render the same route structure with the denser chrome.
</verification>

<success_criteria>
The shared entry layer no longer reads like a demo or architecture explainer, and later Phase 12 screens can sit on a compact, non-decorative shell.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui/12-ui-01-SUMMARY.md`
</output>
