---
phase: 12-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/01_contract.md
  - core-backend/migrations/000011_ui_registry_support.up.sql
  - core-backend/migrations/000011_ui_registry_support.down.sql
  - core-backend/db/queries/auth.sql
  - core-backend/db/queries/questionnaires.sql
  - core-backend/db/queries/specialists.sql
  - core-backend/db/sqlc/models.go
  - core-backend/db/sqlc/auth.sql.go
  - core-backend/db/sqlc/questionnaires.sql.go
  - core-backend/db/sqlc/specialists.sql.go
  - core-backend/internal/auth/repository.go
  - core-backend/internal/auth/service.go
  - core-backend/internal/http/auth_handler.go
  - core-backend/internal/http/auth_handler_test.go
  - core-backend/internal/http/questionnaires_handler.go
  - core-backend/internal/http/specialists_handler.go
  - frontend/lib/api/types.ts
  - frontend/lib/api/client.ts
autonomous: true
requirements:
  - ADMN-01
  - QSTR-02
  - MONR-01
  - RICH-01
  - AMON-01
must_haves:
  truths:
    - "Dense operator/admin registries can show real last activity and summary data without inventing fields in the UI."
    - "Users expose a real last-login timestamp."
    - "Questionnaires expose real usage/editor metadata."
    - "Specialists expose real last examination and baseline-summary metadata for registry rendering."
  artifacts:
    - path: "docs/01_contract.md"
      provides: "Documented enriched DTO shapes for users, questionnaires, and specialists"
    - path: "core-backend/migrations/000011_ui_registry_support.up.sql"
      provides: "Schema support for UI-facing registry metadata"
    - path: "frontend/lib/api/types.ts"
      provides: "Typed frontend contract additions for the enriched DTOs"
  key_links:
    - from: "core-backend/internal/auth/service.go"
      to: "core-backend/db/queries/auth.sql"
      via: "persisting and reading last login metadata"
      pattern: "last_login"
    - from: "core-backend/internal/http/questionnaires_handler.go"
      to: "frontend/lib/api/types.ts"
      via: "questionnaire registry DTO"
      pattern: "usage|last_editor"
    - from: "core-backend/internal/http/specialists_handler.go"
      to: "frontend/lib/api/types.ts"
      via: "specialist registry summary DTO"
      pattern: "last_examination|baseline"
---

<objective>
Publish the bounded contract and backend support needed to make Phase 12 registries genuinely informative.

Purpose: Satisfy the locked screen checklist without frontend-only fake fields or decorative placeholders.
Output: Documented and implemented DTO enrichments for users, questionnaires, and specialists, wired into frontend contract types.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ui/12-CONTEXT.md
@.planning/phases/12-ui/12-RESEARCH.md
@docs/01_contract.md
@frontend/lib/api/types.ts
@frontend/lib/api/client.ts
@core-backend/internal/http/auth_handler.go
@core-backend/internal/http/questionnaires_handler.go
@core-backend/internal/http/specialists_handler.go

<interfaces>
From `frontend/lib/api/types.ts`:

```ts
export interface User {
  id: number;
  login: string;
  role: Role;
  is_active: boolean;
  created_at: string;
  updated_at: string;
}

export interface Questionnaire {
  id: number;
  title: string;
  description: string | null;
  is_active: boolean;
  questions: Question[];
  created_at: string;
  updated_at: string;
}

export interface Specialist {
  id: number;
  full_name: string;
  personnel_number: string | null;
  created_at: string;
  updated_at: string;
}
```

Research constraints from `12-RESEARCH.md`:
- Add backend support only for fields the UI cannot honestly derive.
- Do not invent last login, questionnaire usage, or specialist summary data in frontend code.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish the enriched UI-support contracts per Phase 12 scope</name>
  <files>docs/01_contract.md, core-backend/migrations/000011_ui_registry_support.up.sql, core-backend/migrations/000011_ui_registry_support.down.sql</files>
  <behavior>
    - Test 1: `GET /users` and `GET /users/{id}` include a real nullable `last_login_at`.
    - Test 2: `GET /questionnaires` and `GET /questionnaires/{id}` include real usage/editor metadata, not derived frontend guesses.
    - Test 3: `GET /specialists` exposes registry summary fields needed by the operator list and dashboard.
  </behavior>
  <action>Document the exact new DTO fields in `docs/01_contract.md`, then add the minimal schema support needed for those fields. Keep the additions bounded to registry/dashboard usefulness per 12-RESEARCH.md; do not broaden into new services or unrelated domain redesign.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/http ./internal/auth ./internal/questionnaires ./internal/specialists</automated>
  </verify>
  <done>The contract clearly describes each added field and the schema exists to persist any new metadata needed for runtime truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement backend DTO enrichments for users, questionnaires, and specialists</name>
  <files>core-backend/db/queries/auth.sql, core-backend/db/queries/questionnaires.sql, core-backend/db/queries/specialists.sql, core-backend/db/sqlc/models.go, core-backend/db/sqlc/auth.sql.go, core-backend/db/sqlc/questionnaires.sql.go, core-backend/db/sqlc/specialists.sql.go, core-backend/internal/auth/repository.go, core-backend/internal/auth/service.go, core-backend/internal/http/auth_handler.go, core-backend/internal/http/auth_handler_test.go, core-backend/internal/http/questionnaires_handler.go, core-backend/internal/http/specialists_handler.go</files>
  <behavior>
    - Test 1: successful login updates `last_login_at` and user-read endpoints return it.
    - Test 2: questionnaire reads return stable usage/editor metadata from persisted data.
    - Test 3: specialist list reads return last-exam/status/baseline summary fields without N+1 handler logic.
  </behavior>
  <action>Implement the enriched reads and the minimal write-path support behind them. Keep PostgreSQL as source of truth, use existing service/repository layering, and avoid front-loading a new summary endpoint unless an existing registry route cannot carry the required fields honestly.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/auth ./internal/questionnaires ./internal/specialists ./internal/http</automated>
  </verify>
  <done>Existing list/detail routes return the new metadata from real persisted/backend-derived values, and regression tests pin the new behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the enriched contracts into the frontend API boundary</name>
  <files>frontend/lib/api/types.ts, frontend/lib/api/client.ts</files>
  <action>Extend the frontend types and request helpers to the new documented DTO shape, preserving snake_case contract fidelity. Do not add view-level derivations here; this layer should only expose typed contract fields for later UI plans.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Frontend pages can consume the new registry metadata through typed `apiClient` calls without inline `any` or undocumented fields.</done>
</task>

</tasks>

<verification>
All contract additions must be reflected in `docs/01_contract.md`, backed by tests, and consumable from `frontend/lib/api/types.ts`.
</verification>

<success_criteria>
Phase 12 frontend plans can render denser operator/admin registries using real backend-supported data instead of decorative or guessed placeholders.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui/12-ui-02-SUMMARY.md`
</output>
