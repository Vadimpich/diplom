---
phase: 12-ui
plan: 06
type: execute
wave: 3
depends_on: ["12-01", "12-02", "12-04"]
files_modified:
  - frontend/components/admin/user-form.tsx
  - frontend/components/admin/questionnaire-builder.tsx
  - frontend/app/(app)/admin/users/[id]/page.tsx
  - frontend/app/(app)/admin/questionnaires/new/page.tsx
  - frontend/app/(app)/admin/questionnaires/[id]/page.tsx
  - frontend/app/(app)/admin/settings/page.tsx
autonomous: true
requirements:
  - ADMN-02
  - QSTR-02
  - STNG-02
  - OPRX-04
  - DSGN-02
must_haves:
  truths:
    - "Admin user detail shows access context without internal audit implementation notes."
    - "Questionnaire editing feels like a compact constructor rather than a noisy technical form."
    - "Settings use user-facing grouped copy with explicit save feedback."
  artifacts:
    - path: "frontend/app/(app)/admin/users/[id]/page.tsx"
      provides: "Compact user detail"
    - path: "frontend/components/admin/questionnaire-builder.tsx"
      provides: "Dense questionnaire constructor"
    - path: "frontend/app/(app)/admin/settings/page.tsx"
      provides: "Grouped settings form"
  key_links:
    - from: "frontend/app/(app)/admin/users/[id]/page.tsx"
      to: "frontend/components/admin/user-form.tsx"
      via: "shared edit form"
      pattern: "UserForm"
    - from: "frontend/app/(app)/admin/questionnaires/[id]/page.tsx"
      to: "frontend/components/admin/questionnaire-builder.tsx"
      via: "shared questionnaire builder"
      pattern: "QuestionnaireBuilder"
    - from: "frontend/app/(app)/admin/settings/page.tsx"
      to: "frontend/lib/api/client.ts"
      via: "GET/PUT settings flow"
      pattern: "getSystemSettings|updateSystemSettings"
---

<objective>
Tighten the admin detail and edit surfaces so they behave like concise back-office tools.

Purpose: Close the Phase 12 checklist for user detail, questionnaire editing, and settings without introducing new admin scope.
Output: Compact admin editors with better structure, clearer wording, and preserved confirmation/save feedback behavior.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ui/12-CONTEXT.md
@frontend/app/(app)/admin/users/[id]/page.tsx
@frontend/app/(app)/admin/questionnaires/new/page.tsx
@frontend/app/(app)/admin/questionnaires/[id]/page.tsx
@frontend/app/(app)/admin/settings/page.tsx
@frontend/components/admin/user-form.tsx
@frontend/components/admin/questionnaire-builder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe admin user detail around access, status, and recent activity</name>
  <files>frontend/components/admin/user-form.tsx, frontend/app/(app)/admin/users/[id]/page.tsx</files>
  <action>Remove the internal audit implementation note, tighten spacing, and restructure the page into access data, security/status context, and recent timestamps. Use the real `last_login_at` field from `12-02` if present; if it is absent at runtime, show a truthful fallback instead of inventing activity. Preserve the existing save feedback path.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint -- --file components/admin/user-form.tsx --file app/'(app)'/admin/users/'[id]'/page.tsx</automated>
  </verify>
  <done>User detail is compact, user-facing, and no longer exposes internal implementation commentary.</done>
</task>

<task type="auto">
  <name>Task 2: Compress questionnaire authoring into a cleaner constructor flow</name>
  <files>frontend/components/admin/questionnaire-builder.tsx, frontend/app/(app)/admin/questionnaires/new/page.tsx, frontend/app/(app)/admin/questionnaires/[id]/page.tsx</files>
  <action>Strip backend-process copy, reduce repeated containers, simplify question order controls, and add an explicit unsaved-changes warning path if the current form state differs from the loaded/saved state. Keep existing confirmation before remove, preserve publication toggle semantics, and do not introduce drag-and-drop or a contract change.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>The questionnaire editor centers on title, description, publication, question list, add-question, and save, with markedly less visual noise.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite settings into grouped, user-facing administrative controls</name>
  <files>frontend/app/(app)/admin/settings/page.tsx</files>
  <action>Replace developer-facing settings copy with simple user language, group fields by intent, and keep explicit validation/save feedback prominent. Show last update metadata in a normal administrative form and remove references to env variables, persisted subsets, or `source of truth` phrasing from the UI.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run build</automated>
  </verify>
  <done>Settings reads like a practical admin form with grouped controls, useful help text, and explicit save/error feedback.</done>
</task>

</tasks>

<verification>
Review user detail, questionnaire edit/new, and settings screens directly against their Phase 12 checklist items.
</verification>

<success_criteria>
Admin editors become compact and readable, with no technical implementation notes shown to users and no overloaded form chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui/12-ui-06-SUMMARY.md`
</output>
