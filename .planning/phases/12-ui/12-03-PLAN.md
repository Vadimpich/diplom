---
phase: 12-ui
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - frontend/components/operator/operator-kpi-strip.tsx
  - frontend/components/operator/specialists-registry.tsx
  - frontend/components/operator/examinations-journal.tsx
  - frontend/app/(app)/operator/page.tsx
  - frontend/app/(app)/operator/specialists/page.tsx
  - frontend/app/(app)/operator/history/page.tsx
autonomous: true
requirements:
  - OPRX-03
  - DSGN-02
  - RICH-01
must_haves:
  truths:
    - "Operator dashboard shows actionable workload and recent case information instead of onboarding cards."
    - "Specialists list reads as a dense registry with last activity and current status."
    - "History page behaves like an operational journal with useful filters and status grouping."
  artifacts:
    - path: "frontend/app/(app)/operator/page.tsx"
      provides: "Operational dashboard"
    - path: "frontend/app/(app)/operator/specialists/page.tsx"
      provides: "Dense specialist registry"
    - path: "frontend/app/(app)/operator/history/page.tsx"
      provides: "Filtered examination journal"
  key_links:
    - from: "frontend/app/(app)/operator/page.tsx"
      to: "frontend/lib/api/client.ts"
      via: "specialists and examinations queries"
      pattern: "getSpecialists|getExaminations"
    - from: "frontend/app/(app)/operator/specialists/page.tsx"
      to: "frontend/lib/api/types.ts"
      via: "enriched specialist registry fields"
      pattern: "last_examination|baseline"
    - from: "frontend/app/(app)/operator/history/page.tsx"
      to: "frontend/components/operator/examinations-journal.tsx"
      via: "shared journal rendering"
      pattern: "ExaminationsJournal"
---

<objective>
Turn the operator dashboard, specialists list, and history screen into compact operational surfaces.

Purpose: Close the Phase 12 checklist for operator overviews and registries using the shared chrome plus enriched registry DTOs.
Output: Dense dashboard and registry pages with clear task priority, useful metadata, and honest state handling.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ui/12-CONTEXT.md
@.planning/phases/12-ui/12-RESEARCH.md
@frontend/app/(app)/operator/page.tsx
@frontend/app/(app)/operator/specialists/page.tsx
@frontend/app/(app)/operator/history/page.tsx
@frontend/lib/api/types.ts
@frontend/lib/api/client.ts
@.planning/phases/11-operator-ux-visual-completion/11-UI-REVIEW.md

<interfaces>
Use the enriched registry DTOs from `12-02`, especially:

```ts
apiClient.getSpecialists(): Promise<SpecialistsResponse>
apiClient.getExaminations(): Promise<ExaminationsResponse>
```

And keep the existing navigation helper:

```ts
getOperatorExaminationHref(examinationId: number, specialistId: number, status: ExaminationStatus): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared dense registry widgets for operator overview screens</name>
  <files>frontend/components/operator/operator-kpi-strip.tsx, frontend/components/operator/specialists-registry.tsx, frontend/components/operator/examinations-journal.tsx</files>
  <action>Create reusable operator-specific presentation components for KPI rows, specialist registry rows, and examination journal rows. Reuse existing `Badge`, `Alert`, `EmptyState`, and skeleton primitives; do not introduce a table dependency. These widgets must reflect the Phase 12 density requirement and keep loading/error/empty behavior explicit.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint -- --file components/operator/operator-kpi-strip.tsx --file components/operator/specialists-registry.tsx --file components/operator/examinations-journal.tsx</automated>
  </verify>
  <done>Operator overview pages can share one dense registry language instead of duplicating low-density cards.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the operator dashboard around current work and quick entry points</name>
  <files>frontend/app/(app)/operator/page.tsx</files>
  <action>Replace onboarding cards with real current-work blocks per 12-CONTEXT.md: active cases, recent finished cases, pending attention, risk-oriented highlights where the contract allows, and a quick specialist jump list tied to the new registry fields. Preserve explicit query error handling called out by `11-UI-REVIEW.md`; dashboard failures must not collapse into zeroes or fake empties.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>The dashboard immediately answers what the operator should do now and no longer contains explanatory/onboarding cards.</done>
</task>

<task type="auto">
  <name>Task 3: Convert specialists and history into compact operational registries</name>
  <files>frontend/app/(app)/operator/specialists/page.tsx, frontend/app/(app)/operator/history/page.tsx</files>
  <action>Implement the locked list/history checklist: denser rows, meaningful columns, tighter spacing, better filter behavior, and clearer row-level entry actions. For history, add useful grouping/filtering on top of the current query set without inventing backend data. Keep empty/loading/error states already shipped in Phase 11 where they still apply; refine them instead of rewriting them from scratch.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run build</automated>
  </verify>
  <done>Both pages read as working registries/journals with who/what/when/status/next-action visible in each row group.</done>
</task>

</tasks>

<verification>
Review the three operator overview screens against the exact 12-CONTEXT.md checklist for dashboard, specialists list, and history.
</verification>

<success_criteria>
The operator can identify current workload, locate a specialist, and reopen the right examination state without reading decorative copy or drilling through thin cards.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui/12-ui-03-SUMMARY.md`
</output>
