---
phase: 05-operational-trustworthiness
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - core-backend/internal/observability/logger.go
  - core-backend/internal/observability/tracing.go
  - core-backend/internal/observability/middleware.go
  - core-backend/internal/config/config.go
  - core-backend/internal/app/app.go
  - core-backend/internal/http/router.go
  - core-backend/internal/processing/contracts.go
  - core-backend/internal/processing/publisher.go
  - core-backend/internal/processing/results_consumer.go
  - core-backend/internal/baselineclient/client.go
  - core-backend/internal/kesmi/client.go
  - ml-services/ml-text/app/telemetry.py
  - ml-services/ml-text/app/main.py
  - ml-services/ml-acoustic/app/telemetry.py
  - ml-services/ml-acoustic/app/main.py
  - ml-services/ml-paralinguistic/app/telemetry.py
  - ml-services/ml-paralinguistic/app/main.py
  - ml-services/ml-baseline/app/telemetry.py
  - ml-services/ml-baseline/app/main.py
  - docs/02_implementation.md
autonomous: true
requirements:
  - OBSV-03
  - QUAL-02
must_haves:
  truths:
    - One request or examination can be correlated across HTTP handlers, RabbitMQ publish/consume, baseline calls, and KESMI calls.
    - Go and Python services emit structured logs with the same core fields instead of unstructured `log.Printf` strings.
    - Existing `correlation_id` stays visible while distributed trace context continues across async and outbound boundaries.
  artifacts:
    - core-backend/internal/observability/* provides shared logger, trace propagation, and middleware.
    - core-backend/internal/processing/contracts.go carries the extra trace transport fields required for async continuity.
    - Python services each gain a shared telemetry helper and use it from `app/main.py`.
  key_links:
    - HTTP request middleware must inject request/trace context before services create audit or domain events.
    - Outbox publish and result consume must preserve both `correlation_id` and standardized trace context.
    - Baseline and KESMI clients must forward trace context outbound so the trace does not end at core-backend.
---

<objective>
Add shared structured logging and trace propagation across the shipped Go and Python runtime paths.

Purpose: make one examination or request reconstructable end to end without changing the business workflow.
Output: centralized observability plumbing in `core-backend`, async trace propagation through RabbitMQ, and shared telemetry helpers in Python services.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operational-trustworthiness/05-RESEARCH.md
@.planning/phases/05-operational-trustworthiness/05-02-SUMMARY.md
@docs/00_project.md
@docs/01_contract.md
@docs/02_implementation.md
@core-backend/internal/app/app.go
@core-backend/internal/http/router.go
@core-backend/internal/processing/contracts.go
@core-backend/internal/processing/publisher.go
@core-backend/internal/processing/results_consumer.go
@core-backend/internal/baselineclient/client.go
@core-backend/internal/kesmi/client.go
@ml-services/ml-text/app/main.py
@ml-services/ml-baseline/app/main.py

<interfaces>
From core-backend/internal/processing/contracts.go:
```go
type ProcessingCommandEnvelope struct {
	MessageVersion int
	MessageID      string
	CorrelationID  string
	ExaminationID  int64
	SpecialistID   int64
	Channel        string
	Attempt        int32
	Answers        []CommandAnswerReference
}
```

From research propagation rule:
```text
Keep `correlation_id` for business/debug continuity, but make `traceparent` the canonical distributed trace transport.
Do not replace existing contracts; augment them.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared core-backend observability foundation</name>
  <files>core-backend/internal/observability/logger.go, core-backend/internal/observability/tracing.go, core-backend/internal/observability/middleware.go, core-backend/internal/config/config.go, core-backend/internal/app/app.go, core-backend/internal/http/router.go, docs/02_implementation.md</files>
  <behavior>
    - Test 1: every incoming HTTP request gets stable request and trace identifiers in context and structured logs.
    - Test 2: request logging moves from plain text to one JSON logger style with request, trace, route, method, and duration fields.
    - Test 3: audit and domain code can read correlation fields from context without each handler reimplementing transport plumbing.
  </behavior>
  <action>Create `internal/observability` as the central place for logger and trace setup. Pick one JSON logger style for the whole Go backend and replace the current ad hoc request logger in `router.go`. Add config/env wiring in `config.go` and `app.go` for trace/log initialization, but keep the stack lightweight: no dashboard work, no admin UI, no full telemetry platform requirement in this plan. Middleware should inject request ID, trace ID, and any reusable examination/actor context hooks before handlers run. Append a concise implementation note to `docs/02_implementation.md` when the shared backend observability package is wired.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/http ./internal/observability -run 'TestRequestLoggingEmitsStructuredFields|TestRequestContextIncludesTraceAndRequestIDs|TestTraceContextMiddleware' -count=1</automated>
  </verify>
  <done>`core-backend` no longer relies on plain `log.Printf` request logging and exposes one shared observability foundation for later endpoint and metric work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate correlation through async and outbound service boundaries</name>
  <files>core-backend/internal/processing/contracts.go, core-backend/internal/processing/publisher.go, core-backend/internal/processing/results_consumer.go, core-backend/internal/baselineclient/client.go, core-backend/internal/kesmi/client.go, ml-services/ml-text/app/telemetry.py, ml-services/ml-text/app/main.py, ml-services/ml-acoustic/app/telemetry.py, ml-services/ml-acoustic/app/main.py, ml-services/ml-paralinguistic/app/telemetry.py, ml-services/ml-paralinguistic/app/main.py, ml-services/ml-baseline/app/telemetry.py, ml-services/ml-baseline/app/main.py</files>
  <behavior>
    - Test 1: processing command and result envelopes carry the documented trace transport fields alongside the existing `correlation_id`.
    - Test 2: worker logs and spans extracted from message handling preserve request/examination correlation into result publication.
    - Test 3: baseline and KESMI outbound HTTP requests forward trace context instead of starting disconnected traces.
  </behavior>
  <action>Extend the published processing envelopes with the trace transport fields from Phase 5 contracts and propagate them through the outbox relay, result consumer, and worker runtimes. Add lightweight shared telemetry helpers for `ml-text`, `ml-acoustic`, `ml-paralinguistic`, and `ml-baseline` so the Python services use one pattern for extracting trace context, setting structured log fields, and continuing the trace into outbound calls where applicable. Update `baselineclient` and `kesmi` clients so outbound HTTP requests carry trace context and correlation metadata. Keep `correlation_id` intact; do not replace it with trace IDs.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/processing ./internal/channelresults ./internal/decision ./internal/baselineclient ./internal/kesmi -run 'TestPublisherPreservesTraceContext|TestResultsConsumerContinuesTrace|TestBaselineClientPropagatesTraceContext|TestKESMIClientPropagatesTraceContext' -count=1 && cd /home/vadim/diplom/ml-services/ml-baseline && pytest -q tests/test_observability.py && cd /home/vadim/diplom/ml-services/ml-text && pytest -q tests/test_observability.py</automated>
  </verify>
  <done>The shipped runtime carries correlation consistently across HTTP, RabbitMQ, and outbound HTTP service boundaries.</done>
</task>

</tasks>

<verification>
Run the focused Go and Python tests and confirm that correlation survives both async messaging and outbound service calls while logs stay structured and field-based.
</verification>

<success_criteria>
- Shared structured logging is live in `core-backend`.
- Trace context and `correlation_id` both survive the async pipeline and outbound baseline/KESMI calls.
- Python services use one telemetry helper pattern instead of ad hoc per-service logging.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-trustworthiness/05-03-SUMMARY.md`
</output>
