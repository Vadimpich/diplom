---
phase: 05-operational-trustworthiness
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - core-backend/internal/http/health.go
  - core-backend/internal/http/observability_test.go
  - core-backend/internal/observability/metrics.go
  - core-backend/internal/auth/service_test.go
  - core-backend/internal/examinations/service_test.go
  - core-backend/internal/processing/service_test.go
  - core-backend/internal/channelresults/service_test.go
  - core-backend/internal/decision/relay_test.go
  - ml-services/ml-text/tests/test_observability.py
  - ml-services/ml-baseline/tests/test_observability.py
  - docker-compose.yml
  - ops/prometheus/prometheus.yml
  - README.md
  - .env.example
  - .planning/phases/05-operational-trustworthiness/05-VALIDATION.md
  - docs/02_implementation.md
autonomous: true
requirements:
  - OBSV-02
  - QUAL-02
must_haves:
  truths:
    - Every runtime service exposes cheap liveness, dependency-aware readiness, and Prometheus metrics.
    - Maintainers can verify the trustworthiness stack locally with one compose path instead of manual per-service probing.
    - Critical workflow transitions, idempotency fences, and failure handling are covered by automated regression tests after observability work lands.
  artifacts:
    - core-backend/internal/http/health.go and core-backend/internal/observability/metrics.go implement shipped readiness and metrics behavior.
    - docker-compose.yml and ops/prometheus/prometheus.yml define the minimal local scrape path for `/metrics`.
    - README.md, 05-VALIDATION.md, and docs/02_implementation.md reflect the shipped verification and runtime behavior.
  key_links:
    - `/health` must stay cheap while `/ready` reflects real dependency state for the shipped Phase 1-4 workflow.
    - Prometheus scrape config must match the actual compose service names and internal ports.
    - Regression tests must assert status transitions and idempotency semantics that earlier phases established, now with audit/observability side effects present.
---

<objective>
Finish Phase 5 with shipped readiness and metrics endpoints, minimal compose-level observability verification, and hardened workflow regression coverage.

Purpose: make the current architecture operable and safe to evolve without adding heavier production infrastructure than the roadmap requires.
Output: working `/health`/`/ready`/`/metrics` surfaces, minimal local scrape path, stronger regression tests, and synchronized docs.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operational-trustworthiness/05-RESEARCH.md
@.planning/phases/05-operational-trustworthiness/05-VALIDATION.md
@.planning/phases/05-operational-trustworthiness/05-03-SUMMARY.md
@docs/00_project.md
@docs/01_contract.md
@docs/02_implementation.md
@README.md
@docker-compose.yml
@core-backend/internal/http/health.go
@core-backend/internal/auth/service_test.go
@core-backend/internal/examinations/service_test.go
@core-backend/internal/processing/service_test.go
@core-backend/internal/channelresults/service_test.go
@core-backend/internal/decision/relay_test.go

<interfaces>
From research readiness contract:
```text
`/health`: process alive, cheap enough for container healthcheck.
`/ready`: dependency-aware gate for safe traffic/work.
`/metrics`: Prometheus exposition with low-cardinality labels only.
```

From prior phases:
```text
Critical workflow fences already exist for finish launch, channel retries, aggregation completion, and decision relay recovery.
Phase 5 regression must preserve those behaviors while adding audit and observability side effects.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship readiness and metrics endpoints across the runtime</name>
  <files>core-backend/internal/http/health.go, core-backend/internal/http/observability_test.go, core-backend/internal/observability/metrics.go, ml-services/ml-text/tests/test_observability.py, ml-services/ml-baseline/tests/test_observability.py</files>
  <behavior>
    - Test 1: `core-backend` keeps `/health` cheap and moves PostgreSQL, RabbitMQ, MinIO, baseline, and WiMi dependency fan-in to `/ready`.
    - Test 2: worker services expose `/ready` tied to broker/object-storage readiness, while baseline exposes `/ready` as a compute-only service.
    - Test 3: `/metrics` exposes counters, gauges, and histograms with bounded labels and no examination/user identifiers.
  </behavior>
  <action>Implement the contracts locked in Plan 01. Update `core-backend/internal/http/health.go` and add `internal/observability/metrics.go` so `core-backend` exposes cheap liveness, dependency-aware readiness, and Prometheus metrics for HTTP, errors, and key dependency states. Complete the Python observability tests by adding `/ready` and `/metrics` to the worker and baseline runtimes with the same low-cardinality metric discipline from the contract. Keep the implementation minimal and operational: do not add Grafana, Loki, or admin UI in this plan.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/http ./internal/observability -run 'TestHealthEndpointIsCheap|TestReadinessDegradesOnDependencyFailure|TestMetricsEndpointExposesLowCardinalityFamilies' -count=1 && cd /home/vadim/diplom/ml-services/ml-baseline && pytest -q tests/test_observability.py && cd /home/vadim/diplom/ml-services/ml-text && pytest -q tests/test_observability.py</automated>
  </verify>
  <done>The shipped services expose working `/health`, `/ready`, and `/metrics` surfaces that match the documented Phase 5 contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden workflow regression coverage with observability side effects</name>
  <files>core-backend/internal/auth/service_test.go, core-backend/internal/examinations/service_test.go, core-backend/internal/processing/service_test.go, core-backend/internal/channelresults/service_test.go, core-backend/internal/decision/relay_test.go</files>
  <behavior>
    - Test 1: critical status transitions still behave correctly when audit and observability code is enabled.
    - Test 2: idempotent finish and retry flows still produce one semantic state transition and bounded side effects.
    - Test 3: failure paths remain classified and terminal/non-terminal exactly as earlier phases defined.
  </behavior>
  <action>Expand the existing Go test suites so the critical workflow remains safe after Phase 5 plumbing. Strengthen auth, examinations, processing, channelresults, and decision relay tests around login audit side effects, finish idempotency, outbox retry semantics, mandatory-channel failure handling, aggregation-to-decision continuity, and decision relay retry exhaustion. Focus assertions on state transitions, audit event counts/types, readiness behavior, and correlation fields; do not assert whole log lines.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/auth ./internal/examinations ./internal/processing ./internal/channelresults ./internal/decision ./internal/http -count=1</automated>
  </verify>
  <done>The critical workflow has automated regression coverage for status transitions, idempotency, failures, and new audit/observability side effects.</done>
</task>

<task type="auto">
  <name>Task 3: Wire minimal compose observability and refresh the runbook</name>
  <files>docker-compose.yml, ops/prometheus/prometheus.yml, README.md, .env.example, .planning/phases/05-operational-trustworthiness/05-VALIDATION.md, docs/02_implementation.md</files>
  <action>Add the minimal compose-level verification path needed for Phase 5, not a full monitoring platform. Extend `docker-compose.yml` with the smallest practical internal Prometheus service that scrapes the shipped `/metrics` endpoints, add `ops/prometheus/prometheus.yml`, and document the runtime in `README.md`, `.env.example`, and `05-VALIDATION.md`. Keep scope realistic: one local scrape path is enough if it proves the metrics endpoints and dependency-aware readiness checks. Append the Phase 5 implementation log entry to `docs/02_implementation.md` after the smoke path is documented and verified.</action>
  <verify>
    <automated>cd /home/vadim/diplom && docker compose up -d --build core-backend frontend text-worker acoustic-worker paralinguistic-worker ml-baseline wimi prometheus && docker compose ps && curl -fsS http://localhost:8080/health >/dev/null && curl -fsS http://localhost:8080/ready >/dev/null && curl -fsS http://localhost:9090/-/healthy >/dev/null && cd /home/vadim/diplom/core-backend && go test ./... -count=1</automated>
  </verify>
  <done>Phase 5 ships with a reproducible local observability verification path and synchronized operator/developer documentation.</done>
</task>

</tasks>

<verification>
Run the Phase 5 validation file after implementation so the compose smoke, backend tests, and Python tests all reflect the shipped runtime and not only unit-level changes.
</verification>

<success_criteria>
- `core-backend` and Python services expose working `/health`, `/ready`, and `/metrics`.
- Minimal compose-level scraping verifies metrics availability without introducing unnecessary platform scope.
- Critical workflow regression coverage includes transitions, idempotency, failures, audit side effects, and observability contracts.
- `README.md`, `.env.example`, `05-VALIDATION.md`, and `docs/02_implementation.md` reflect the shipped Phase 5 runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-trustworthiness/05-04-SUMMARY.md`
</output>
