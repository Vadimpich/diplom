---
phase: 05-operational-trustworthiness
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - frontend/app/api/health/route.ts
  - frontend/app/api/ready/route.ts
  - frontend/app/api/metrics/route.ts
  - core-backend/internal/http/health.go
  - core-backend/internal/http/observability_test.go
  - core-backend/internal/observability/metrics.go
  - ml-services/ml-text/app/telemetry.py
  - ml-services/ml-acoustic/app/telemetry.py
  - ml-services/ml-paralinguistic/app/telemetry.py
  - ml-services/ml-acoustic/tests/test_observability.py
  - ml-services/ml-paralinguistic/tests/test_observability.py
  - ml-services/ml-text/tests/test_observability.py
  - ml-services/ml-baseline/tests/test_observability.py
  - docker-compose.yml
  - ops/prometheus/prometheus.yml
  - README.md
  - .env.example
  - docs/02_implementation.md
autonomous: true
requirements:
  - OBSV-02
  - QUAL-01
must_haves:
  truths:
    - Every runtime service exposes cheap liveness, dependency-aware readiness, and Prometheus metrics.
    - Maintainers can verify the trustworthiness stack locally with one compose path instead of manual per-service probing.
    - Runtime verification must explicitly cover `frontend`, `core-backend`, `ml-text`, `ml-acoustic`, `ml-paralinguistic`, and `ml-baseline`.
    - Python worker hops must not break end-to-end traceability: they must extract incoming trace context, log with correlation fields, and continue trace context when publishing results.
  artifacts:
    - core-backend/internal/http/health.go and core-backend/internal/observability/metrics.go implement shipped readiness and metrics behavior.
    - docker-compose.yml and ops/prometheus/prometheus.yml define the minimal local scrape path for `/metrics`.
    - frontend/app/api/health/route.ts, frontend/app/api/ready/route.ts, and frontend/app/api/metrics/route.ts provide minimal frontend runtime surfaces.
    - worker telemetry helpers preserve trace context and structured correlated logs across the RabbitMQ processing hop.
    - README.md and docs/02_implementation.md reflect the shipped verification and runtime behavior.
  key_links:
    - `/health` must stay cheap while `/ready` reflects real dependency state for the shipped Phase 1-4 workflow.
    - Prometheus scrape config must match the actual compose service names and internal ports.
    - Compose smoke must probe the real runtime endpoints instead of assuming docs are enough.
---

<objective>
Ship readiness and metrics endpoints across the runtime and prove them with explicit compose-level observability verification.

Purpose: make the current architecture operationally inspectable without adding heavier production infrastructure than the roadmap requires.
Output: working `/health`/`/ready`/`/metrics` surfaces for the shipped services, minimal local scrape path, and synchronized runtime docs.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operational-trustworthiness/05-RESEARCH.md
@.planning/phases/05-operational-trustworthiness/05-03-SUMMARY.md
@docs/00_project.md
@docs/01_contract.md
@docs/02_implementation.md
@README.md
@docker-compose.yml
@core-backend/internal/http/health.go
@frontend/app
@ml-services/ml-text/app/main.py
@ml-services/ml-text/app/telemetry.py
@ml-services/ml-acoustic/app/main.py
@ml-services/ml-acoustic/app/telemetry.py
@ml-services/ml-paralinguistic/app/main.py
@ml-services/ml-paralinguistic/app/telemetry.py
@ml-services/ml-baseline/app/main.py

<interfaces>
From research readiness contract:
```text
`/health`: process alive, cheap enough for container healthcheck.
`/ready`: dependency-aware gate for safe traffic/work.
`/metrics`: Prometheus exposition with low-cardinality labels only.
```

From prior phases:
```text
Critical workflow fences already exist for finish launch, channel retries, aggregation completion, and decision relay recovery.
Phase 5 regression must preserve those behaviors while adding audit and observability side effects.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship readiness and metrics endpoints across the runtime</name>
  <files>frontend/app/api/health/route.ts, frontend/app/api/ready/route.ts, frontend/app/api/metrics/route.ts, core-backend/internal/http/health.go, core-backend/internal/http/observability_test.go, core-backend/internal/observability/metrics.go, ml-services/ml-text/app/telemetry.py, ml-services/ml-acoustic/app/telemetry.py, ml-services/ml-paralinguistic/app/telemetry.py, ml-services/ml-text/tests/test_observability.py, ml-services/ml-acoustic/tests/test_observability.py, ml-services/ml-paralinguistic/tests/test_observability.py, ml-services/ml-baseline/tests/test_observability.py</files>
  <behavior>
    - Test 1: `core-backend` keeps `/health` cheap and moves PostgreSQL, RabbitMQ, MinIO, baseline, and WiMi dependency fan-in to `/ready`.
    - Test 2: `frontend`, worker services, and baseline expose `/health`, `/ready`, and `/metrics` according to their actual dependency model.
    - Test 3: worker runtimes extract incoming trace context from RabbitMQ, include `request_id`/`correlation_id` in structured logs, and preserve trace context on result publication.
    - Test 4: `/metrics` exposes counters, gauges, and histograms with bounded labels and no examination/user identifiers.
  </behavior>
  <action>Implement the contracts locked in Plan 01. Update `core-backend/internal/http/health.go` and add `internal/observability/metrics.go` so `core-backend` exposes cheap liveness, dependency-aware readiness, and Prometheus metrics for HTTP, errors, and key dependency states. Add minimal runtime surfaces for `frontend` via Next.js route handlers. For `ml-text`, `ml-acoustic`, and `ml-paralinguistic`, add lightweight telemetry helpers that extract incoming trace context from RabbitMQ, include correlation fields in structured logs, and preserve trace context when publishing result envelopes. Complete the Python observability tests by adding `/ready` and `/metrics` to `ml-text`, `ml-acoustic`, `ml-paralinguistic`, and `ml-baseline` with the same low-cardinality metric discipline from the contract. Keep the implementation minimal and operational: do not add Grafana, Loki, or admin UI in this plan.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/http ./internal/observability -run 'TestHealthEndpointIsCheap|TestReadinessDegradesOnDependencyFailure|TestMetricsEndpointExposesLowCardinalityFamilies' -count=1 && cd /home/vadim/diplom/ml-services/ml-baseline && pytest -q tests/test_observability.py && cd /home/vadim/diplom/ml-services/ml-text && pytest -q tests/test_observability.py && cd /home/vadim/diplom/ml-services/ml-acoustic && pytest -q tests/test_observability.py && cd /home/vadim/diplom/ml-services/ml-paralinguistic && pytest -q tests/test_observability.py && cd /home/vadim/diplom/frontend && npm run lint</automated>
  </verify>
  <done>The shipped services expose working `/health`, `/ready`, and `/metrics` surfaces that match the documented Phase 5 contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire minimal compose observability and refresh the runtime docs</name>
  <files>docker-compose.yml, ops/prometheus/prometheus.yml, README.md, .env.example, docs/02_implementation.md</files>
  <action>Add the minimal compose-level verification path needed for Phase 5, not a full monitoring platform. Extend `docker-compose.yml` with the smallest practical internal Prometheus service that scrapes the shipped `/metrics` endpoints, add `ops/prometheus/prometheus.yml`, and document the runtime in `README.md` and `.env.example`. Keep scope realistic: one local scrape path is enough if it proves the metrics endpoints and dependency-aware readiness checks. Append the Phase 5 implementation log entry to `docs/02_implementation.md` after the smoke path is documented and verified.</action>
  <verify>
    <automated>cd /home/vadim/diplom && docker compose up -d --build frontend core-backend text-worker acoustic-worker paralinguistic-worker ml-baseline wimi prometheus && curl -fsS http://localhost:3000/api/health >/dev/null && curl -fsS http://localhost:3000/api/ready >/dev/null && curl -fsS http://localhost:3000/api/metrics >/dev/null && curl -fsS http://localhost:8080/health >/dev/null && curl -fsS http://localhost:8080/ready >/dev/null && curl -fsS http://localhost:8080/metrics >/dev/null && docker compose exec text-worker python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/ready', timeout=3).read(); urllib.request.urlopen('http://127.0.0.1:8080/metrics', timeout=3).read()" && docker compose exec acoustic-worker python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/ready', timeout=3).read(); urllib.request.urlopen('http://127.0.0.1:8080/metrics', timeout=3).read()" && docker compose exec paralinguistic-worker python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/ready', timeout=3).read(); urllib.request.urlopen('http://127.0.0.1:8080/metrics', timeout=3).read()" && docker compose exec ml-baseline python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/ready', timeout=3).read(); urllib.request.urlopen('http://127.0.0.1:8080/metrics', timeout=3).read()" && curl -fsS http://localhost:9090/-/healthy >/dev/null && curl -fsS 'http://localhost:9090/api/v1/targets' | rg 'core-backend|frontend|text-worker|acoustic-worker|paralinguistic-worker|ml-baseline'</automated>
  </verify>
  <done>Phase 5 ships with a reproducible local observability verification path and synchronized runtime documentation.</done>
</task>

</tasks>

<verification>
Run the compose smoke and service-specific probes after implementation so the runtime endpoints are verified on the actual local stack, not only in unit tests.
</verification>

<success_criteria>
- `core-backend` and Python services expose working `/health`, `/ready`, and `/metrics`.
- Minimal compose-level scraping verifies metrics availability without introducing unnecessary platform scope.
- Runtime verification explicitly covers `frontend`, `core-backend`, `ml-text`, `ml-acoustic`, `ml-paralinguistic`, and `ml-baseline`.
- `README.md`, `.env.example`, and `docs/02_implementation.md` reflect the shipped Phase 5 runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-trustworthiness/05-04-SUMMARY.md`
</output>
