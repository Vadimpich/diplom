---
phase: 05-operational-trustworthiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/01_contract.md
  - .planning/phases/05-operational-trustworthiness/05-VALIDATION.md
  - core-backend/internal/audit/audit_test.go
  - core-backend/internal/http/observability_test.go
  - core-backend/internal/processing/publisher_test.go
  - core-backend/internal/channelresults/service_test.go
  - core-backend/internal/decision/relay_test.go
  - ml-services/ml-acoustic/tests/test_observability.py
  - ml-services/ml-paralinguistic/tests/test_observability.py
  - ml-services/ml-text/tests/test_observability.py
  - ml-services/ml-baseline/tests/test_observability.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - OBSV-02
  - OBSV-03
must_haves:
  truths:
    - Audit events, readiness endpoints, metrics exposition, and correlation fields are documented before implementation changes start.
    - The repo has failing tests that pin Phase 5 behavior for audit append, readiness degradation, and async trace propagation.
    - Maintainers have one validation file listing the commands that prove operational trustworthiness after execution.
  artifacts:
    - docs/01_contract.md documents the audit event schema, `/health`, `/ready`, `/metrics`, and correlation rules.
    - core-backend/internal/audit/audit_test.go and core-backend/internal/http/observability_test.go exist as RED scaffolds for backend trustworthiness behavior.
    - ml-services/ml-text/tests/test_observability.py, ml-services/ml-acoustic/tests/test_observability.py, ml-services/ml-paralinguistic/tests/test_observability.py, and ml-services/ml-baseline/tests/test_observability.py exist as RED scaffolds for Python service observability surfaces.
    - .planning/phases/05-operational-trustworthiness/05-VALIDATION.md exists with Phase 5 verification commands.
  key_links:
    - `docs/01_contract.md` defines the same correlation fields that later plans must carry through HTTP, RabbitMQ, and outbound HTTP clients.
    - RED tests pin readiness and metrics contracts before `/ready` and `/metrics` handlers are added.
    - Validation commands cover backend, Python services, and compose-level smoke instead of only file existence.
---

<objective>
Publish the Phase 5 contracts and RED verification scaffolds before adding audit, logging, or observability plumbing.

Purpose: keep audit and observability work contract-first and prevent drift across Go core, Python workers, and compose runtime.
Output: authoritative observability contracts, failing backend/Python tests, and a tracked validation file for the phase.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operational-trustworthiness/05-RESEARCH.md
@docs/00_project.md
@docs/01_contract.md
@docs/02_implementation.md
@core-backend/internal/http/router.go
@core-backend/internal/http/health.go
@core-backend/internal/processing/contracts.go
@core-backend/internal/decision/contracts.go
@ml-services/ml-text/app/main.py
@ml-services/ml-baseline/app/main.py

<interfaces>
From core-backend/internal/processing/contracts.go:
```go
type ProcessingCommandEnvelope struct {
	MessageVersion int
	MessageID      string
	CorrelationID  string
	ExaminationID  int64
	SpecialistID   int64
	Channel        string
	Attempt        int32
	MaxAttempts    int32
	RequestedAt    time.Time
	Answers        []CommandAnswerReference
}

type ChannelResultEnvelope struct {
	MessageVersion int
	MessageID      string
	CorrelationID  string
	ExaminationID  int64
	Channel        string
	Attempt        int32
	Status         string
	CompletedAt    time.Time
	ModelVersion   string
	ErrorCode      *string
	ErrorMessage   *string
	Payload        json.RawMessage
}
```

From core-backend/internal/http/router.go:
```go
router.Get("/health", handler.Health)
```

From research constraints:
```text
Use durable PostgreSQL-backed audit storage, not log scraping.
Keep `/health` cheap and push dependency fan-in into `/ready`.
Preserve existing `correlation_id`; augment it with standardized trace context.
Do not add an admin audit UI unless Phase 5 requirements force it.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the audit and observability contracts</name>
  <files>docs/01_contract.md, .planning/phases/05-operational-trustworthiness/05-VALIDATION.md</files>
  <behavior>
    - Test 1: contract docs define a versioned `audit_event` schema with event type, actor, outcome, request/trace identifiers, and domain references.
    - Test 2: each runtime service has documented `/health`, `/ready`, and `/metrics` semantics, including what degrades readiness.
    - Test 3: correlation rules explicitly preserve `correlation_id` and add `traceparent` or equivalent trace-context transport across HTTP, RabbitMQ, and KESMI calls.
  </behavior>
  <action>Update `docs/01_contract.md` first. Add a Phase 5 observability section that documents: the internal `audit_event` schema; required event types for login, admin mutations, examination create/start/finish, processing launch, channel result receipt, aggregation completion, and decision completion/failure; `/health`, `/ready`, and `/metrics` contracts for `core-backend`, `frontend`, `ml-text`, `ml-acoustic`, `ml-paralinguistic`, `ml-baseline`, and the internal WiMi-backed decision dependency boundary; metric naming conventions with low-cardinality labels only; and the correlation model that keeps the existing `correlation_id` while adding trace context propagation. Create `05-VALIDATION.md` with the exact backend, Python, and compose smoke commands that later plans must satisfy. Keep this task documentation-only; do not implement handlers yet.</action>
  <verify>
    <automated>cd /home/vadim/diplom && rg -n 'audit_event|/ready|/metrics|traceparent|correlation_id|processing.launch|decision.failed' docs/01_contract.md .planning/phases/05-operational-trustworthiness/05-VALIDATION.md</automated>
  </verify>
  <done>`docs/01_contract.md` becomes the single source of truth for Phase 5 audit, observability, and correlation contracts, and `05-VALIDATION.md` exists with concrete verification commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backend RED scaffolds for audit, readiness, and correlation</name>
  <files>core-backend/internal/audit/audit_test.go, core-backend/internal/http/observability_test.go, core-backend/internal/processing/publisher_test.go, core-backend/internal/channelresults/service_test.go, core-backend/internal/decision/relay_test.go</files>
  <behavior>
    - Test 1: successful and failed auth flows append audit events with `outcome` and correlation fields.
    - Test 2: `/ready` reports degraded readiness when PostgreSQL, RabbitMQ, MinIO, baseline, or WiMi-backed dependencies required by the shipped workflow are unavailable, while `/health` remains cheap.
    - Test 3: outbox publish, result consume, and decision relay preserve `correlation_id` and carry trace context instead of breaking distributed traces at async boundaries.
  </behavior>
  <action>Add RED tests that describe the backend behavior Phase 5 must deliver. Create `core-backend/internal/audit/audit_test.go` for audit append and idempotent event writing at the source-of-truth boundary. Add `core-backend/internal/http/observability_test.go` for `/health`, `/ready`, and `/metrics` endpoint behavior. Extend `core-backend/internal/processing/publisher_test.go`, `core-backend/internal/channelresults/service_test.go`, and `core-backend/internal/decision/relay_test.go` so they pin correlation propagation and structured event metadata across outbox publish, result receipt, and decision delivery. Tests should assert event types, IDs, and payload fields, not logger text formatting.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/audit ./internal/http ./internal/processing ./internal/channelresults ./internal/decision -run 'TestAudit|TestReadiness|TestMetrics|TestTrace|TestCorrelation' -count=1</automated>
  </verify>
  <done>The backend has intentional RED coverage for Phase 5 audit, readiness, metrics, and correlation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Python service RED scaffolds for readiness and metrics</name>
  <files>ml-services/ml-text/tests/test_observability.py, ml-services/ml-acoustic/tests/test_observability.py, ml-services/ml-paralinguistic/tests/test_observability.py, ml-services/ml-baseline/tests/test_observability.py</files>
  <behavior>
    - Test 1: worker services expose `/health`, `/ready`, and `/metrics` with readiness coupled to broker/object-storage availability rather than process liveness only.
    - Test 2: the baseline service exposes `/ready` and `/metrics` without inventing DB or RabbitMQ dependencies.
    - Test 3: observability responses and metric names match the contract written in `docs/01_contract.md`.
  </behavior>
  <action>Add RED pytest scaffolds for all shipped Python runtimes. `ml-services/ml-text/tests/test_observability.py`, `ml-services/ml-acoustic/tests/test_observability.py`, and `ml-services/ml-paralinguistic/tests/test_observability.py` should pin worker `/health`, `/ready`, and `/metrics` semantics plus correlation-aware processing expectations. `ml-services/ml-baseline/tests/test_observability.py` should pin baseline `/ready` and `/metrics` behavior as a compute-only HTTP service. Keep the tests lightweight and contract-shaped; do not implement telemetry in this task.</action>
  <verify>
    <automated>cd /home/vadim/diplom/ml-services/ml-baseline && pytest -q tests/test_observability.py && cd /home/vadim/diplom/ml-services/ml-text && pytest -q tests/test_observability.py && cd /home/vadim/diplom/ml-services/ml-acoustic && pytest -q tests/test_observability.py && cd /home/vadim/diplom/ml-services/ml-paralinguistic && pytest -q tests/test_observability.py</automated>
  </verify>
  <done>The Python services have failing observability contract tests ready for the implementation plans that follow.</done>
</task>

</tasks>

<verification>
Run the targeted Go and Python tests and confirm they fail because implementation is missing, not because contracts or test files are absent.
</verification>

<success_criteria>
- `docs/01_contract.md` documents the Phase 5 audit schema, observability endpoints, metric naming rules, and correlation transport.
- Backend and Python RED tests exist for audit append, readiness, metrics, and async correlation.
- `05-VALIDATION.md` lists the commands needed to prove the phase end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-trustworthiness/05-01-SUMMARY.md`
</output>
