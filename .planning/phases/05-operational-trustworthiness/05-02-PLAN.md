---
phase: 05-operational-trustworthiness
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - core-backend/migrations/000009_audit_observability.up.sql
  - core-backend/migrations/000009_audit_observability.down.sql
  - core-backend/db/queries/audit.sql
  - core-backend/db/sqlc/audit.sql.go
  - core-backend/internal/audit/contracts.go
  - core-backend/internal/audit/repository.go
  - core-backend/internal/audit/service.go
  - core-backend/internal/audit/audit_test.go
  - core-backend/internal/auth/service.go
  - core-backend/internal/auth/repository.go
  - core-backend/internal/questionnaires/service.go
  - core-backend/internal/examinations/service.go
  - core-backend/internal/processing/service.go
  - core-backend/internal/channelresults/service.go
  - core-backend/internal/decision/service.go
  - docs/02_implementation.md
autonomous: true
requirements:
  - OBSV-01
  - QUAL-02
must_haves:
  truths:
    - Critical user and system actions are persisted in an audit trail instead of depending on mutable logs.
    - Audit rows for authoritative state transitions are written in the same transaction as the state change where possible.
    - Replayed requests or retries do not create misleading duplicate semantic audit events for one fenced transition.
  artifacts:
    - core-backend/migrations/000009_audit_observability.up.sql creates durable audit storage.
    - core-backend/internal/audit/repository.go and core-backend/internal/audit/service.go implement audit append helpers for source-of-truth writes.
    - auth, questionnaires, examinations, processing, channelresults, and decision packages append audit events for the required Phase 5 actions.
  key_links:
    - Admin write-paths for users and questionnaires must emit audit events from the same core-owned mutation boundary as the persisted change.
    - Examination finish and processing launch must audit from the same transactional fence that already prevents duplicate launches.
    - Channel result receipt and decision completion must append audit from the core-owned state transition, not from workers or raw logs.
    - Failed login auditing must record `outcome=failed` even though no domain transaction commits.
---

<objective>
Implement the durable audit trail in `core-backend` and wire it to the critical actions required by Phase 5.

Purpose: satisfy operational auditability without introducing an admin UI or moving source-of-truth writes out of PostgreSQL.
Output: audit schema, repository/service helpers, wired event emission for critical auth, admin, examination, processing, result, and decision transitions.
</objective>

<execution_context>
@/home/vadim/.codex/get-shit-done/workflows/execute-plan.md
@/home/vadim/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operational-trustworthiness/05-RESEARCH.md
@.planning/phases/05-operational-trustworthiness/05-01-SUMMARY.md
@docs/00_project.md
@docs/01_contract.md
@docs/02_implementation.md
@core-backend/internal/auth/service.go
@core-backend/internal/questionnaires/service.go
@core-backend/internal/examinations/service.go
@core-backend/internal/processing/service.go
@core-backend/internal/channelresults/service.go
@core-backend/internal/decision/service.go

<interfaces>
From research audit scope:
```text
Required audit coverage: login, critical admin actions (user and questionnaire mutations in current v1 scope), examination creation/start/finish, processing launch, result receipt, decision completion/failure.
Append audit rows in the same transaction as the source-of-truth write where possible.
```

From core-backend/internal/processing/service.go and existing phase patterns:
```text
The finish path already uses an idempotent processing-launch fence and transactional outbox.
Audit must attach to that existing fence, not create a second launch path.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the authoritative audit schema and repository surface</name>
  <files>core-backend/migrations/000009_audit_observability.up.sql, core-backend/migrations/000009_audit_observability.down.sql, core-backend/db/queries/audit.sql, core-backend/db/sqlc/audit.sql.go, core-backend/internal/audit/contracts.go, core-backend/internal/audit/repository.go, core-backend/internal/audit/service.go, core-backend/internal/audit/audit_test.go</files>
  <behavior>
    - Test 1: audit rows store event type, actor, request/trace IDs, outcome, happened_at, and domain references such as examination_id or specialist_id.
    - Test 2: repository helpers support transactional append from existing domain flows without raw SQL in service packages.
    - Test 3: duplicate semantic events can be fenced by a stable event key or equivalent source reference when the underlying state transition is idempotent.
  </behavior>
  <action>Create the durable audit foundation. Add migration `000009_audit_observability` for an `audit_logs` table with typed columns for event metadata plus JSON payload, and create `db/queries/audit.sql` so sqlc can generate append/query helpers. Add `internal/audit/contracts.go`, `repository.go`, and `service.go` to define the stable event model and append helpers. Keep the package backend-only in this phase; do not add an admin read API or UI. Make the schema queryable by event type, actor, examination, and time window so later phases can read it without redesign.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && sqlc generate && go test ./internal/audit -run 'TestAppendAuditEvent|TestAppendAuditEventUsesStableEventKey|TestListAuditEvents' -count=1</automated>
  </verify>
  <done>The repo has a durable, queryable audit schema and one backend package that other services can call transactionally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire audit append into critical source-of-truth transitions</name>
  <files>core-backend/internal/auth/service.go, core-backend/internal/auth/repository.go, core-backend/internal/questionnaires/service.go, core-backend/internal/examinations/service.go, core-backend/internal/processing/service.go, core-backend/internal/channelresults/service.go, core-backend/internal/decision/service.go, core-backend/internal/audit/audit_test.go, docs/02_implementation.md</files>
  <behavior>
    - Test 1: login success and failed login append distinct audit events with correct `outcome` and correlation metadata.
    - Test 2: admin user and questionnaire mutations append audit events with actor context and mutated resource coordinates.
    - Test 3: examination create/start/finish and processing launch append one event per fenced transition, even when finish is retried.
    - Test 4: channel result receipt and decision completion/failure append audit events from core-owned persisted transitions, not from workers.
  </behavior>
  <action>Inject audit emission into the critical services already owning the source-of-truth state changes. For auth, record successful and failed login attempts with `outcome` and actor context when available. For the current admin surface, record user create/update and questionnaire create/update from the backend mutation boundary rather than from HTTP wrappers. For examinations, record creation, start, and finish. In processing, append the processing-launch audit event when the transactional finish fence succeeds. In `channelresults` append a result-received audit event when a channel result is persisted. In `decision` append final decision success/failure audit events when snapshot state becomes terminal. Preserve idempotency by attaching audit append to existing transactional fences where possible. Append a concise dated implementation log entry to `docs/02_implementation.md` once the audit trail is wired.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/auth ./internal/questionnaires ./internal/examinations ./internal/processing ./internal/channelresults ./internal/decision ./internal/audit -run 'TestLoginWritesAuditEvent|TestFailedLoginWritesAuditEvent|TestUserMutationWritesAuditEvent|TestQuestionnaireMutationWritesAuditEvent|TestExaminationFinishWritesSingleAuditEvent|TestProcessingLaunchWritesAuditEvent|TestResultReceiptWritesAuditEvent|TestDecisionTerminalStateWritesAuditEvent' -count=1</automated>
  </verify>
  <done>Phase 5 audit coverage is implemented for the required auth, admin, examination, processing, result, and decision actions and recorded in the append-only implementation log.</done>
</task>

</tasks>

<verification>
Run the focused Go tests and confirm audit rows are appended from the real transactional boundaries rather than from ad hoc logging hooks.
</verification>

<success_criteria>
- `audit_logs` persistence exists in PostgreSQL with generated query access.
- Critical auth, admin, examination, processing, result, and decision transitions append audit events.
- Idempotent workflow fences do not create misleading duplicate audit rows.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-trustworthiness/05-02-SUMMARY.md`
</output>
