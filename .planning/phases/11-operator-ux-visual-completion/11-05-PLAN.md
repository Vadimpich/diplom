---
phase: 11-operator-ux-visual-completion
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - core-backend/internal/http/router.go
  - core-backend/internal/http/rbac_test.go
  - docs/01_contract.md
autonomous: true
requirements:
  - OPRX-01
  - OPRX-02
gap_closure: true
must_haves:
  truths:
    - "Оператор может открыть создание обследования без 403 на загрузке списка опросников."
    - "Оператор может открыть экран текущего обследования и увидеть связанный опросник, если он был выбран."
    - "Контракт, router ACL и runtime-поведение одинаково трактуют read-only доступ оператора к списку опросников."
  artifacts:
    - path: "core-backend/internal/http/router.go"
      provides: "Router ACL where questionnaire list read is available to operator/admin, while admin mutations remain protected."
    - path: "core-backend/internal/http/rbac_test.go"
      provides: "Regression coverage pinning operator read access and admin-only questionnaire mutations."
    - path: "docs/01_contract.md"
      provides: "Synchronized role matrix and GET /questionnaires contract text."
  key_links:
    - from: "frontend/lib/api/client.ts"
      to: "core-backend/internal/http/router.go"
      via: "GET /questionnaires used by operator examination flow"
      pattern: "getQuestionnaires|/questionnaires"
    - from: "core-backend/internal/http/router.go"
      to: "docs/01_contract.md"
      via: "documented role access must match enforced ACL"
      pattern: "questionnaires"
---

<objective>
Close the operator questionnaire ACL drift that blocks UAT tests 1 and 2 before the operator can even start an examination.

Purpose: Restore a contract-consistent read path for operator examination screens without expanding questionnaire mutation scope.
Output: Backend ACL fix, RBAC regression test, and synchronized questionnaire access contract.
</objective>

<context>
@.planning/phases/11-operator-ux-visual-completion/11-UAT.md
@.planning/phases/11-operator-ux-visual-completion/11-RESEARCH.md
@.planning/debug/operator-questionnaires-403.md
@.planning/debug/operator-uat-test2-feedback-blocked-403.md
@docs/00_project.md
@docs/01_contract.md
@core-backend/internal/http/router.go
@core-backend/internal/http/rbac_test.go
@frontend/lib/api/client.ts
@frontend/app/(app)/operator/examinations/new/page.tsx
@frontend/app/(app)/operator/examinations/[id]/page.tsx

<interfaces>
From frontend/lib/api/client.ts:
```ts
createExamination(payload: { specialist_id: number; questionnaire_id?: number })
startExamination(id: number)
finishExamination(id: number)
getExamination(id: number)
getQuestionnaires()
```

From core-backend/internal/http/router.go (current state before fix):
```go
admin.Get("/questionnaires", questionnairesHandler.List)
admin.Post("/questionnaires", questionnairesHandler.Create)
admin.Get("/questionnaires/{id}", questionnairesHandler.GetByID)
admin.Put("/questionnaires/{id}", questionnairesHandler.Update)

operatorOrAdmin.Use(RequireRoles("operator", "admin"))
operatorOrAdmin.Post("/examinations", examinationsHandler.Create)
operatorOrAdmin.Get("/examinations/{id}", examinationsHandler.GetByID)
operatorOrAdmin.Post("/examinations/{id}/start", examinationsHandler.Start)
operatorOrAdmin.Post("/examinations/{id}/finish", examinationsHandler.Finish)
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Unblock operator questionnaire reads at the router layer</name>
  <files>core-backend/internal/http/router.go, core-backend/internal/http/rbac_test.go</files>
  <behavior>
    - Test 1: `operator` token can `GET /questionnaires` and does not receive `403`.
    - Test 2: `operator` token still cannot mutate questionnaire resources (`POST /questionnaires`, `PUT /questionnaires/{id}` stay forbidden).
    - Test 3: existing admin questionnaire routes remain reachable for `admin`.
  </behavior>
  <action>Per the diagnosed UAT gap, move only the read-only questionnaire list route into the `RequireRoles("operator", "admin")` group or expose an equivalent shared GET path there. Keep create/update/detail mutation ownership under `admin` and do not introduce a new endpoint family. Extend `core-backend/internal/http/rbac_test.go` with focused router-level regression coverage that proves operator `GET /questionnaires` succeeds while questionnaire mutations remain admin-only.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/http ./...</automated>
  </verify>
  <done>Operator JWTs can read the questionnaire list needed by the examination flow, and tests pin the intended ACL split.</done>
</task>

<task type="auto">
  <name>Synchronize questionnaire access contract with runtime ACL</name>
  <files>docs/01_contract.md</files>
  <action>Update the role-access matrix and the `GET /questionnaires` section so they describe one consistent model: operator/admin may read the questionnaire list for examination UX, while questionnaire creation and editing remain admin-only. Remove the current contradiction called out in the debug notes and keep wording aligned to the actual shipped routes.</action>
  <verify>
    <automated>cd /home/vadim/diplom/core-backend && go test ./internal/http ./...</automated>
  </verify>
  <done>`docs/01_contract.md` no longer contradicts the enforced questionnaire ACL.</done>
</task>
</tasks>

<verification>
The operator questionnaire dependency path is closed when `GET /questionnaires` is reachable for operator tokens, questionnaire mutations stay admin-only, and the contract text matches the router behavior.
</verification>

<success_criteria>
- UAT tests 1 and 2 are no longer blocked by a deterministic `403` on questionnaire list loading.
- Questionnaire read access is explicitly documented as read-only operator/admin behavior.
- A regression test will fail if questionnaire list access is moved back under admin-only ACL.
</success_criteria>

<output>
After completion, create `.planning/phases/11-operator-ux-visual-completion/11-05-SUMMARY.md`
</output>
