---
phase: 11-operator-ux-visual-completion
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/(app)/operator/history/page.tsx
  - frontend/app/(app)/operator/page.tsx
autonomous: true
requirements:
  - OPRX-03
  - DSGN-02
gap_closure: true
must_haves:
  truths:
    - "Пустая история обследований показывает один однозначный CTA без дублирования."
    - "Dashboard различает полностью пустую базу специалистов и поиск без совпадений."
    - "Поиск на dashboard сохраняет reset action только для filtered-empty сценария."
  artifacts:
    - path: "frontend/app/(app)/operator/history/page.tsx"
      provides: "History empty-state without duplicate start-examination CTA."
    - path: "frontend/app/(app)/operator/page.tsx"
      provides: "Dashboard branch order that separates empty-base and filtered-empty states."
  key_links:
    - from: "frontend/app/(app)/operator/history/page.tsx"
      to: "/operator/examinations/new"
      via: "single CTA rendered only in the states that need it"
      pattern: "Запустить новое обследование"
    - from: "frontend/app/(app)/operator/page.tsx"
      to: "frontend query state"
      via: "render branch depends on query.trim() and loaded specialist count"
      pattern: "filtered.length|items.length|query.trim"
---

<objective>
Close the two remaining frontend-only UAT gaps in operator empty-state composition without touching backend contracts or adding new APIs.

Purpose: Make day-to-day operator list screens unambiguous when there is no data or no search match.
Output: Fixed history CTA composition and corrected dashboard empty-state branching.
</objective>

<context>
@.planning/phases/11-operator-ux-visual-completion/11-UAT.md
@.planning/phases/11-operator-ux-visual-completion/11-RESEARCH.md
@.planning/debug/operator-history-empty-cta.md
@.planning/debug/operator-dashboard-search-empty-state.md
@docs/00_project.md
@frontend/app/(app)/operator/history/page.tsx
@frontend/app/(app)/operator/page.tsx
@frontend/components/ui/empty-state.tsx

<interfaces>
From frontend/app/(app)/operator/history/page.tsx:
```tsx
<EmptyState
  title="История пока пуста"
  action={<Link href="/operator/examinations/new">Запустить новое обследование</Link>}
/>
...
<Link href="/operator/examinations/new">Запустить новое обследование</Link>
```

From frontend/app/(app)/operator/page.tsx:
```tsx
filtered.length === 0 && (specialistsQuery.data?.items.length ?? 0) === 0
filtered.length === 0
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Remove the duplicated empty-history CTA</name>
  <files>frontend/app/(app)/operator/history/page.tsx</files>
  <action>Use the diagnosed composition issue as the guide: keep the launch action inside the dedicated empty-history `EmptyState`, then make the footer CTA conditional or remove it so the fully empty history screen renders one clear action. Preserve the existing filtered-empty state and populated-list behavior.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint && npm run build && npx tsc --noEmit</automated>
  </verify>
  <done>The empty-history screen exposes exactly one `Запустить новое обследование` CTA, while populated and filtered states keep their intended controls.</done>
</task>

<task type="auto">
  <name>Fix dashboard search empty-state precedence</name>
  <files>frontend/app/(app)/operator/page.tsx</files>
  <action>Reorder or tighten the empty-state conditions so an active non-empty search query prefers the filtered-empty branch, even when the full specialists dataset is empty. Keep the empty-database state only for the true no-data-and-no-query case, per the diagnosed gap and the Phase 11 decision that filtered-empty must not collapse into generic empty.</action>
  <verify>
    <automated>cd /home/vadim/diplom/frontend && npm run lint && npm run build && npx tsc --noEmit</automated>
  </verify>
  <done>`/operator` shows `Пока нет специалистов` only when no query is active, and `Совпадений нет` with reset action when the operator has typed a query with no matches.</done>
</task>
</tasks>

<verification>
Both operator list screens now expose distinct empty and filtered-empty states without duplicate or unreachable actions.
</verification>

<success_criteria>
- `/operator/history` no longer duplicates the launch CTA on a completely empty dataset.
- `/operator` differentiates zero specialists from zero matches under an active search.
- The fixes stay frontend-local and do not add backend or contract scope.
</success_criteria>

<output>
After completion, create `.planning/phases/11-operator-ux-visual-completion/11-06-SUMMARY.md`
</output>
