services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-dimplom}
      POSTGRES_USER: ${POSTGRES_USER:-dimplom}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dimplom_dev_password}
    ports:
      - "${POSTGRES_HOST_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$$POSTGRES_USER\" -d \"$$POSTGRES_DB\""]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER:-dimplom}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS:-dimplom_dev_password}
      RABBITMQ_DEFAULT_VHOST: ${RABBITMQ_DEFAULT_VHOST:-/}
    ports:
      - "${RABBITMQ_AMQP_HOST_PORT:-5672}:5672"
      - "${RABBITMQ_MANAGEMENT_HOST_PORT:-15672}:15672"
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-dimplom-minio}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-dimplom_minio_dev_password}
    ports:
      - "${MINIO_API_HOST_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_HOST_PORT:-9001}:9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9000/minio/health/live >/dev/null || wget -qO- http://127.0.0.1:9000/minio/health/live >/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  core-backend:
    build:
      context: ./core-backend
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_started
      minio:
        condition: service_started
    environment:
      HTTP_ADDR: ${HTTP_ADDR:-:8080}
      DATABASE_URL: postgres://${POSTGRES_USER:-dimplom}:${POSTGRES_PASSWORD:-dimplom_dev_password}@postgres:5432/${POSTGRES_DB:-dimplom}?sslmode=${DATABASE_SSLMODE:-disable}

      RABBITMQ_HOST: ${RABBITMQ_HOST:-rabbitmq}
      RABBITMQ_PORT: ${RABBITMQ_PORT:-5672}
      RABBITMQ_MANAGEMENT_PORT: ${RABBITMQ_MANAGEMENT_PORT:-15672}
      RABBITMQ_VHOST: ${RABBITMQ_DEFAULT_VHOST:-/}
      RABBITMQ_USER: ${RABBITMQ_DEFAULT_USER:-dimplom}
      RABBITMQ_PASSWORD: ${RABBITMQ_DEFAULT_PASS:-dimplom_dev_password}

      MINIO_ENDPOINT: ${MINIO_ENDPOINT:-http://minio:9000}
      MINIO_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-dimplom-minio}
      MINIO_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-dimplom_minio_dev_password}
      MINIO_BUCKET: ${MINIO_BUCKET:-dimplom-audio}
      MINIO_USE_SSL: ${MINIO_USE_SSL:-false}

      JWT_ISSUER: ${JWT_ISSUER:-dimplom}
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:-change-me-access-secret}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-change-me-refresh-secret}
      JWT_ACCESS_TTL: ${JWT_ACCESS_TTL:-15m}
      JWT_REFRESH_TTL: ${JWT_REFRESH_TTL:-720h}
      INITIAL_USER_LOGIN: ${INITIAL_USER_LOGIN:-operator}
      INITIAL_USER_PASSWORD: ${INITIAL_USER_PASSWORD:-operator123}
      INITIAL_USER_ROLE: ${INITIAL_USER_ROLE:-operator}
    ports:
      - "${CORE_BACKEND_HOST_PORT:-8080}:${CORE_BACKEND_PORT:-8080}"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO- http://127.0.0.1:${CORE_BACKEND_PORT:-8080}/health >/dev/null",
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      core-backend:
        condition: service_healthy
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://core-backend:8080}
    ports:
      - "${FRONTEND_HOST_PORT:-3000}:3000"

  text-worker:
    build:
      context: ./ml-text
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
      minio:
        condition: service_healthy
    environment:
      WORKER_APP_NAME: ${TEXT_WORKER_APP_NAME:-text-worker}
      WORKER_PORT: ${TEXT_WORKER_PORT:-8080}
      WORKER_QUEUE_NAME: ${TEXT_WORKER_QUEUE_NAME:-qq.processing.text}
      WORKER_COMMAND_ROUTING_KEY: ${TEXT_WORKER_COMMAND_ROUTING_KEY:-processing.command.text}
      WORKER_COMMAND_EXCHANGE: ${PROCESSING_COMMAND_EXCHANGE:-processing.commands}
      WORKER_RESULT_EXCHANGE: ${PROCESSING_RESULT_EXCHANGE:-processing.results}
      WORKER_RESULT_ROUTING_KEY: ${PROCESSING_RESULT_ROUTING_KEY:-processing.result}
      WORKER_PREFETCH_COUNT: ${WORKER_PREFETCH_COUNT:-1}
      RABBITMQ_HOST: ${RABBITMQ_HOST:-rabbitmq}
      RABBITMQ_PORT: ${RABBITMQ_PORT:-5672}
      RABBITMQ_VHOST: ${RABBITMQ_DEFAULT_VHOST:-/}
      RABBITMQ_USER: ${RABBITMQ_DEFAULT_USER:-dimplom}
      RABBITMQ_PASSWORD: ${RABBITMQ_DEFAULT_PASS:-dimplom_dev_password}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT:-http://minio:9000}
      MINIO_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-dimplom-minio}
      MINIO_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-dimplom_minio_dev_password}
      MINIO_USE_SSL: ${MINIO_USE_SSL:-false}
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=3).read()\""]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s

  acoustic-worker:
    build:
      context: ./ml-acoustic
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
      minio:
        condition: service_healthy
    environment:
      WORKER_APP_NAME: ${ACOUSTIC_WORKER_APP_NAME:-acoustic-worker}
      WORKER_PORT: ${ACOUSTIC_WORKER_PORT:-8080}
      WORKER_QUEUE_NAME: ${ACOUSTIC_WORKER_QUEUE_NAME:-qq.processing.acoustic}
      WORKER_COMMAND_ROUTING_KEY: ${ACOUSTIC_WORKER_COMMAND_ROUTING_KEY:-processing.command.acoustic}
      WORKER_COMMAND_EXCHANGE: ${PROCESSING_COMMAND_EXCHANGE:-processing.commands}
      WORKER_RESULT_EXCHANGE: ${PROCESSING_RESULT_EXCHANGE:-processing.results}
      WORKER_RESULT_ROUTING_KEY: ${PROCESSING_RESULT_ROUTING_KEY:-processing.result}
      WORKER_PREFETCH_COUNT: ${WORKER_PREFETCH_COUNT:-1}
      RABBITMQ_HOST: ${RABBITMQ_HOST:-rabbitmq}
      RABBITMQ_PORT: ${RABBITMQ_PORT:-5672}
      RABBITMQ_VHOST: ${RABBITMQ_DEFAULT_VHOST:-/}
      RABBITMQ_USER: ${RABBITMQ_DEFAULT_USER:-dimplom}
      RABBITMQ_PASSWORD: ${RABBITMQ_DEFAULT_PASS:-dimplom_dev_password}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT:-http://minio:9000}
      MINIO_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-dimplom-minio}
      MINIO_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-dimplom_minio_dev_password}
      MINIO_USE_SSL: ${MINIO_USE_SSL:-false}
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=3).read()\""]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s

  paralinguistic-worker:
    build:
      context: ./ml-paralinguistic
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
      minio:
        condition: service_healthy
    environment:
      WORKER_APP_NAME: ${PARALINGUISTIC_WORKER_APP_NAME:-paralinguistic-worker}
      WORKER_PORT: ${PARALINGUISTIC_WORKER_PORT:-8080}
      WORKER_QUEUE_NAME: ${PARALINGUISTIC_WORKER_QUEUE_NAME:-qq.processing.paralinguistic}
      WORKER_COMMAND_ROUTING_KEY: ${PARALINGUISTIC_WORKER_COMMAND_ROUTING_KEY:-processing.command.paralinguistic}
      WORKER_COMMAND_EXCHANGE: ${PROCESSING_COMMAND_EXCHANGE:-processing.commands}
      WORKER_RESULT_EXCHANGE: ${PROCESSING_RESULT_EXCHANGE:-processing.results}
      WORKER_RESULT_ROUTING_KEY: ${PROCESSING_RESULT_ROUTING_KEY:-processing.result}
      WORKER_PREFETCH_COUNT: ${WORKER_PREFETCH_COUNT:-1}
      RABBITMQ_HOST: ${RABBITMQ_HOST:-rabbitmq}
      RABBITMQ_PORT: ${RABBITMQ_PORT:-5672}
      RABBITMQ_VHOST: ${RABBITMQ_DEFAULT_VHOST:-/}
      RABBITMQ_USER: ${RABBITMQ_DEFAULT_USER:-dimplom}
      RABBITMQ_PASSWORD: ${RABBITMQ_DEFAULT_PASS:-dimplom_dev_password}
      MINIO_ENDPOINT: ${MINIO_ENDPOINT:-http://minio:9000}
      MINIO_ACCESS_KEY_ID: ${MINIO_ROOT_USER:-dimplom-minio}
      MINIO_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD:-dimplom_minio_dev_password}
      MINIO_USE_SSL: ${MINIO_USE_SSL:-false}
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=3).read()\""]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s

volumes:
  postgres-data:
  rabbitmq-data:
  minio-data:
