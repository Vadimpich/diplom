# Журнал реализации

## 2026-03-19

- В core backend добавлен минимальный CORS middleware на уровне общего chi-router: preflight `OPTIONS` больше не падает `405`, разрешённые origin берутся из `CORS_ALLOWED_ORIGINS` с локальными значениями по умолчанию `http://localhost:3000,http://127.0.0.1:3000`.
- Созданы обязательные документы `docs/01_contract.md` и `docs/02_implementation.md` в соответствии с требованиями `AGENTS.md`.
- Обновлены конфигурации субагентов в `.codex/agents/*.toml`: добавлены явные требования читать `docs/00_project.md` и `docs/01_contract.md`, синхронизироваться через документы, фиксировать изменения контрактов в `docs/01_contract.md` и добавлять записи в `docs/02_implementation.md`.
- Добавлен локальный инфраструктурный каркас: `docker-compose.yml`, `.env.example`, `.dockerignore`, `README.md`; backend Dockerfile переведён на сборку из текущего Go-каркаса с healthcheck по `/health`.
- Добавлен минимальный backend-каркас на Go: `cmd/api`, `internal/*`, конфигурация из env, HTTP-сервер на `chi`, health-check `GET /health` с проверкой PostgreSQL через `pgx`, стартовая миграция `users`, базовые `db/` и `Dockerfile`.
- Реализован базовый синхронный домен core backend: роли и пользователи с JWT login `/auth/login` и `/me`, CRUD специалистов, обследования со статусами `created` -> `collecting_answers` -> `ready_for_processing`, загрузка ответов в MinIO/S3 через `POST /answers`, полная схема БД и `sqlc`-repository слой.
- Доведён runtime домена до рабочего состояния: добавлен `POST /users`, автоприменение SQL-миграций при старте backend, bootstrap начального пользователя через env и совместимая загрузка S3-конфига для MinIO в локальном Docker Compose.
- Минимально завершён stage 3 backend API для frontend-сценариев: добавлены `GET/PUT /users`, `GET /examinations`, `GET /examinations/{id}`, `GET /specialists/{id}/examinations`, CRUD API опросников, sqlc-репозиторий для `questionnaires/questions/questionnaire_questions` и минимальная привязка `examinations.questionnaire_id` для operator flow.
- Реализован stage 3 frontend в `frontend/` на Next.js App Router + TypeScript: auth/login, TanStack Query и API-клиент, server-side protected layouts для `/operator/*` и `/admin/*`, operator flow со специалистами, обследованиями, MediaRecorder, processing/results shells, а также admin UI для пользователей, опросников, monitoring и settings placeholders по актуальным контрактам `docs/01_contract.md`.
- Реализован stage 3 frontend в `frontend/` на Next.js App Router: отдельные контуры `/operator/*` и `/admin/*` с раздельными layout, JWT login и route guards, базовый API-клиент, TanStack Query, Tailwind-стили и совместимая shadcn/ui-база компонентов.
- Подключён приоритетный operator flow: dashboard, CRUD специалистов, создание и прохождение обследования с выбором опросника, `MediaRecorder`-запись и загрузка ответов в `POST /answers`, экраны processing/results как честные UI-shell, история обследований по `GET /examinations` и `GET /specialists/{id}/examinations`.
- Подключён admin flow по актуальным контрактам: список/создание/редактирование пользователей, список/создание/редактирование опросников, monitoring по `GET /health`, placeholders для settings; frontend проверен командами `eslint`, `tsc --noEmit` и `next build`.
- Добавлен `frontend` сервис в корневой `docker-compose.yml` с production Dockerfile, `NEXT_PUBLIC_API_URL=http://core-backend:8080` внутри сети Compose и минимальными env/contract обновлениями для локального запуска.

## 2026-03-20

- Сгенерирована актуальная карта кодовой базы в `.planning/codebase/`: `STACK.md`, `INTEGRATIONS.md`, `ARCHITECTURE.md`, `STRUCTURE.md`, `CONVENTIONS.md`, `TESTING.md`, `CONCERNS.md` на основе текущего состояния `core-backend/`, `frontend/`, `docs/` и инфраструктурных файлов для последующего планирования фаз и онбординга.
- Инициализирован GSD planning-контур проекта для brownfield-репозитория: добавлены `.planning/PROJECT.md`, `.planning/config.json`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`; зафиксированы существующие validated capabilities, целевые v1 requirements и roadmap из 5 фаз для доведения реализации до архитектуры из `docs/00_project.md`.
- Созданы planning-артефакты `.planning/ROADMAP.md` и `.planning/STATE.md`, а также обновлена traceability-матрица в `.planning/REQUIREMENTS.md`: roadmap сжат до 5 фаз по coarse granularity и сфокусирован на brownfield-gap к целевой архитектуре из `docs/00_project.md`.
- В `core-backend` добавлена серверная модель `refresh_sessions`: login теперь возвращает пару `access_token` + opaque `refresh_token`, `/auth/refresh` ротирует refresh-сессию в PostgreSQL, `/auth/logout` отзывает её, а транспорт `HttpOnly` cookie оставлен за trusted frontend BFF boundary; добавлены тесты на login, refresh rotation, logout revocation и `/me`.
- Для Phase 1 начата реализация wave 2: backend-router разделён на role-scoped группы `admin` и `operator/admin`, а frontend auth transport переведён на Next.js BFF endpoints `/api/auth/*`, где refresh-session хранится через `HttpOnly` cookie и больше не записывается напрямую через `document.cookie`.
- Protected frontend layouts и `RouteGuard` переведены на BFF-backed session bootstrap через `/api/auth/session`: доступ в `/admin/*` и `/operator/*` больше не опирается на access-token cookie как на источник истины, а root/login redirects синхронизированы с refresh-session presence и ответом session endpoint.
- В intake-контуре ответы начали связываться не только с обследованием, но и с snapshot-вопросом/специалистом: `POST /answers` теперь требует `examination_question_id` и `specialist_id`, а backend валидирует принадлежность snapshot-вопроса конкретному обследованию и отклоняет дубликаты по одному вопросу.
- Завершение обследования переведено на transactional finish fence: `POST /examinations/{id}/finish` требует полноты ответов по `examination_questions`, идемпотентно удерживает единственный processing launch и оставляет `GET /specialists/{id}/examinations` авторитетным источником workflow-статусов.
- Specialist history UI на `/operator/specialists/[id]` теперь показывает статусы `created`, `collecting_answers`, `ready_for_processing` и timestamps напрямую из backend API, без вычисления workflow из локального состояния браузера.

## 2026-03-21

- Для Phase 2 опубликован единый processing contract в `docs/01_contract.md`: добавлены versioned AMQP envelopes, topology `processing.commands` / `processing.results`, endpoint `GET /examinations/{id}/processing-status` и расширенная status vocabulary обследования (`processing`, `failed`) при сохранении PostgreSQL как source of truth.
- В `core-backend/internal/processing/contracts.go` зафиксированы обязательные каналы `text`, `acoustic`, `paralinguistic` и DTO/envelope-структуры, чтобы следующие планы опирались на один и тот же Go-контракт.
- Добавлены RED-тесты `core-backend/internal/processing/service_test.go` и `core-backend/internal/http/processing_status_test.go`: они намеренно фиксируют ещё не реализованные fan-out после `finish` и availability processing-status endpoint до появления publisher/result-consumer логики.
- Добавлена миграция `000006_processing_pipeline` и SQL skeleton `db/queries/processing.sql` для durable outbox, per-channel runtime state и normalized result storage без пересмотра существующего finish fence.
- Добавлены runnable stub-сервисы `ml-services/ml-text`, `ml-services/ml-acoustic`, `ml-services/ml-paralinguistic`: каждый worker поднимает FastAPI `/health`, читает только свою RabbitMQ queue, забирает audio object по S3 reference и публикует унифицированный result envelope с `model_version`, `temporary_error`/`fatal_error`.
- Корневой `docker-compose.yml` расширен сервисами `text-worker`, `acoustic-worker`, `paralinguistic-worker`, а `.env.example` и `docs/01_contract.md` синхронизированы по exchange, routing keys, queue names и runtime env для локального self-hosted Phase 2 stack.
- `POST /examinations/{id}/finish` переведён на `internal/processing.Service`: core backend теперь в одной PostgreSQL-транзакции завершает обследование, поддерживает launch fence, создаёт `examination_channel_runs` и versioned `processing_outbox` записи для обязательных каналов без публикации в RabbitMQ из HTTP-пути.
- В `core-backend/internal/processing/publisher.go` добавлен фоновый outbox relay на `amqp091-go`: он объявляет RabbitMQ 3.13-совместимую topology с quorum queues, `processing.commands.dlx` и явным `x-delivery-limit`, публикует команды только после publisher confirm и пишет успешные/ошибочные попытки обратно в PostgreSQL для безопасного retry.
- Подключён route `GET /examinations/{id}/processing-status`: при наличии processing service backend собирает channel runtime state из PostgreSQL, а router/test fallback больше не падает на отсутствующем handler wiring.
- Core backend замкнул обратный async-контур: `internal/processing/results_consumer.go` читает unified queue `qq.processing.results`, а `internal/channelresults/service.go` сохраняет `channel_results`, переводит `examination_channel_runs` в `succeeded/retry_scheduled/failed_fatal/exhausted` и помечает обследование `failed` при исчерпании обязательного канала.
- Проекция `GET /examinations/{id}/processing-status` усилена backend-authoritative логикой: endpoint теперь отдаёт `409`, если обследование ещё не вошло в processing pipeline, и вычисляет `started_at`, `failed_at` и terminal-состояние только из PostgreSQL channel-run данных, без опоры на broker management state.
- Phase 2 закрыт reproducibility-обвязкой: локальный compose stack теперь поднимает frontend, core backend, PostgreSQL, RabbitMQ, MinIO и `text/acoustic/paralinguistic` stub workers как один проверяемый runtime; core backend использует явный `RABBITMQ_URL`, worker-очереди объявляются с теми же quorum/DLX retry аргументами, а runbook/validation зафиксировали команды smoke-проверки и operator-facing `processing-status` progress polling для shipped transactional outbox и channel queues pipeline.

## 2026-03-22

- Для Phase 3 plan 01 обновлён `docs/01_contract.md`: зафиксированы канонический aggregated result DTO, `GET /examinations/{id}/result`, `GET /specialists/{id}/result-history`, baseline service boundary `POST /baseline/calculate` и расширенная status vocabulary `aggregating` / `aggregated` с правилом `terminal=true` только для `aggregated` или `failed`.
- Добавлены RED-scaffolds `core-backend/internal/aggregation/service_test.go`, `core-backend/internal/http/results_handler_test.go`, `ml-services/ml-baseline/tests/test_service.py`, `ml-services/ml-baseline/tests/test_algorithms.py`, чтобы следующие планы реализовывали aggregation, result-history и baseline update gating против уже опубликованных контрактов.
- Добавлен persistence skeleton Phase 3: миграция `core-backend/migrations/000007_aggregated_profiles.*.sql`, SQL-файл `core-backend/db/queries/aggregation.sql` и экспортируемые Go-типы `core-backend/internal/aggregation/contracts.go` для aggregated profiles, metric snapshots, channel contributions, baseline state и examination baseline snapshots.
- Для Phase 3 plan 03 создан standalone `ml-services/ml-baseline` FastAPI service без доступа к PostgreSQL: versioned `POST /baseline/calculate` и `GET /health`, robust median/MAD update gating, bounded next-baseline snapshot и локальный compose wiring через внутренний сервис `ml-services/ml-baseline` с env `BASELINE_*`.
- Phase 3 закрыт end-to-end поверх async pipeline: core backend после всех `succeeded` mandatory channels переводит обследование в `aggregating`, строит канонический proxy-oriented профиль, вызывает internal `ml-services/ml-baseline`, сохраняет baseline snapshot и specialist baseline state в PostgreSQL, а затем финализирует workflow статусом `aggregated`.
- Добавлены operator-facing result surfaces `GET /examinations/{id}/result` и `GET /specialists/{id}/result-history`; frontend `/operator/examinations/[id]/results`, `/operator/specialists/[id]` и `/operator/history` переведён с placeholder-shells на реальные aggregated DTO, baseline deviations и history dynamics.
- Runbook и validation-артефакты синхронизированы с shipped Phase 3 flow: `README.md` фиксирует local stack с `ml-services/ml-baseline`, а `.planning/phases/03-aggregated-baseline-aware-profiles/03-VALIDATION.md` отражает актуальные regression-команды для backend, frontend и baseline service.

## 2026-03-23

- Для очистки рабочего дерева расширены ignore-правила репозитория: корневой `.gitignore` теперь скрывает IDE/tooling metadata (`.idea/`, `.codex/`), frontend generated artifacts (`frontend/node_modules`, `.next`, `tsconfig.tsbuildinfo`) и общие Python cache directories; добавлены локальные `frontend/.gitignore` и `ml-services/ml-baseline/.gitignore`.
- Для локального запуска актуализированы зависимости окружения: во `frontend` установлен dependency tree с `next`/`eslint-config-next` на `15.5.14`, в `core-backend` успешно скачаны Go-модули, а для каждого ML-сервиса созданы отдельные `.venv` с установленными `requirements.txt` из-за несовместимых pinned версий между `ml-baseline` и остальными Python сервисами.
- Исследована поставка внешней СППР `wimi-server` (WiMi / КЭСМИ Разуматор), включая локальную REST-документацию, бинарные поставки Linux/Windows и открытые внешние источники; добавлен подробный интеграционный гайд `docs/04_wimi_guide.md` с выводами по API, deployment, ошибкам и плану Phase 4 интеграции.
- Из `wimi-server` удалены мусорные файлы `.DS_Store` и `:Zone.Identifier`, а `docs/04_wimi_guide.md` сокращён до прикладного интеграционного guide без инвентаризации поставки и второстепенных деталей.
- В документации проекта зафиксировано текущее рабочее допущение: финальные ML-модели и decision-модель WiMi могут быть встроены на завершающем этапе, а до этого проект развивается contract-first через стабильные внутренние DTO, stub/mock integrations, adapter seams и готовые workflow/persistence/UI вокруг будущих моделей.
- Для Phase 4 собран planning-context в `.planning/phases/04-decision-delivery-to-operator/04-CONTEXT.md`: зафиксированы внутренние `decision_input` / `decision_result`, переходы `aggregated -> decision_pending -> completed`, границы retry/error classification, обязательность WiMi в compose-стеке и честный fixed response в UI до появления реальной decision-модели.
- Phase 4 Wave 0: добавлены RED-scaffolds `core-backend/internal/decision/service_test.go`, `core-backend/internal/kesmi/client_test.go`, расширен `core-backend/internal/http/results_handler_test.go`, а также заведены `wimi-server/scripts/smoke.sh` и `.planning/phases/04-decision-delivery-to-operator/04-UI-SMOKE.md` как tracked validation artifacts для дальнейшего выполнения фазы.
- Для Phase 4 plan `04-01` опубликован decision contract поверх aggregated profile: `docs/01_contract.md` теперь фиксирует `decision_pending` / `completed`, `decision_input`, `decision_result`, а в backend добавлены `internal/decision/contracts.go`, миграция `000008_decision_delivery` и sqlc skeleton для модели `decision snapshot + attempt ledger`.
- Для Phase 4 plan `04-02` backend получил persisted decision delivery path: после Phase 3 aggregation создаётся `decision_pending` snapshot, фоновый relay recovery подхватывает pending-delivery после рестарта, `internal/kesmi` отделяет classifier boundaries для transport/business ошибок, а pre-model terminal projection честно завершается сообщением `analysis_not_implemented_yet`.
- WiMi is now a mandatory internal compose service для Phase 4 smoke verification: добавлены container runtime path, `KESMI_*` env contract и canonical smoke через `wimi-server/scripts/smoke.sh`, где `core-backend` проверяет `http://wimi:8081/Models` изнутри compose-сети.
- Phase 4 operator result: `GET /examinations/{id}/result` переведён на расширенный DTO с `decision` block, а страница `/operator/examinations/[id]/results` теперь показывает честный статус `Не реализовано`, `correlation_id`, `attempt_count`, `raw_response_available` и diagnostics поверх уже существующих metrics/baseline/channel contributions.
- Реально верифицирован WiMi runtime в Docker Compose: для vendor binary потребовался системный `libglib2.0-0`, после чего `wimi-server/scripts/smoke.sh` успешно подтвердил healthy `wimi`, доступность `GET /Models` из `core-backend` и host-side `GET /health`; сам smoke script усилен ожиданием backend health, чтобы убрать стартовую гонку.
- Для Phase 5 plan `05-01` опубликован operational contract: `docs/01_contract.md` теперь фиксирует append-only `audit_event`, runtime endpoints `/health` `/ready` `/metrics`, low-cardinality metric rules и перенос `request_id`/`traceparent` через HTTP, RabbitMQ, baseline и WiMi; добавлен `.planning/phases/05-operational-trustworthiness/05-VALIDATION.md` и RED-scaffolds для backend/Python observability.
- Для Phase 5 plan `05-02` в `core-backend` добавлены миграция `000009_audit_observability`, sqlc query layer и backend package `internal/audit`; auth, admin user/questionnaire mutations, examination finish, processing launch, channel result receipt и decision terminal projection теперь записывают append-only audit events из core-owned source-of-truth boundaries.
- Для Phase 5 plan `05-03` `core-backend` получил общий observability layer: JSON request logging, `request_id`/`traceparent` middleware, контекст trace continuity через processing command/result envelopes и outbound propagation в `baselineclient` и `kesmi`.
- Для Phase 5 plan `05-04` runtime surfaces расширены до operational shape: `core-backend` теперь отдаёт cheap `GET /health`, dependency-aware `GET /ready` и Prometheus `GET /metrics`; frontend получил `/api/health`, `/api/ready`, `/api/metrics`; Python workers и `ml-baseline` получили `/ready` и `/metrics`, а локальный stack дополнился `prometheus` scrape path через `ops/prometheus/prometheus.yml`.
- Для Phase 5 plan `05-04` после реального `docker compose` smoke исправлена runtime-конфигурация baseline integration: `BASELINE_SERVICE_URL` теперь по умолчанию указывает на `http://ml-baseline:8080`, что убирает ложный degraded-state у `core-backend`/frontend readiness при healthy baseline container.
- Для Phase 6 закрыты два audit-gap'а на frontend: история обследований теперь открывает `aggregated` / `decision_pending` / `completed` через канонический result screen, а `/api/ready` и `/api/metrics` используют общий live probe, поэтому dependency metrics больше не захардкожены и честно отражают состояние `core-backend`.
- Для Phase 7 по артефактам верификации Phases 1-6 повторно синхронизированы `.planning/REQUIREMENTS.md`, rerun milestone audit `v1.0` и зафиксировано, что прежние блокеры были stale auditability gaps, а не актуальные дефекты репозитория.
- Выполнен ретро-аудит UI и записан `.planning/phases/07-verification-evidence-and-requirement-revalidation/07-UI-REVIEW.md`: итоговая оценка `14/24`, ключевые замечания касаются операторского copy на экране результатов, отсутствия подтверждения destructive actions и неполного покрытия loading/empty states в admin/operator списках.
- Инициализирован milestone `v1.1 UI & Admin Completion`: созданы новые `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` и `.planning/STATE.md` для frontend-first roadmap из Phases 8-11; отдельно зафиксировано, что исправления из `07-UI-REVIEW.md` входят в Phase 11, включая очистку result copy, confirm/success feedback для edit/destructive действий и нормализацию list-state UX.
- Для Phase 8 собран `.planning/phases/08-ui-contours-design-foundation/08-CONTEXT.md`: зафиксированы два явно разных UI-контура (`operator` и `admin`) на общей design-system, корпоративное минималистичное visual direction, shared sidebar pattern, обязательные skeleton/alert/toast/confirmation primitives и решение ввести отдельную admin landing page вместо implicit `/admin/users`.
- Для Phase 8 создан и одобрен `.planning/phases/08-ui-contours-design-foundation/08-UI-SPEC.md`: контракт фиксирует `shadcn/ui` как каноническую базу design-system milestone, строгий корпоративный visual style, 4-size / 2-weight typography scale, 4-based spacing scale, semantic status colors, skeleton-first loading contract и first-read visual hierarchy для operator/admin landing screens.
- Для Phase 8 подготовлен полный planning-пакет: `.planning/phases/08-ui-contours-design-foundation/08-RESEARCH.md` сузил фазу до shell/primitives/redirect scope без stack migration, а `08-01-PLAN.md`..`08-03-PLAN.md` разбили работу на три последовательных шага — foundation, contour shells/role-home contracts и финальное wiring `/admin` + layout/loading verification.

## 2026-03-24

- В Python-сервисах `ml-text`, `ml-acoustic`, `ml-paralinguistic` и `ml-baseline` ослаблены `requirements.txt`: точечные version pins сняты, зависимости оставлены без фиксированных версий, чтобы локальная сборка и установка меньше зависели от наличия конкретных пакетов в дистрибутиве.
- Для Phase 8 plan `08-01` в `frontend/app/globals.css` и `frontend/tailwind.config.ts` зафиксирован единый foundation token layer: корпоративные surface/sidebar aliases, 4-based spacing scale, semantic success/warning/danger colors, radius/motion tokens и общие theme aliases для Tailwind без смены Next 15 / Tailwind 3 стека.
- Нормализованы shared UI primitives `button`, `card`, `page-header`, `alert`, `badge`, `input`, `textarea`, `empty-state`: компоненты переведены на единый typography/spacing rhythm, restrained motion и semantic state styling вместо разрозненных локальных значений.
- Добавлены недостающие shared feedback primitives Phase 8: `frontend/components/ui/skeleton.tsx`, `confirm-dialog.tsx` и `toaster.tsx`; `AppProviders` теперь монтирует один app-level toaster, чтобы operator/admin контуры могли переиспользовать toast/confirmation/loading UX без page-local wiring.
- Во `frontend/package.json` обновлены scripts `lint` и `build` на прямые `node` entrypoints и зафиксированы зависимости `@radix-ui/react-alert-dialog`, `sonner`, `yocto-queue`; это обошло повреждённые локальные `.bin` shim-файлы и вернуло воспроизводимую `npm run lint && npm run build` в текущем workspace.
- Для Phase 8 plan `08-02` добавлен канонический helper `frontend/lib/navigation/role-home.ts` с Vitest-покрытием, а shell-контур разделён на общий `AppShell` и два wrapper-компонента `OperatorShell`/`AdminShell`; root/login/guard redirects теперь используют единое role-home mapping вместо локальных `/admin/users` литералов.
- Для Phase 8 plan `08-03` auth-entry и role-guard transitions переведены на skeleton-first surfaces: login больше не мигает формой при активной сессии, а `RouteGuard` не показывает чужой контур во время role mismatch и использует shared loading primitives вместо старого centered spinner box.
- Для Phase 8 plan `08-03` маршрут `/admin` стал явной overview-страницей с административными entry points, а оба UI-контура получили собственные `loading.tsx`, чтобы operator/admin переходы показывали разные skeleton surfaces в соответствии с contour-specific reading order.
- Для локального Phase 8 compose-smoke восстановлена frontend container-сборка: в репозиторий добавлен пустой `frontend/public/`, чтобы production Dockerfile с `COPY --from=builder /app/public ./public` больше не падал на проектах без статических asset-файлов.
- Исправлен runtime-start `wimi` в compose: `wimi-server/scripts/entrypoint.sh` переписан с LF line endings вместо CRLF, из-за чего контейнер перестал падать на `/usr/bin/env: 'sh\\r'`, снова стал `healthy` и подтверждён ответ `GET /Models` внутри контейнера.
- Для локального Docker Desktop/WSL запуска стабилизирован `prometheus` bind mount: compose теперь монтирует весь каталог `ops/prometheus/` в `/etc/prometheus`, а не одиночный `prometheus.yml`, что убирает file-vs-dir mount error на некоторых WSL bind-paths.
- Для локального compose запуска освобождён host port `8080`: `core-backend` теперь по умолчанию публикуется как `localhost:18080 -> container:8080`, а `.env.example`, `README.md` и `docs/01_contract.md` синхронизированы с новым external default без изменения внутренних compose-адресов.
- Исправлен frontend/backend runtime split для Compose: browser-side frontend теперь использует `NEXT_PUBLIC_API_URL=http://localhost:18080`, а server-side Next handlers и readiness probe используют отдельный `INTERNAL_API_BASE_URL=http://core-backend:8080`; соответствующие изменения внесены в `frontend/lib/constants.ts`, auth BFF routes, `core-readiness.ts`, `frontend/Dockerfile`, `docker-compose.yml`, `.env`, `.env.example`, `README.md` и `docs/01_contract.md`.
- Исправлен browser request routing во frontend: `frontend/lib/api/client.ts` больше не префиксует `NEXT_PUBLIC_API_URL` для same-origin BFF endpoints `/api/*`, поэтому login/logout/refresh/session снова идут в frontend routes, а не в `core-backend` напрямую.
- Для Phase 9 plan `09-01` завершён admin users workflow: `/admin/users` получил явные loading/empty/error/populated states через shared `user-list`, а страницы создания и редактирования переведены на общий `user-form` с валидацией, disabled submit во время mutation, toast/alert feedback и cache invalidation после успешного сохранения.
- Для той же поставки исправлен backend audit gap: `core-backend/internal/auth.Service.GetUserByID` больше не пишет `admin.user_updated` при открытии `/admin/users/{id}`, а regression tests закрепляют, что update-side audit остаётся только на mutation path `UpdateUser`.
- Для Phase 9 plan `09-02` завершён questionnaire management UI: `/admin/questionnaires` переведён на shared `questionnaire-list` с explicit loading/empty/error/populated states, а create/edit маршруты используют общий `questionnaire-builder` с field-array редактированием, явными кнопками reorder, confirm-dialog на удаление вопроса, publication toggle и toast/alert feedback без изменения backend-контракта.
- Для финального polish-pass Phase 9 `/admin` сфокусирован на users/questionnaires как на завершённых сценариях текущего этапа, а runtime verification ведётся отдельно на production frontend `:3100`, потому что локальный dev-server пользователя уже занимает `localhost:3000` и не должен ломаться нашими проверками.
- По прямому указанию пользователя Phase 9 закрыта без обязательного runtime/manual verification: зафиксированы кодовые изменения, automated checks (`go test`, `eslint`, `tsc`, `next build`) и planning artifacts, а live admin smoke оставлен на усмотрение следующей итерации.
- Для Phase 10 plan `10-01` `/admin/monitoring` перестал быть health-only карточкой: страница теперь показывает liveness, readiness, dependency matrix и один curated technical indicator через существующие frontend BFF routes `/api/ready` и `/api/metrics`, с explicit loading/error states и без raw Prometheus dump в UI.
- Для Phase 10 plan `10-02` `core-backend` получил admin-only `GET /audit/events`: endpoint читает существующий append-only audit trail без отдельной read-model, поддерживает фильтры `event_type`, `resource_kind`, `resource_id`, `from`, `to`, `limit`, а контракт зафиксирован в `docs/01_contract.md`.
- Для Phase 10 plan `10-03` в admin contour добавлен отдельный экран `/admin/audit`: он доступен из системной навигации, использует новый backend contract `GET /audit/events`, даёт явные loading/empty/error states и показывает администраторам читаемую ленту audit events с фильтрами по типу события, периоду и связанному объекту без raw payload dump.
- Для Phase 10 plan `10-04` placeholder `/admin/settings` заменён на реальный persisted control surface: `core-backend` получил таблицу `system_settings`, admin `GET/PUT /settings`, audit event `admin.settings_updated`, а frontend — валидированную форму с load/save flow для `audio_retention_ttl_days`, `processing_max_attempts` и `kesmi_max_retries`.
- Для Phase 11 plan `11-01` operator results screen перестроен из DTO/debug вида в interpretation-first surface: основная область теперь объясняет итог обследования, baseline-сравнение, ключевые показатели и вклад каналов на операторском языке, а correlation/attempt/diagnostics вынесены в отдельный вторичный блок технических деталей.
- Для Phase 11 plan `11-02` ключевые operator mutations получили явный feedback: upload/start/finish/update/delete теперь подтверждаются toast-сообщениями, удаление карточки специалиста требует confirm-dialog, а intake/processing copy переписан на операторский язык без изменения backend-поведения.
- Для Phase 11 plan `11-03` нормализованы `loading` / `empty` / `filtered-empty` / `error` состояния на `/operator`, `/operator/history` и `/operator/specialists/[id]`: добавлены skeleton-first загрузки, разделены первый пустой запуск и пустой результат фильтра, specialist history больше не сваливается в generic warning state.
- Для финального Phase 11 plan `11-04` выполнен shipped-UX finish pass: из operator results/processing убран остаточный технический wording, `/admin` обновлён под реально доступные users/questionnaires/monitoring/audit/settings surfaces, а phase validation и planning artifacts синхронизированы как complete.
- Для post-UAT Phase 11 plan `11-05` закрыт главный blocker operator flow: `GET /questionnaires` теперь доступен для `operator/admin` как read-only список, RBAC regression tests закрепляют split между list-read и admin-only questionnaire mutations, а `docs/01_contract.md` синхронизирован с runtime ACL.
- Для post-UAT Phase 11 plan `11-06` закрыты два оставшихся operator empty-state дефекта: пустая история больше не дублирует CTA запуска обследования, а dashboard теперь различает полностью пустую базу специалистов и active-search filtered-empty сценарий.
- Для Phase 12 plan `12-02` опубликованы и реализованы enriched registry DTO: пользователи теперь отдают `last_login_at`, опросники — `usage_count` / `last_used_at` / `last_editor`, а специалисты — summary по последнему обследованию и baseline; для этого в core backend добавлена миграция `000011_ui_registry_support`, обновлены SQL read-model queries и синхронизированы frontend API types.
- Для Phase 12 plan `12-01` ужесточены shared entry surfaces: экран `/login` переведён в компактный form-first enterprise layout без developer-facing copy, а `AppShell`/`OperatorShell`/`AdminShell` очищены от декоративных summary-блоков и длинных пояснений в боковой навигации.
- Для Phase 12 plan `12-03` operator overview surfaces переведены в плотный рабочий режим: `/operator` теперь показывает реальную текущую нагрузку и последние готовые кейсы через общие registry widgets, а `/operator/specialists` и `/operator/history` стали компактными реестрами с baseline/последней активностью, статусными фильтрами и честной обработкой query errors без выдуманных данных.
- Для Phase 12 plan `12-04` административный контур переведён в компактный режим контроля: `/admin` теперь показывает реальные сводки по доступу, опросникам, обследованиям, состоянию системы и последним audit-событиям, реестры `/admin/users` и `/admin/questionnaires` получили поиск/фильтры/сортировку по реальным полям из `12-02`, а `/admin/monitoring` переписан на прикладной русский язык с честным описанием доступных сигналов и текущих ограничений измерений.
- Для Phase 12 plan `12-05` операторские detail/in-session поверхности переведены в плотный рабочий режим: `/operator/specialists/[id]` теперь показывает короткую summary по последнему статусу, baseline и готовым результатам с табличным журналом обследований, а `/operator/examinations/[id]`, `/processing` и `/results` получили progress-first intake, явный список сохранённых ответов, структурные skeleton-first loading states и очищенный операторский wording без raw contract/debug ключей.
- Для Phase 12 plan `12-06` ужесточены edit-surfaces администратора: карточка пользователя теперь показывает деловой контекст доступа и честный `last_login_at` fallback без внутренних audit-ремарок, конструктор опросника стал компактнее и предупреждает о несохранённых изменениях, а `/admin/settings` перегруппирован в пользовательские блоки хранения и повторных попыток без developer-facing wording.
- Закрывающая запись по Phase 12 `12-01`: экран входа и общий shell переведены в компактный рабочий режим без декоративных summary-блоков и developer-facing copy, при этом auth redirects, validation и logout flow оставлены без контрактных изменений.
- Закрывающая запись по Phase 12 `12-02`: backend и контракты получили ограниченную поддержку плотных реестров через `last_login_at`, `usage_count`, `last_used_at`, `last_editor` и derived specialist summary-поля без новых summary endpoints и без изменения source-of-truth workflow.
- Закрывающая запись по Phase 12 `12-03`: операторский dashboard, список специалистов и журнал обследований собраны в плотные operational registries с KPI, фильтрами, статусными группами и только на реальных данных из существующих `/specialists`, `/examinations` и result-history surfaces.
- Закрывающая запись по Phase 12 `12-04`: административный home, пользовательский и questionnaire-реестры, а также monitoring screen сведены к кратким control surfaces с реальными summary-метриками, поиском/сортировкой и честным описанием границ доступной observability.
- Закрывающая запись по Phase 12 `12-05`: карточка специалиста, intake обследования, processing и results pages переведены в progress-first режим с явным контекстом baseline, списком сохранённых ответов, табличной историей и operator-facing переводом contract-shaped полей.
- Закрывающая запись по Phase 12 `12-06`: admin edit surfaces для пользователя, опросника и настроек ужаты до деловых форм с truthful fallbacks, предупреждением о несохранённых изменениях и пользовательским grouping без внутренних технических ремарок.
- После Phase 12 устранён runtime-regression на `GET /questionnaires`: questionnaire read-model теперь принудительно `COALESCE`-ит `usage_count` в `0`, поэтому опросники без обследований больше не падают на PostgreSQL/sqlc scan и снова отдаются через API корректно.
- После Phase 12 устранён frontend runtime-crash на `/admin`: список последних audit-событий теперь честно обрабатывает `actor=null` для system-originated events, а TypeScript тип `AuditEvent` синхронизирован с контрактом `docs/01_contract.md`.
- После Phase 12 выровнен audit read-contract и frontend fallback-логика: `GET /audit/events` больше не возвращает PascalCase-поля, а admin summary/audit screens корректно обрабатывают события без `actor` или `resource`, не падая в браузере.
- После Phase 12 устранено бесконечное мигание `/login` после logout: при выходе `AppShell` теперь сбрасывает TanStack Query cache для `["session"]`, поэтому login page больше не видит stale cached session и не уходит в ложный redirect обратно в контур роли.
- После Phase 12 исправлен повторный вход после logout: logout больше не записывает `null` в `["session"]`, а успешный login сразу прогревает session cache пользователем, поэтому role-guard не зависает на пустой странице при переходе в `/admin` или `/operator`.

- После Phase 12 устранён пустой экран после повторного входа: `RouteGuard` больше не возвращает `null` при кратком отсутствии session-data и вместо этого держит skeleton + redirect backstop на `/login`, поэтому logout/login race не оставляет `/admin` и `/operator` с пустым контентом.

- Исправлен операторский flow сохранения ответов: `GET /examinations/{id}` теперь отдаёт snapshot-массив `questions`, `POST /answers` переведён в audio-only режим без ручного текстового ввода, frontend отправляет реальные `examination_question_id` и `specialist_id`, а экран записи ответа получил более интерактивный recorder UI с progress/state feedback.

- Исправлен runtime-crash на `/operator/examinations/[id]/processing`: frontend processing screen теперь понимает legacy channel statuses `temporary_error` / `fatal_error` наряду с UI aliases `failed_temporary` / `failed_fatal` и имеет neutral fallback для неизвестных значений, поэтому badge rendering больше не падает на `variant`.

- Исправлен Phase 4/5 drift у `GET /examinations/{id}/processing-status`: backend больше не режет post-processing статусы `decision_pending` и `completed`, frontend processing screen понимает эти состояния и показывает корректные операторские сообщения вплоть до финальной рекомендации.

- Исправлен `500` на `GET /examinations/{id}/result`: repository больше не пытается сканировать PostgreSQL `generated_at` (`timestamptz`) прямо в строку, а нормализует его через `time.Time` в RFC3339 перед ответом operator UI.
- Для milestone `v1.2 Operator UI` полностью перестроен operator-facing interaction model: `/login` переведён в один центрированный form-only screen, operator shell получил фиксированную по viewport боковую панель с logout у нижнего края, а `/operator`, `/operator/specialists` и `/operator/history` пересобраны в search-first и list-first рабочие поверхности без dashboard-карточек и поясняющего текста.
- Для `v1.2` упрощён операторский examination flow: экран `/operator/examinations/[id]` теперь оставляет только статус управления сессией, прогресс, текущий вопрос и аудиорекордер, а `media-recorder-card` очищен от дублирующих explanatory blocks при сохранении существующего audio-only save path.
- В начале milestone `v1.3 Admin UI Simplification` закрыт Phase 16: admin shell убран до label-only навигации без overview-групп и описаний, `/admin` заменён на прямой redirect в `/admin/users`, а loading-state административного entry surface больше не имитирует dashboard-карточки.
- В Phase 17 milestone `v1.3` users и questionnaires переведены в compact table-first registries: убраны `Открыть`-кнопки, usage-heavy summary-метаданные и описательные тексты, а открытие записи перенесено на клик по строке.
- В Phase 18 milestone `v1.3` audit и monitoring сведены к тихим operational read-surfaces: журнал аудита стал компактной таблицей с раскрытием строки, а мониторинг — одной краткой таблицей статусов без summary-карточек, action-панелей и developer-facing пояснений.
- В Phase 19 milestone `v1.3` admin edit surfaces ужаты до single-column forms: user/questionnaire/settings экраны больше не используют правые summary-панели, а в редакторе опросника порядок вопросов теперь можно менять нативным drag-and-drop.
- Исправлен admin shell layout: боковая панель администратора теперь фиксируется по высоте viewport, как и operator shell, больше не растягивается вместе с основным контентом, а заголовок контура переименован из `Администрирование` в `Администратор`.
- Унифицированы фильтр-панели operator UI: на `/operator`, `/operator/specialists` и `/operator/history` поисковые и статусные фильтры теперь оформлены в том же паттерне, что и на странице аудита, с подписью над каждым полем или группой фильтра.
- Для admin-контуров унифицировано оформление filter/search-панелей с audit screen: в реестрах пользователей и опросников поиск переведён на тот же `Input`-паттерн с подписью над полем, чтобы фильтры визуально совпадали со страницей аудита.
- Ослаблены глобальные UI-тени во frontend design tokens: `shadow-panel` и `shadow-soft` в `frontend/app/globals.css` уменьшены, чтобы кнопки, карточки, alerts и toast больше не давали тяжёлые размазанные тени на светлых экранах.
- Глобальные frontend-тени дополнительно ужаты почти до плоского состояния: `shadow-panel` и `shadow-soft` ещё раз уменьшены, чтобы элементы отделялись от фона в основном за счёт границ, а не заметного свечения.
- Исправлен layout всех filter labels в operator/admin UI: паттерн с подписью над полем переведён с inline `label` на `flex-col`, поэтому подписи `Поиск`, `Роль`, `Статус`, `Сортировка`, даты и лимиты теперь гарантированно стоят сверху, а не уезжают в одну строку с контролом.
- Предупреждение о несохранённых изменениях в admin questionnaire editor вынесено из самого контейнера формы в правый sticky sidebar на страницах создания и редактирования опросника, чтобы конструктор оставался чистым и не раздувал основной рабочий блок.
- На operator intake screen убран сценарий `без опросника`: `/operator/examinations/new` теперь позволяет запускать обследование только с реально существующим опросником, показывает честное пустое состояние `Нет опросников`, если список пуст, и блокирует старт без выбранного специалиста и опросника.
- Главная страница оператора перестроена под action-first flow: вместо dashboard-таблицы теперь есть hero-действие `Начать обследование`, центральный быстрый поиск со мгновенной фильтрацией и кликом по строке без кнопки `Открыть`, компактный блок `Продолжить работу` на 3–5 активных кейсов и строка мини-статистики без карточек.
- Уточнён operator flow выбора и открытия: на `/operator/examinations/new` карточки опросников больше не показывают `Активен/Черновик`, а отмечают только выбранный вариант; на `/operator/specialists` реестр упрощён до имени, количества исследований и даты последнего исследования, а открытие карточки перенесено с кнопки на клик по всей строке.
- В нижнем блоке sidebar role label переведён на русский: shared `AppShell` теперь показывает `Администратор` и `Оператор` вместо backend-названия роли из session payload.
- Перед переходом к реализации реальных ML-каналов выполнен stabilization pass: backend `go test ./...`, frontend `lint`/`typecheck`/unit tests/production build и контейнерные pytest-проверки всех четырёх Python-сервисов проходят; в `frontend/package.json` добавлен явный `typecheck` script, а README обновлён под Docker-based Python test workflow для окружений без `python3.12-venv`.
- Исправлен WiMi smoke-runbook: `wimi-server/scripts/smoke.sh` переведён в исполняемый LF shell-script, больше не запускает принудительный `--build`, работает с TTY-less `docker compose exec -T` и проверяет backend health через актуальный порт `18080`.
- Паралингвистический worker переведён со stub payload на реальный lightweight VAD-анализ: audio object декодируется через local `ffmpeg`, speech-сегменты выделяются WebRTC VAD, payload возвращает `features`/`scores`/`quality_flags`/`evidence`/`model_version=paralinguistic-vad-v1`, а расчёт признаков покрыт unit-тестами на искусственных VAD-сегментах; contract и env config синхронизированы в `docs/01_contract.md`, `.env.example` и `docker-compose.yml`.
- Акустический worker переведён со stub payload на explainable feature extraction через `ffmpeg` + `librosa`/`numpy`: считаются RMS, zero-crossing rate, spectral centroid/bandwidth/rolloff, MFCC mean/std, F0 mean/std/valid ratio, intensity variability, `voice_stability_score` и `acoustic_stress_score`; payload возвращает `features`/`scores`/`quality_flags`/`evidence`/`model_version=acoustic-librosa-v1`, а расчёт покрыт unit-тестами на синтетическом сигнале и low-quality аудио.
- В `text-worker` добавлена локальная STT-интеграция через lazy-loaded `faster-whisper`: worker читает audio object из S3, транскрибирует внутри text-channel, возвращает `stt.transcript`, `language`, `segments`, `duration_ms`, `word_count` и `model_version`, а при ошибке модели/транскрибации сохраняет shape результата с `quality_flags=["stt_failed"]`; добавлены env `STT_MODEL_PATH`, `STT_MODEL_SIZE`, `STT_DEVICE`, `STT_COMPUTE_TYPE`, `HF_HUB_OFFLINE`, а host `./models` монтируется в контейнер как `/app/models:ro`.
- `text-worker` расширен с STT-only payload до реального анализа transcript: добавлен lazy-loaded RuBERT-compatible emotion classifier (`TEXT_EMOTION_MODEL_PATH=/app/models/text-emotion`, fallback `seara/rubert-tiny2-russian-emotion-detection-ru-go-emotions`) и explainable-признаки/score-ы `features`, `scores`, `emotion_probs`; при недоступной emotion-модели канал возвращает heuristic features/scores и `quality_flags=["emotion_model_failed"]`, не принимая финальных решений.
- Зафиксирован единый canonical channel result payload для `text`, `acoustic`, `paralinguistic`: successful payload теперь содержит `channel`, `status`, `examination_id`, `answer_id`, `features`, `scores`, `quality_flags`, `evidence`, `model_version`, `processing_time_ms`, `error`; старые stub/proxy поля удалены из worker payload, core backend валидирует canonical payload перед сохранением, а aggregator строит профиль по реальным `scores` каналов.
- `ml-baseline` переведён с proxy-oriented robust snapshot на MVP baseline по реальным стабильным метрикам: сервис принимает `existing_baseline` и workflow `context`, считает `general|personal` deviation с `z_score`/`deviation_level`, готовит кандидатный EWMA baseline (`baseline_mean`, `baseline_std`, `sample_count`, `method`) и отдаёт `data_reliability`/`significant_deviations`; при этом core backend больше не обновляет `specialist_baseline_states` на этапе aggregation, а применяет candidate snapshot только после завершения decision-stage и только если обследование прошло eligibility-гейт.
- Aggregator v2 переведён на параметрический decision payload для КЭСМИ: `core-backend` теперь собирает mandatory channel scores (`acoustic`, `text`, `paralinguistic`), baseline source/deviation/z-scores, derived indicators и `data_reliability` в canonical `decision-input-v2`; для этого в baseline snapshot persistence добавлен `baseline_metric_scores`, а WiMi client отправляет в `incommingParameters` уже structured payload вместо пустой заглушки.
- Интеграция с реальной decision-моделью WiMi привязана к `wimi-server/models/specialists_model_v2_decision.xml`: контейнер `wimi` теперь копирует каталог `models` в образ и автозагружает XML через `POST /Models` при старте, а `core-backend/internal/kesmi` маппит наши входы в официальный WiMi-формат массива `{id, value}` по model parameter IDs `i1..i22` и запрашивает выходы `final_decision (p32)` и `final_summary (p33)`.
- WiMi `ModelCalc` дополнительно сужен по `service.outputFields`: backend теперь явно просит только `requiredExploredParameters` и `notRequiredExploredParameters`, чтобы raw response не захламлялся `algorithm`, `timing` и прочими диагностическими блоками при обычной decision delivery.
- WiMi `ModelCalc` упрощён ещё на один шаг: backend теперь запрашивает только `requiredExploredParameters`, так как `notRequiredExploredParameters` в текущей decision-модели не возвращает полезных данных. Подробный отчёт планируется строить на стороне backend/front по сырым channel scores, а не через расширение WiMi-ответа.
- Decision delivery больше не сохраняет placeholder при успешном `ModelCalc`: backend теперь парсит `requiredExploredParameters` (`p32=final_decision`, `p33=final_summary`), нормализует coarse `recommendation` (`allow -> allowed`, `monitoring|extended_check -> risk`, `no_access -> denied`) и дополнительно отдаёт в публичный result-contract структурированные поля `decision_code`, `risk_class`, `patterns`. Некорректный `200 OK` от WiMi теперь трактуется как `business_error` с `invalid_kesmi_response`.
- Operator result page расширен под реальный вывод КЭСМИ: `GET /examinations/{id}/result` теперь дополнительно возвращает `channel_reports` с explainable score-метриками по каждому каналу (`channel`, `model_version`, `quality_flags`, `evidence`, `scores[]`), а frontend показывает карточки `decision_code / risk_class / patterns` и открывает модальный “Подробный отчёт” со всеми model scores и цветовой дифференциацией.
- Operator-facing блок КЭСМИ дополнительно локализован: вне “Подробного отчёта” экран больше не показывает сырые английские коды `decision_code`/`risk_class`, а рендерит только русские подписи решения, класса риска и симптомокомплексов; оригинальные поля остаются только в модальном подробном отчёте.
- Result page теперь дополнительно опрашивает `GET /examinations/{id}/processing-status` тем же polling-механизмом, что и processing screen, и показывает operator-facing блок `Статусы каналов` с живыми состояниями `queued / processing / retry_scheduled / succeeded / failed_fatal / exhausted` в human-readable виде без изменения backend-контракта результата.
- Для ручной отладки decision-модели `wimi` ранее временно публиковался на host-port `8081`, но эта схема позже была убрана: актуальный локальный compose снова держит WiMi internal-only, а smoke/debug проверки выполняются изнутри compose-сети.
- Исправлено падение модалки “Подробный отчёт” на result page: frontend теперь терпимо обрабатывает `channel_reports[].quality_flags` и `channel_reports[].evidence`, когда backend временно отдаёт `null` вместо пустого массива, и больше не читает `.length` у `null`.
- Result page оператора полностью переработана под сценарий `итог → причины → действия → детали`: монолитный экран разбит на компоненты `ResultHeader`, `DecisionHero`, `KeyReasons`, `ChannelContribution`, `BaselineSummary`, `OperatorActionCard`, `ExpertTraceSummary`, `DetailedReportModal`; убран визуальный шум, решение вынесено в hero-блок, baseline и вклад каналов объясняются человеческим языком, а технические score-поля оставлены только в подробном отчёте.
- Result page оператора получила второй UX-pass: hero ужат до компактного итогового блока с риском, решением и симптомами; с основного экрана убраны блоки `Что сделать дальше`, `КЭСМИ` и цепочка вывода, `Почему такой вывод` сокращён до компактных причин с источником сигнала, `Вклад каналов` переименован в `Сигналы по каналам анализа`, baseline стал компактнее и показывает предупреждение о недостаточной истории только условно, а статусы обработки спрятаны в сворачиваемый технический блок.
- Ослаблено влияние незрелого baseline на decision payload: если personal baseline ещё недоступен (`baseline_available=false`), backend больше не передаёт general fallback как полноценный сильный сигнал в КЭСМИ. `baseline_deviation_index`, baseline `z_scores` и производный `baseline_shift_index` теперь аттенуируются по зрелости истории, чтобы общая норма не доминировала над реальными каналами до накопления достаточного числа качественных обследований.
- Акустический worker переведён с hand-crafted `librosa`-пайплайна на SER-модель `xbgoose/hubert-large-speech-emotion-recognition-russian-dusha-finetuned`: worker нормализует аудио в mono `16 kHz`, лениво грузит локальную или HF-модель по `ACOUSTIC_MODEL_PATH` / `ACOUSTIC_MODEL_ID`, возвращает `emotion_probs`, `dominant_emotion`, новый explainable `acoustic_stress_score` и `voice_stability_score`, а `intensity_variability_score` оставляет как совместимый вспомогательный сигнал для текущего aggregator/KЭСМИ mapping.
- `text-worker` переведён на multi-label emotion backend `seara/rubert-tiny2-russian-emotion-detection-ru-go-emotions`: default runtime и `.env.example` обновлены, inference для go-emotions теперь использует `sigmoid` вместо `softmax`, а исходные эмоции нормализуются к существующим backend-owned ключам `joy/sadness/anger/fear/surprise/neutral`, чтобы не ломать aggregator, КЭСМИ и operator-facing payload.
- Исправлена агрегация multi-label go-emotions в `text-worker`: при схлопывании нескольких исходных меток в один канонический bucket (`joy/sadness/anger/fear/surprise/neutral`) больше не используется простая сумма вероятностей, вместо этого применяется `noisy-or`, чтобы `emotion_probs` оставались в диапазоне `0..1` и не раздували `text_negativity_score`/`text_anxiety_score` на моделях `seara` из-за накопления нескольких близких эмоций.
- Для локальной отладки качества STT в `text-worker` добавлен opt-in debug logging transcript-ов: при `TEXT_DEBUG_LOG_STT_TRANSCRIPTS=1` worker пишет в собственные логи per-answer и aggregate `stt_debug` строки с распознанным текстом, а длина текста ограничивается `TEXT_DEBUG_LOG_STT_MAX_CHARS`; по умолчанию это выключено, чтобы не логировать чувствительные данные без явного запроса.
- Усилена чувствительность текстового канала к явно негативным/дистрессовым сценариям: `text-worker` получил explainable `distress_marker_count` по русскоязычным маркерам неблагополучия (`переживаю`, `не готов`, `плохо`, `болит`, `не спал`, `ужас` и т.п.), формулы `text_negativity_score`, `text_anxiety_score` и `text_confidence_score` стали более реактивными к накоплению негативных эмоций и distress-маркеров, а backend `text_risk_signal` и `semantic_stress_index` больше не усредняют выраженный красный текст обратно в слабый сигнал. На ручном green/red E2E-паре это подняло `text_risk_signal` с `0.0771` до `0.8901`, а вклад text-канала в `overall_deviation_index` — с `0.0254` до `0.2937`.
- Baseline bootstrap и decision influence приведены к ожидаемому поведению: history для personal baseline теперь собирается только из финально принятых baseline snapshot-ов, `risk` больше не блокирует само накопление baseline bootstrap, а до появления реальной личной нормы general fallback полностью исключён из decision payload для КЭСМИ (`baseline_deviation_index`, `baseline_shift_index`, baseline `z_scores` обнуляются). Operator UI baseline-блока больше не называет fallback `личной нормой` и явно показывает, когда personal baseline ещё формируется.
- Смягчён паралингвистический канал под спонтанную устную речь: повышен порог `long pause`, снижен sensitivity `low_speech_ratio`, формулы `hesitation_score` и `speech_disorganization_score` стали менее агрессивными к естественным паузам и фрагментации ответа. В агрегаторе paralinguistic channel получил наименьший вес (`0.20` против `0.40` у text и acoustic), чтобы канал оставался дополнительным сигналом, а не главным источником риска. На green e2e это снизило вклад канала и перевело итоговое решение с `high / extended_check` на `medium / monitoring`.
- Выполнена мягкая калибровка WiMi-модели `specialists_model_v2_decision.xml` под green-case без изменения backend-контрактов: ослаблены rules для `acoustic_activation`, `tempo_disturbance`, `voice_instability`, `pause_overload`, `hesitation_overload`, `latency_problem`, `acoustic_alarm`, `hidden_stress`, `cognitive_overload`, `fatigue_state`, `masked_distress`, `contradictory_profile`, `risk_retest`, а также убран ложный триггер `baseline_alarm=="unavailable"` из `operator_action`. После перезагрузки модели живой green E2E (`exam #24`) начал завершаться `risk=low; decision=allow; patterns=none`, тогда как red-case на тех же мягких правилах остаётся не зелёным (`risk=medium; decision=monitoring`).
- Доведена балансировка WiMi между green и red кейсами по текстово-эмоциональной ветке: в `emotional_cross` и `risk_emotional` усилено срабатывание для сценария `negative_affect=high + text_alarm=high + acoustic_alarm!=low`. После перезагрузки модели ручной `ModelCalc` и живой red E2E (`exam #26`) снова дают `risk=high; decision=extended_check; patterns=emotional_cross;baseline_sensitive_case;`, при этом green E2E (`exam #24`) сохраняет `risk=low; decision=allow; patterns=none`.
- В operator recording flow добавлен demo-friendly fallback для загрузки ответа из готового аудиофайла: в `MediaRecorderCard` рядом с кнопками записи появилась компактная круглая кнопка с иконкой файла, которая позволяет выбрать локальный `audio/*` файл, прослушать его и отправить через тот же `POST /answers` multipart-маршрут без изменения backend-контрактов.
- Исправлена промежуточная индикация processing-статусов каналов: `GET /examinations/{id}/processing-status` больше не перескакивает визуально из `queued` сразу в `succeeded`, а вычисляет runtime `processing`, если channel run уже опубликован в `processing_outbox` (`published_at` есть), но terminal result ещё не получен. Для такого случая `started_at` теперь выводится из `published_at`, что делает operator UI честнее без введения отдельного worker start-event.
- В `aggregation.LoadExistingBaseline` добавлена обратная совместимость со старым proxy-форматом `specialist_baseline_states.metrics`: устаревшие baseline-state записи больше не роняют `processing.results` consumer и не блокируют доставку новых результатов в обследованиях, пока система дорабатывает baseline на актуальной схеме стабильных метрик.
- Из локального `docker-compose` убран отдельный сервис `prometheus`: runtime `/metrics` endpoints во frontend/backend/ML-сервисах сохранены, но внешний scrape-container больше не поднимается по умолчанию, так как в текущем проекте он не используется в рабочем operator/admin сценарии.

- Экран operator processing переработан из card-grid в workflow UI: убран отдельный header и блок «Что делать оператору», сверху оставлена одна статусная карточка с обследованием, специалистом, текущим этапом, прогрессом и CTA результата, а ниже построен pipeline с параллельными ветвями обязательных каналов (`text`, `acoustic`, `paralinguistic`) и более частым polling (`~1.25s` на активной вкладке, `5s` в фоне) без изменения backend-контрактов.
- Workflow на operator processing screen дополнительно переведён из вертикального списка в компактный горизонтальный DAG/pipeline в стиле CI: шаги схлопнуты до node-блоков с иконкой, статусом и hover-деталями, аналитические каналы расходятся в три ветви и затем сходятся обратно, а live polling на активной вкладке ускорен до `~1s`, чтобы экран ощущался как почти real-time orchestration UI.
- Processing pipeline дополнительно ужат под основной viewport: мгновенные шаги до аналитических каналов убраны из графа, все node-блоки выровнены по одной компактной ширине, подписи внутри сокращены до минимума, канальные ветви названы прямыми именами каналов, а отдельный финальный узел `Результат готов` убран — доступность результата теперь показывается только в верхней статусной карточке.
- Проведён точечный UI/UX-polish operator flow без изменения бизнес-логики: логин получил более продуктовый входной экран, dashboard — компактный hero с inline-метриками и cleaner empty-state для активных обследований, списки специалистов и история переведены на кликабельные строки без отдельных `Открыть`, карточка специалиста очищена от лишних описаний и тяжёлых secondary-blocks, экран нового обследования стал более пошаговым (`специалист -> опросник -> запись ответов`) с compact selected-state и поиском по специалистам, а shell/sidebar получили более спокойный active-state и hover-поведение.
- Проведён точечный UI/UX-polish админ-панели без изменения API и бизнес-логики: страницы пользователей, опросников, аудита, настроек и мониторинга приведены к более плотному корпоративному стилю с ограниченной шириной контента, компактными summary-метриками и фильтрами, кликабельными строками таблиц, унифицированными status-badge’ами, более собранными empty-state’ами и компактной health/dashboard-подачей для мониторинга.
- Фронтенд получил product-level browser metadata polish: глобальный `title`/`title template` и description переведены с технического `diplom Frontend` на человекочитаемую систему названий, для auth/admin/operator сегментов добавлены section titles, а также заведены app icons (`icon.svg`, `apple-icon.svg`) и `manifest.webmanifest` для более цельного поведения вкладок и installable metadata.
- Из локального compose убрана внешняя публикация порта WiMi: `wimi` снова работает только как internal-only сервис внутри compose-сети, а backend продолжает обращаться к нему по `http://wimi:8081` без изменения runtime-логики decision delivery.
- Для развёртывания на другой машине подготовлен deployment handoff: `.gitignore` очищен от локальных моделей/venv/debug-артефактов, `README.md` переписан под полный self-hosted setup со staging/offline/LAN сценариями, добавлены env-профили в `ops/env/`, а `ops/setup/download-models.sh` автоматизирует подготовку STT, text-emotion и acoustic моделей под текущую калибровку.
- Для deploy consistency синхронизированы runtime defaults с текущей калибровкой: рекомендуемая text emotion-модель в env/deploy шаблонах переведена на `seara/rubert-base-cased-russian-emotion-detection-ru-go-emotions`, а paralinguistic env defaults выровнены на `long_pause=1200ms` и `low_speech_ratio_threshold=0.15`.
- Deployment handoff дополнительно обновлён под актуальный Hugging Face CLI: инструкции и `ops/setup/download-models.sh` больше не используют deprecated `huggingface-cli` и убран устаревший флаг `--local-dir-use-symlinks`; подготовка моделей теперь описана через `hf download`.
- В `MediaRecorderCard` добавлен компактный live-таймер записи: во время active recording оператор видит аккуратный `мм:сс` индикатор рядом со статусом записи, без изменения audio upload flow и без утяжеления examination intake UI.
- Логин-экран дополнительно ужат и слегка стилизован: убраны лишние объясняющие надписи, оставлен один короткий заголовок, добавлен спокойный branded акцент через иконку и тонкую верхнюю линию карточки, чтобы экран выглядел более продуктово без усложнения auth flow.
- Admin monitoring упрощён до двух честных health-проверок вместо четырёх частично дублирующих сигналов: экран теперь показывает только `Клиент` (ответ frontend health) и `Сервер` (доступность core backend через readiness probe), а подпись верхнего статуса заменена с `Автообновление` на более точное `Автопроверка`.
- Исправлен auth session transport: `LoginResponse`/`RefreshResponse` теперь возвращают `refresh_expires_in`, frontend BFF выставляет `refresh` cookie по реальному TTL refresh-session, а browser API client делает один автоматический `POST /api/auth/refresh` после `401` и повторяет исходный запрос без ручного relogin.
- Во все три ML-воркера (`text`, `acoustic`, `paralinguistic`) добавлено операционное per-message логирование без чувствительных данных: теперь в логах видны `processing_started`, `audio_objects_loaded`, `processing_succeeded`, `processing_failed` и `result_published` с `examination_id`, `channel`, `attempt`, количеством ответов, `correlation_id` и временем обработки, что позволяет реально отслеживать распределение задач между репликами.
- В `README.md` добавлена отдельная инструкция по репликации ML-воркеров через `docker compose --scale`: зафиксировано, что без архитектурных изменений безопасно масштабируются только `text-worker`, `acoustic-worker` и `paralinguistic-worker`, а `core-backend` и инфраструктурные сервисы пока должны оставаться в одной реплике; также описаны команды запуска и способы проверки consumers/logs.
